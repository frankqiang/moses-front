# Token自动刷新功能 - 快速测试操作手册

## ⚡ 5分钟快速测试

**目标**：快速验证Token自动刷新功能是否正常工作

**前置条件**：
- ✅ 前端开发服务器运行中 (http://localhost:9529)
- ✅ 后端服务已重启（Token配置已生效）

---

## 🎯 测试场景1：正常Token刷新（必测）

### 步骤1：登录系统

1. 打开浏览器访问：http://localhost:9529/login
2. 输入用户名密码登录
3. 登录成功后停留在首页

### 步骤2：准备测试环境

**打开浏览器开发者工具**（按F12）：
- ✅ 切换到 **Network（网络）** 标签
- ✅ 勾选 **Preserve log**（保留日志）
- ✅ 切换到 **Console（控制台）** 标签

### 步骤3：让Token快速过期

在 **Console** 中执行以下代码：

```javascript
// 复制粘贴到Console执行
// 根据是否勾选"记住我"，token可能在sessionStorage或localStorage
const storageKey = 'moses_auth_data';
let authData;
let storage;

// 先尝试从sessionStorage获取（默认情况）
if (sessionStorage.getItem(storageKey)) {
  authData = JSON.parse(sessionStorage.getItem(storageKey));
  storage = sessionStorage;
  console.log('📍 Token存储位置: sessionStorage (未勾选记住我)');
} else if (localStorage.getItem(storageKey)) {
  authData = JSON.parse(localStorage.getItem(storageKey));
  storage = localStorage;
  console.log('📍 Token存储位置: localStorage (已勾选记住我)');
} else {
  console.error('❌ 未找到Token，请先登录');
  throw new Error('未找到Token');
}

console.log('当前Token过期时间:', new Date(authData.token.expiresAt));

// 设置Token为已过期
authData.token.expiresAt = Date.now() - 1000;
storage.setItem(storageKey, JSON.stringify(authData));

console.log('✅ Token已设置为过期状态');
```

### 步骤4：触发API请求

**点击任意菜单**，例如：
- "主数据管理" → "铝箔产品管理"
- 或 "主数据管理" → "料框规格管理"

### 步骤5：观察结果 🔍

#### ✅ 成功的标志

**Network面板应该看到**：
```
1️⃣ GET /v1/... (或其他API)
   Status: 401
   Response: { error: { code: "AUTH_004" } }

2️⃣ POST /v1/auth/refresh-tokens
   Status: 200
   Response: { success: true, data: { access: {...}, refresh: {...} } }

3️⃣ GET /v1/... (重试)
   Status: 200
   Response: 正常数据
```

**Console面板应该看到**：
```
🔄 Token过期，尝试自动刷新...
✅ Token刷新成功
```

**页面表现**：
- ✅ 没有跳转到登录页
- ✅ 页面正常加载数据
- ✅ **用户完全无感知**

#### ❌ 失败的标志

如果看到以下情况，说明有问题：
- ❌ 直接跳转到登录页
- ❌ 显示"登录已过期"
- ❌ Network只看到一次401，没有刷新请求
- ❌ Console报错

### 步骤6：验证Token已更新

在 **Console** 中执行：

```javascript
// 获取Token（兼容两种存储位置）
const storageKey = 'moses_auth_data';
const authData = sessionStorage.getItem(storageKey) 
  ? JSON.parse(sessionStorage.getItem(storageKey))
  : JSON.parse(localStorage.getItem(storageKey));

console.log('新accessToken:', authData.token.access.substring(0, 50) + '...');
console.log('新过期时间:', new Date(authData.token.expiresAt));
console.log('距离过期还有:', Math.round((authData.token.expiresAt - Date.now()) / 60000), '分钟');
console.log('存储位置:', authData.token.storage);
```

**预期结果**：
```
新accessToken: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
新过期时间: [约30分钟后的时间]
距离过期还有: 29 或 30 分钟
```

---

## 🎯 测试场景2：refreshToken过期（可选）

### 快速操作

1. 在 **Console** 执行：

```javascript
// 同时让accessToken和refreshToken都无效
const storageKey = 'moses_auth_data';
let authData, storage;

if (sessionStorage.getItem(storageKey)) {
  authData = JSON.parse(sessionStorage.getItem(storageKey));
  storage = sessionStorage;
} else {
  authData = JSON.parse(localStorage.getItem(storageKey));
  storage = localStorage;
}

authData.token.access = 'invalid_token';
authData.token.refresh = 'invalid_refresh_token';
authData.token.expiresAt = Date.now() - 1000;
storage.setItem(storageKey, JSON.stringify(authData));
console.log('✅ Token已设置为无效状态');
```

2. **点击任意菜单**触发请求

3. **预期结果**：
   - ✅ 显示提示："刷新令牌已过期，请重新登录"
   - ✅ 自动跳转到 `/login` 页面
   - ✅ sessionStorage/localStorage中的Token被清空

---

## 🎯 测试场景3：并发请求（可选）

### 快速操作

1. 让Token过期（同场景1步骤3）

2. 在 **Console** 执行：

```javascript
// 模拟3个并发请求
console.log('发起3个并发请求...');

Promise.all([
  fetch('/v1/users', {
    headers: {
      'Authorization': `Bearer expired_token`,
      'Content-Type': 'application/json'
    }
  }),
  fetch('/v1/products', {
    headers: {
      'Authorization': `Bearer expired_token`,
      'Content-Type': 'application/json'
    }
  }),
  fetch('/v1/equipment', {
    headers: {
      'Authorization': `Bearer expired_token`,
      'Content-Type': 'application/json'
    }
  })
]).then(() => {
  console.log('✅ 所有请求完成');
}).catch(err => {
  console.log('❌ 有请求失败:', err);
});
```

3. **观察Network面板**：
   - ✅ 应该只看到 **1次** `POST /v1/auth/refresh-tokens`
   - ✅ 3个API请求都会重试并成功

---

## 📊 测试结果记录

### 场景1：正常Token刷新

| 检查项 | 结果 | 备注 |
|--------|------|------|
| Token过期返回401 | [ ] 通过 / [ ] 失败 | |
| 自动调用refresh接口 | [ ] 通过 / [ ] 失败 | |
| 刷新成功返回新Token | [ ] 通过 / [ ] 失败 | |
| 原始请求自动重试 | [ ] 通过 / [ ] 失败 | |
| localStorage已更新 | [ ] 通过 / [ ] 失败 | |
| 用户体验无感知 | [ ] 通过 / [ ] 失败 | |

### 场景2：refreshToken过期

| 检查项 | 结果 | 备注 |
|--------|------|------|
| 刷新失败返回401 | [ ] 通过 / [ ] 失败 | |
| 显示友好提示 | [ ] 通过 / [ ] 失败 | |
| 跳转登录页 | [ ] 通过 / [ ] 失败 | |
| localStorage清空 | [ ] 通过 / [ ] 失败 | |

### 场景3：并发请求

| 检查项 | 结果 | 备注 |
|--------|------|------|
| 只调用1次refresh | [ ] 通过 / [ ] 失败 | |
| 所有请求都成功 | [ ] 通过 / [ ] 失败 | |

---

## 🐛 常见问题排查

### 问题1：Token刷新后还是跳转登录页

**可能原因**：
- refreshToken也过期了
- 后端刷新接口返回错误

**排查方法**：
1. 检查Network面板，查看 `POST /v1/auth/refresh-tokens` 的响应
2. 如果返回401，说明refreshToken过期，重新登录即可
3. 如果返回500，说明后端有问题，联系后端

### 问题2：没有触发自动刷新

**可能原因**：
- Token未真正过期
- 前端代码未正确检测过期

**排查方法**：
```javascript
// Console检查Token状态（兼容两种存储）
const storageKey = 'moses_auth_data';
const authData = sessionStorage.getItem(storageKey)
  ? JSON.parse(sessionStorage.getItem(storageKey))
  : JSON.parse(localStorage.getItem(storageKey));
console.log('Token过期时间:', new Date(authData.token.expiresAt));
console.log('当前时间:', new Date());
console.log('是否过期:', Date.now() > authData.token.expiresAt);
console.log('存储位置:', authData.token.storage);
```

### 问题3：Network看不到refresh请求

**可能原因**：
- 请求被浏览器缓存
- 拦截器配置有问题

**排查方法**：
1. 清空浏览器缓存
2. 勾选 Network 面板的 "Disable cache"
3. 刷新页面重新测试

### 问题4：并发场景下多次刷新

**可能原因**：
- 前端队列机制失效

**排查方法**：
1. 检查 Console 是否有 JavaScript 错误
2. 查看 `src/utils/request.js` 的 `isRefreshingToken` 标志
3. 联系前端开发排查

---

## ✅ 测试通过标准

**必须全部满足**：
- ✅ 场景1：所有6项检查通过
- ✅ 用户体验：Token刷新完全无感知
- ✅ Console：无JavaScript错误
- ✅ Network：请求流程正确

**可选**：
- ✅ 场景2：4项检查通过
- ✅ 场景3：2项检查通过

---

## 📞 遇到问题？

**如果测试失败**：
1. 截图保存 Network 和 Console 的内容
2. 记录具体的错误信息
3. 联系前端或后端开发人员

**前端问题**：检查 `src/utils/request.js` 第341-408行
**后端问题**：检查后端日志，确认Token配置

---

## 🎉 测试完成

所有测试通过后：
1. [ ] 在测试记录表中标记"通过"
2. [ ] 通知团队测试结果
3. [ ] 准备部署到生产环境

---

**测试时间**：预计5-10分钟
**测试人**：_________
**测试日期**：_________
**测试环境**：开发环境
**测试结果**：[ ] 通过 / [ ] 失败

**备注**：


---
description: 
globs: 
alwaysApply: false
---
# 表格模块开发规范

## 适用范围
本规则适用于所有包含表格列表的功能模块开发，规定了统一的组件架构、全局组件使用和交互模式，所有新的表格模块和改造的旧模块都需遵循此规范。

## 参考示例
请参考`src/views/master-data/equipment`目录作为表格模块开发的标准示例。该模块展示了如何使用全局组件和配置化方式构建高复用、易维护的表格功能。

## 最佳实践和高级功能

### 1. 类型化开发
- 为表格数据和表单数据定义TypeScript接口
- 使用类型注解提高代码可维护性和开发效率
- 示例：

```typescript
// 设备数据接口
interface Equipment {
  id: number;
  equipmentId: string;
  name: string;
  model: string;
  equipmentType: string;
  installDate: string;
  status: number;
  [key: string]: any;
}

// 表格列配置接口
interface TableColumn {
  prop: string;
  label: string;
  width?: string;
  minWidth?: string;
  align?: 'left' | 'center' | 'right';
  sortable?: boolean | string;
  renderType?: 'status' | 'date' | 'enum' | 'custom';
  formatter?: (row: any, column: any, cellValue: any) => string;
  [key: string]: any;
}
```

### 2. 性能优化
- 大数据量表格使用虚拟滚动
- 复杂表单使用懒加载
- 表格数据使用本地缓存减少请求
- 列表页面添加防抖/节流优化搜索性能

### 3. 高级搜索和过滤
- 实现保存搜索条件功能
- 支持高级筛选器和复杂查询条件
- 筛选条件可视化构建器

### 4. 用户体验增强
- 操作引导和提示
- 列设置拖拽排序
- 自适应布局，支持不同屏幕分辨率
- 表格行内编辑功能

## 目录结构规范
每个表格模块必须遵循以下目录结构：
```
module-name/
├── index.vue                  # 主页面（容器组件）
├── components/                # 子组件目录
│   ├── SearchForm.vue         # 搜索表单组件
│   ├── [Module]Table.vue      # 数据表格组件（如EquipmentTable.vue）
│   ├── [Module]FormDrawer.vue # 表单抽屉组件（如EquipmentFormDrawer.vue）
│   └── ActionBar.vue          # 操作栏组件（可选）
├── docs/                      # 文档目录
│   └── [模块名称]说明文档.md   # 模块详细说明文档（包含需求、接口、数据结构等）
└── mixins/                    # 混入目录（可选）
    └── [功能].js              # 特定功能的混入文件
```

特别说明：
1. 模块说明文档应放在`docs`目录中，而非根目录的README.md
2. 组件命名应与模块相关，如设备模块的表格组件命名为EquipmentTable.vue
3. 表单抽屉组件命名为[Module]FormDrawer.vue，以区别于旧版的表单组件
4. API调用方法不再放在模块目录下，而是集中在`src/api`目录对应的业务模块中

## 全局组件使用规范

### 1. SearchForm组件
- **强制要求**：所有列表搜索表单必须使用全局SearchForm组件
- 通过配置方式定义表单项，而不是手写表单元素
- 支持展开/收起功能，将非主要搜索条件设为折叠项
- 示例用法：

```vue
<search-form
  ref="searchForm"
  :items="formItems"
  :value="formModel"
  :loading="loading"
  @search="handleSearch"
  @reset="handleReset"
>
  <!-- 自定义插槽内容（如有需要） -->
</search-form>
```

### 2. TableToolbar组件
- **强制要求**：所有表格上方的工具栏必须使用全局TableToolbar组件
- 整合了批量操作、列设置、导入导出和自定义操作按钮
- 支持左右两侧自定义内容插槽
- 示例用法：

```vue
<table-toolbar
  :enable-column-settings="true"
  :column-options="columnOptions"
  :storage-key="columnSettingsKey"
  :default-visible-columns="defaultVisibleColumns"
  :enable-batch-actions="true"
  :selected-rows="selectedRows"
  :enable-import="true"
  :import-api="importApi"
  :template-api="templateApi"
  :enable-export="true"
  :export-api="exportApi"
  :export-params="exportParams"
  @refresh="$emit('refresh')"
  @column-change="handleColumnChange"
  @batch-delete="$emit('batch-delete', selectedRows)"
  @batch-enable="$emit('batch-enable', selectedRows)"
  @batch-disable="$emit('batch-disable', selectedRows)"
  @import-success="$emit('import-success', $event)"
  @export-success="$emit('export-success', $event)"
>
  <template #toolbar-left>
    <el-button type="primary" icon="el-icon-plus" size="mini" @click="$emit('add')">新增</el-button>
  </template>
</table-toolbar>
```

### 3. ActionButtons组件
- **强制要求**：表格中的操作按钮必须使用全局ActionButtons组件
- 使用预设按钮配置或自定义按钮配置
- 支持文本模式和图标模式
- 示例用法：

```vue
<action-buttons
  :buttons="getActionButtons(row)"
  mode="text"
  :row="row"
  @click="handleActionClick"
/>
```

### 4. StatusTag组件
- **强制要求**：所有状态展示必须使用全局StatusTag组件
- 使用预设状态类型或自定义状态配置
- 示例用法：

```vue
<status-tag
  :status="row.status"
  :text-map="statusTextMap"
  :type-map="statusTypeMap"
/>
```

### 5. DrawerForm组件
- **强制要求**：所有表单抽屉必须使用全局DrawerForm组件
- 通过`formSections`配置分段表单内容
- 支持create/update/view三种模式
- 示例用法：

```vue
<drawer-form
  ref="drawerForm"
  :visible.sync="drawerVisible"
  :title="formTitle"
  :mode="formMode"
  :data="formData"
  :rules="rules"
  :form-sections="formSections"
  @submit="handleFormSubmit"
>
  <!-- 自定义表单内容（如有需要） -->
</drawer-form>
```

### 6. Pagination组件
- **强制要求**：所有表格分页必须使用全局Pagination组件
- 正确处理分页事件和同步属性
- 示例用法：

```vue
<pagination
  v-show="total > 0"
  :total="total"
  :page.sync="currentPage"
  :limit.sync="pageSize"
  @pagination="handlePagination"
/>
```

## 组件集成规范

### [Module]Table 组件集成
- 表格组件应集成 TableToolbar、ActionButtons、StatusTag 和 Pagination
- 必须实现列设置功能，支持用户自定义显示列
- 将表格操作逻辑封装在组件内部，通过事件向父组件传递操作

```vue
<!-- [Module]Table.vue 组件示例 -->
<template>
  <div class="module-table">
    <!-- 工具栏 -->
    <table-toolbar
      :enable-column-settings="true"
      :column-options="columnOptions"
      :storage-key="`${equipmentType}_table_columns`"
      :default-visible-columns="defaultVisibleColumns"
      :enable-batch-actions="true"
      :selected-rows="selectedRows"
      :enable-import="true"
      :import-api="importApi"
      :template-api="templateApi"
      :enable-export="true"
      :export-api="exportApi"
      :export-params="exportParams"
      @refresh="$emit('refresh')"
      @column-change="handleColumnChange"
      @batch-delete="$emit('batch-delete', selectedRows)"
      @batch-enable="$emit('batch-enable', selectedRows)"
      @batch-disable="$emit('batch-disable', selectedRows)"
      @import-success="$emit('import-success', $event)"
      @export-success="$emit('export-success', $event)"
    >
      <template #toolbar-left>
        <el-button type="primary" icon="el-icon-plus" size="mini" @click="$emit('add')">新增</el-button>
      </template>
    </table-toolbar>

    <!-- 数据表格 -->
    <el-table
      v-loading="loading"
      :data="data"
      border
      stripe
      highlight-current-row
      @selection-change="handleSelectionChange"
      @sort-change="handleSortChange"
    >
      <!-- 选择列 -->
      <el-table-column type="selection" width="45" align="center" fixed="left" />
      
      <!-- 动态列 -->
      <template v-for="column in visibleColumns">
        <el-table-column
          :key="column.prop"
          :prop="column.prop"
          :label="column.label"
          :width="column.width"
          :min-width="column.minWidth"
          :align="column.align || 'left'"
          :sortable="column.sortable"
        >
          <template slot-scope="scope">
            <!-- 状态列渲染 -->
            <template v-if="column.renderType === 'status'">
              <status-tag 
                :status="scope.row[column.prop]" 
                :text-map="statusTextMap"
                :type-map="statusTypeMap"
              />
            </template>
            
            <!-- 日期列渲染 -->
            <template v-else-if="column.renderType === 'date'">
              {{ formatDate(scope.row[column.prop]) }}
            </template>
            
            <!-- 默认渲染 -->
            <template v-else>
              {{ scope.row[column.prop] }}
            </template>
          </template>
        </el-table-column>
      </template>
      
      <!-- 操作列 -->
      <el-table-column label="操作" width="180" align="center" fixed="right">
        <template slot-scope="scope">
          <action-buttons
            :buttons="getActionButtons(scope.row)"
            mode="text"
            :row="scope.row"
            @click="handleActionClick"
          />
        </template>
      </el-table-column>
    </el-table>
    
    <!-- 分页器 -->
    <pagination
      v-if="total > 0"
      :total="total"
      :page.sync="currentPage"
      :limit.sync="currentLimit"
      @pagination="handlePagination"
    />
  </div>
</template>
```

### [Module]FormDrawer 组件集成
- 表单抽屉组件应使用全局 DrawerForm 组件
- 根据不同的模式（create/update/view）展示不同的表单内容
- 通过 formSections 配置表单分段内容

```vue
<!-- [Module]FormDrawer.vue 组件示例 -->
<template>
  <drawer-form
    :visible.sync="visibleSync"
    :title="drawerTitle"
    :mode="type"
    :data="formData"
    :loading="loading"
    :rules="formRules"
    :form-sections="formSections"
    :submit-button-text="submitButtonText"
    :show-continue-create="type === 'create'"
    @submit="handleSubmit"
    @close="handleClose"
  >
    <!-- 可选的自定义内容 -->
    <template v-slot:custom-field="{field, formData}">
      <!-- 根据需要自定义表单项 -->
    </template>
  </drawer-form>
</template>
```

## 交互规范

### 表格操作
- 表格行操作使用文本按钮，放置在最右侧固定列中
- 批量操作放置在表格上方工具栏中
- 操作按钮图标和文字统一，参照预设按钮配置

### 表单抽屉
- 表单在抽屉中打开，不使用弹窗
- 表单按分类分段展示，每个分段有明确标题
- 表单抽屉宽度统一为550px
- 查看模式下表单项禁用，不显示保存按钮

### 状态展示
- 使用不同颜色和图标区分不同状态
- 状态切换操作需要二次确认
- 禁用状态的行整行显示为灰色

### 分类导航
- 对于需要按类型区分的数据（如设备类型、产品类型等），应使用类型导航栏
- 类型导航栏放置在搜索表单上方，支持快速切换不同类型数据
- 每个类型显示其包含的记录数量

```vue
<!-- 类型导航示例 -->
<div class="type-navigation">
  <div 
    v-for="type in typeOptions" 
    :key="type.value"
    :class="['type-item', currentType === type.value ? 'active' : '']"
    @click="handleTypeChange(type.value)"
  >
    {{ type.label }} ({{ type.count || 0 }})
  </div>
</div>
```

## 批量操作规范

> **注意**：详细的消息提示规范已移至[组件架构设计指南](mdc:.cursor/rules/07-component-architecture.mdc#通用消息提示规范)的"通用消息提示规范"章节。本节仅针对表格模块的批量操作提供特定指南。

### 批量操作实现规范
- 批量操作前应检查是否有选中的行，如无则提示用户
- 危险操作（如删除）必须进行二次确认
- 批量操作应向用户展示操作进度或加载状态
- 操作完成后必须刷新数据列表

```javascript
// 批量删除示例实现
handleBatchDelete(rows) {
  if (!rows || rows.length === 0) {
    this.$message.warning('请至少选择一条记录')
    return
  }
  
  this.$confirm('确认批量删除选中的记录吗？此操作不可恢复', '警告', {
    confirmButtonText: '确定',
    cancelButtonText: '取消',
    type: 'warning'
  }).then(() => {
    this.listLoading = true
    const ids = rows.map(row => row.id)
    
    batchDeleteAPI(ids).then(response => {
      this.$message.success(response.data.message || `成功删除 ${response.data.count || ids.length} 条记录`)
      this.refreshList()
    }).catch(error => {
      // 处理业务错误
      if (error && error.code) {
        this.$message.error(error.message || '批量删除失败')
      }
      this.listLoading = false
    })
  }).catch(() => {
    // 取消操作
    this.$message.info('已取消删除操作')
  })
}
```

## 配置化开发

### TableToolbar配置示例
```javascript
// 列设置存储的键名
columnSettingsKey() {
  return `${this.equipmentType}_equipment_columns`
},

// 导出参数
exportParams() {
  return {
    ...this.listQuery,
    export: true
  }
},

// 导入API函数
importApiFunction(file) {
  const formData = new FormData()
  formData.append('file', file)
  formData.append('type', this.equipmentType)
  return request({
    url: this.importApi,
    method: 'post',
    data: formData
  })
},

// 默认显示的列
defaultVisibleColumns: [
  'equipmentId', 'name', 'model', 'status', 'installDate'
]
```

### 表单配置示例
```javascript
// 动态表单分段
formSections() {
  return [
    {
      title: '一、基础信息',
      items: [
        {
          prop: 'name',
          label: '名称',
          type: 'input',
          placeholder: '请输入名称',
          maxlength: 50,
          showWordLimit: true
        },
        {
          prop: 'status',
          label: '状态',
          type: 'radio',
          options: [
            { label: '启用', value: 1 },
            { label: '禁用', value: 0 }
          ]
        }
        // 更多表单项...
      ]
    },
    {
      title: '二、详细信息',
      items: [
        // 分段表单项...
      ]
    }
  ]
}
```

### 表格列配置示例
```javascript
// 动态表格列配置
columns() {
  return [
    { prop: 'id', label: 'ID', width: '80' },
    { prop: 'name', label: '名称', width: '150' },
    { 
      prop: 'status', 
      label: '状态', 
      width: '100',
      // 使用StatusTag渲染
      renderType: 'status'
    },
    // 更多列配置...
  ]
}
```

### ActionButtons配置示例
```javascript
// 获取操作按钮配置
getActionButtons(row) {
  // 使用预设按钮生成操作按钮
  const buttons = generateTableButtons(['edit', 'view'])
  
  // 添加状态切换按钮
  const statusButton = {
    text: row.status === 1 ? '禁用' : '启用',
    action: 'statusToggle',
    icon: row.status === 1 ? 'el-icon-close' : 'el-icon-check',
    type: 'text',
    class: row.status === 1 ? 'status-disable' : 'status-enable'
  }
  
  return buttons.concat([statusButton])
}

// 处理按钮点击事件
handleActionClick({ action, row }) {
  switch (action) {
    case 'edit':
      this.$emit('update', row)
      break
    case 'view':
      this.$emit('view', row)
      break
    case 'statusToggle':
      this.$emit('status-change', row)
      break
  }
}
```

## 最佳实践

1. **使用计算属性**：将复杂的表单和表格配置放在计算属性中
2. **模块化API**：将API调用封装在单独的文件中
3. **配置预设**：使用预设配置简化常见操作
4. **TypeScript支持**：为配置项添加TypeScript类型定义
5. **文档完善**：在README.md中记录模块功能、组件说明和注意事项

## 核心原则

1. **统一性**：所有表格模块使用相同的组件结构和交互模式
2. **配置化**：尽量通过配置而非硬编码实现功能
3. **复用性**：充分利用全局组件和预设配置
4. **可维护性**：清晰的结构和足够的文档
5. **用户体验**：一致的交互模式和视觉样式

### 设计模式应用
- 使用工厂模式创建不同类型的表单配置
- 使用策略模式处理不同的数据渲染逻辑
- 使用适配器模式处理不同来源的数据格式
- 使用装饰器模式扩展组件功能

## 质量检查清单

开发完成后，请检查以下项目是否符合规范：

- [ ] 目录结构符合规范
- [ ] 模块说明文档完善，放在 docs 目录下
- [ ] 组件命名符合规范，使用模块名+功能名方式
- [ ] 使用全局组件实现核心功能
- [ ] 搜索表单实现完整搜索和重置功能
- [ ] 表格实现列设置功能和本地存储
- [ ] 表单抽屉分段清晰，字段归类合理
- [ ] 批量操作实现符合规范
- [ ] 状态管理和切换功能完整
- [ ] 导入导出功能正常工作
- [ ] 代码注释完善，遵循注释规范
- [ ] 代码符合 ESLint 规范
- [ ] 功能完整：查询、新增、编辑、查看、删除、状态切换
- [ ] UI 风格统一，与系统整体风格一致
- [ ] 错误处理完善，提供友好的错误提示
- [ ] 添加了必要的加载状态指示



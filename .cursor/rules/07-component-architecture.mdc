---
description: 
globs: 
alwaysApply: true
---
# 组件化架构设计指南

## 适用范围
本规则适用于所有Vue组件开发，规定了组件设计的基本原则、目录结构和通用规范。对于表格类模块的详细开发规范，请参考 [表格模块开发规范](mdc:.cursor/rules/08-table-module-development.mdc)。

## 主布局结构
项目使用了基于Element UI的布局系统，主要由侧边栏导航、顶部导航和主内容区组成。

## 关键布局文件
- [src/layout/index.vue](mdc:src/layout/index.vue) - 主布局容器
- [src/layout/components/Sidebar](mdc:src/layout/components/Sidebar) - 侧边栏导航组件
- [src/layout/components/Navbar.vue](mdc:src/layout/components/Navbar.vue) - 顶部导航组件

## 组件系统
项目组件分为两类：
1. 通用组件 (`src/components`) - 可在多个页面复用的组件
2. 视图组件 (`src/views`) - 特定页面的组件

## 路由与视图
- [src/router/index.js](mdc:src/router/index.js) - 定义路由与对应的视图组件
- 路由配置中的 `meta` 属性用于定义图标、标题和权限等信息

## 组件开发规范
- 组件名称使用 PascalCase 命名
- 页面组件放在 `views` 目录下
- 可复用组件放在 `components` 目录下
- 使用 `@/` 别名代替相对路径引用模块

## 核心原则
- **组件化开发**：将大型组件拆分为小型、可复用的组件
- **关注点分离**：组件应专注于单一功能或职责
- **逻辑与UI分离**：使用mixins或Vuex管理逻辑，组件专注于UI渲染
- **命名统一**：组件、文件和目录命名应遵循统一规范
- **配置化**：尽量通过配置而非硬编码实现功能
- **复用性**：充分利用全局组件和预设配置
- **可维护性**：清晰的结构和足够的文档
- **用户体验**：一致的交互模式和视觉样式

## 标准模块目录结构
每个功能模块应遵循以下标准目录结构：

```
views/module-name/
├── index.vue                 # 主页面组件（入口）
├── router/                   # 路由配置
│   └── index.js              # 模块路由定义
├── api/                      # API调用
│   └── index.js              # 模块API封装
├── components/               # 子组件目录
│   ├── common/               # 通用组件
│   │   ├── ActionBar.vue     # 操作按钮组件
│   │   └── StatusTag.vue     # 状态标签组件
│   ├── form/                 # 表单相关组件
│   │   ├── index.vue         # 表单组件入口
│   │   └── FormSections/     # 表单分段组件
│   │       ├── BaseInfo.vue  # 基础信息表单段
│   │       └── TypeSpecific/ # 特定类型表单段
│   ├── table/                # 表格相关组件
│   │   ├── index.vue         # 表格组件入口
│   │   └── columns/          # 表格列配置
│   │       ├── base.js       # 基础列配置
│   │       └── specific.js   # 特定列配置
│   └── search/               # 搜索相关组件
│       └── index.vue         # 搜索表单组件
├── constants/                # 常量定义
│   ├── types.js              # 类型常量
│   └── status-codes.js       # 状态码常量
├── mixins/                   # 混入目录
│   ├── form-mixin.js         # 表单相关混入
│   └── table-mixin.js        # 表格相关混入
├── utils/                    # 工具函数
│   └── formatter.js          # 格式化工具
├── styles/                   # 样式文件
│   └── index.scss            # 模块样式
├── docs/                     # 文档目录
│   └── README.md             # 详细需求文档
└── README.md                 # 模块说明文档
```

### 目录结构说明

1. **index.vue**: 主页面组件，应保持轻量，主要负责组织布局和状态管理
2. **router/**: 包含模块内部路由配置，便于大型模块的路由管理
3. **api/**: 集中管理模块的API调用，避免在组件中直接调用API
4. **components/**: 按功能分类的子组件目录
5. **constants/**: 存放模块内的常量定义，便于统一管理和修改
6. **mixins/**: 存放可复用的业务逻辑
7. **utils/**: 存放模块特定的工具函数
8. **styles/**: 存放模块特定的样式文件
9. **docs/**: 存放详细的文档和需求说明
10. **README.md**: 提供模块的简要说明和使用指南

### 目录结构调整指南

根据模块的复杂度和特点，可以适当调整目录结构：

- **简单模块**：可以省略router、constants等目录
- **中等复杂度模块**：保留主要目录，如api、components、mixins
- **复杂模块**：使用完整的目录结构，必要时可以增加子模块目录

## 参考示例
- 请参考`src/views/master-data/bin-specification`目录作为组件化开发的标准示例
- 对于表格类模块，请参考`src/views/master-data/equipment`目录，并遵循[表格模块开发规范](mdc:.cursor/rules/08-table-module-development.mdc)

## 组件设计规范

### 容器组件 (index.vue)
- 负责组合子组件、管理状态和数据流转
- 不应包含复杂UI逻辑，主要是组件组合和事件处理
- 包含数据加载和提交的核心方法
- 引入必要的mixins
- **新建议**: 将API调用逻辑抽离到api目录中，容器组件只负责调用API并处理响应

### 子组件设计
- 专注于单一功能或UI区块
- 通过props接收数据，通过events发送用户操作
- 避免直接调用API或修改全局状态
- 保持组件的独立性和可复用性
- **新建议**: 对于表单和表格等复杂组件，进一步拆分为更小的功能组件

### 表单组件最佳实践
- 将表单拆分为基础信息和特定类型信息等逻辑段落
- 使用动态组件加载不同类型的表单段
- 避免在单个组件中包含大量表单项
- 使用局部表单模型与外部数据解耦，避免双向绑定死循环
- 表单提交前进行数据验证和格式化

### 表格组件最佳实践
- 将表格列配置抽离到单独的文件中管理
- 使用动态列渲染，支持列设置和持久化
- 使用slot自定义单元格渲染
- 表格操作按钮统一管理，避免重复代码
- 优化大数据量表格的性能

### 搜索组件最佳实践
- 提供基础搜索和高级搜索两种模式
- 使用防抖处理避免频繁触发搜索
- 搜索条件与URL参数同步，支持页面刷新保留搜索条件
- 使用清晰的数据流向，避免双向绑定陷阱
- 支持搜索历史记录功能

## 全局组件使用规范

### 全局通用组件
- **Icon组件**：使用全局Icon组件显示图标
- **SvgIcon组件**：使用全局SvgIcon组件显示SVG图标
- **Breadcrumb组件**：使用全局Breadcrumb组件显示面包屑导航
- **Hamburger组件**：使用全局Hamburger组件显示折叠按钮
- **Screenfull组件**：使用全局Screenfull组件实现全屏功能
- **ErrorLog组件**：使用全局ErrorLog组件显示错误日志
- **ThemePicker组件**：使用全局ThemePicker组件切换主题

### 滚动动画使用规范
- **强制要求**：所有需要滚动功能的页面组件必须使用全局滚动动画工具函数
- 在以下场景必须使用滚动动画：
  1. 分页切换后返回顶部
  2. 表单提交后返回列表顶部
  3. 点击"返回顶部"按钮
  4. 任何需要页面滚动的交互

```js
// 正确使用方式
import { scrollTo } from '@/utils/scroll-to'

export default {
  methods: {
    handleSubmit() {
      // 提交表单后滚动到顶部
      this.submitForm().then(() => {
        scrollTo(0, 500)
      })
    },
    backToTop() {
      scrollTo(0, 800)
    }
  }
}
```

## API 管理规范

### API封装原则
- 每个模块在api目录下集中管理API调用
- 使用统一的命名和参数传递方式
- 提供清晰的函数名和注释说明
- 处理基本的错误和异常情况

### API封装示例
```javascript
// api/index.js
import request from '@/utils/request'

// 基础API路径
const baseUrl = '/api/module-name'

/**
 * 获取列表数据
 * @param {Object} params - 查询参数
 * @returns {Promise} - 返回Promise对象
 */
export function getList(params) {
  return request({
    url: `${baseUrl}/list`,
    method: 'get',
    params
  })
}

/**
 * 创建新记录
 * @param {Object} data - 创建数据
 * @returns {Promise} - 返回Promise对象
 */
export function create(data) {
  return request({
    url: `${baseUrl}/create`,
    method: 'post',
    data
  })
}

// 其他API函数...
```

## 常量管理规范

### 常量定义原则
- 将重复使用的字符串、数值、配置等提取为常量
- 使用语义化命名，提高代码可读性
- 按照功能和用途分类组织常量
- 导出常量便于在不同组件中复用

### 常量定义示例
```javascript
// constants/types.js
// 数据类型常量
export const DATA_TYPES = {
  TYPE_A: 'TYPE_A',
  TYPE_B: 'TYPE_B',
  TYPE_C: 'TYPE_C'
}

// 类型显示名称映射
export const TYPE_LABELS = {
  [DATA_TYPES.TYPE_A]: '类型A',
  [DATA_TYPES.TYPE_B]: '类型B',
  [DATA_TYPES.TYPE_C]: '类型C'
}

// 类型特性字段映射
export const TYPE_FIELDS = {
  [DATA_TYPES.TYPE_A]: ['fieldA1', 'fieldA2', 'fieldA3'],
  [DATA_TYPES.TYPE_B]: ['fieldB1', 'fieldB2'],
  [DATA_TYPES.TYPE_C]: ['fieldC1', 'fieldC2', 'fieldC3', 'fieldC4']
}
```

## Mixin设计规范

### 通用Mixin设计原则
- 一个mixin应专注于一个功能领域
- 避免在mixin中定义过多的方法和属性
- 使用命名前缀避免命名冲突
- 在mixin中提供适当的注释和文档

### 常用通用Mixin
- **resizeMixin**：处理窗口大小变化
- **i18nMixin**：处理国际化
- **permissionMixin**：处理权限控制
- **themeMixin**：处理主题切换
- **formMixin**：处理表单相关逻辑
- **tableMixin**：处理表格相关逻辑
- **searchMixin**：处理搜索相关逻辑

### Mixin示例
```javascript
// mixins/form-mixin.js
export default {
  data() {
    return {
      loading: false,
      form: {},
      rules: {}
    }
  },
  methods: {
    // 初始化表单数据
    initFormData() {
      this.form = {
        // 默认表单数据
      }
    },
    // 验证表单
    validateForm() {
      return new Promise((resolve, reject) => {
        this.$refs.form.validate(valid => {
          if (valid) {
            resolve(this.form)
          } else {
            this.$message.warning('表单填写有误，请检查')
            reject(new Error('表单验证失败'))
          }
        })
      })
    },
    // 重置表单
    resetForm() {
      this.$refs.form && this.$refs.form.resetFields()
      this.initFormData()
    }
  }
}
```

## 组件通信规范

### Props命名规范
- 使用小驼峰命名法
- 提供默认值和类型验证
- 文档化prop的用途和限制

### Events命名规范
- 使用kebab-case（短横线）命名法
- 使用动词前缀如：`search`、`update`、`select`、`change`
- 确保事件名称明确表达意图

### 父子组件通信规范
- 父传子：使用props
- 子传父：使用自定义事件($emit)
- 复杂状态：考虑使用Vuex

## 交互规范

### 通用消息提示规范

#### 基本原则
- **一致性**: 全系统保持统一的消息提示方式
- **精确性**: 消息内容应准确传达操作结果
- **层次性**: 区分不同级别的消息（成功、警告、错误等）
- **简洁性**: 消息内容应简明扼要

#### 消息类型与使用场景
1. **成功消息(`success`)**:
   - 数据创建/更新/删除成功
   - 批量操作成功完成
   - 用户操作达到预期效果

2. **警告消息(`warning`)**:
   - 操作可能导致不可恢复的结果时
   - 用户输入不完整但不影响继续
   - 提醒用户注意但不阻止操作

3. **错误消息(`error`)**:
   - API请求返回错误
   - 表单验证失败
   - 操作未能完成

4. **信息消息(`info`)**:
   - 操作取消通知
   - 中性提示信息
   - 系统通知

#### 消息提示实现规范

##### API响应消息处理
- **成功消息**:
  - 在业务组件中处理，因为它们与具体业务操作相关
  - 优先使用API返回的成功消息，如果没有则使用默认消息

```javascript
// 单个操作成功消息处理
this.$message({
  type: 'success',
  message: response.data.message || '操作成功'
})

// 批量操作成功消息处理
this.$message({
  type: 'success',
  message: `成功${operationName} ${response.data.count || items.length} 条记录`
})
```

- **错误消息**:
  - 业务错误应在业务组件中处理
  - 认证错误和网络错误由全局拦截器处理
  - 错误消息应包含具体错误原因

```javascript
// 业务错误处理 - 在业务组件中
.catch(error => {
  // 处理业务错误
  if (error && error.code) {
    this.$message({
      type: 'error',
      message: error.message || `${operationName}失败`
    })
  }
})
```

##### 表单操作消息提示
```javascript
// 表单提交成功消息
submitForm() {
  this.form.validate(valid => {
    if (valid) {
      createResource(this.form).then(response => {
        this.$message.success('创建成功')
        this.dialogVisible = false
        this.refreshList()
      }).catch(error => {
        if (error && error.code) {
          this.$message.error(error.message || '创建失败')
        }
      })
    }
  })
}
```

##### 批量操作消息提示
```javascript
// 批量删除示例
handleBatchDelete(rows) {
  if (!rows || rows.length === 0) {
    this.$message.warning('请至少选择一条记录')
    return
  }
  
  this.$confirm('确认批量删除选中的记录吗？此操作不可恢复', '警告', {
    confirmButtonText: '确定',
    cancelButtonText: '取消',
    type: 'warning'
  }).then(() => {
    const ids = rows.map(row => row.id)
    
    batchDeleteAPI(ids).then(response => {
      this.$message.success(response.data.message || `成功删除 ${response.data.count || ids.length} 条记录`)
      this.refreshList()
    }).catch(error => {
      // 处理业务错误
      if (error && error.code) {
        this.$message.error(error.message || '批量删除失败')
      }
    })
  }).catch(() => {
    this.$message.info('已取消删除操作')
  })
}
```

##### 状态变更消息提示
```javascript
// 启用/禁用等状态变更操作
handleStatusChange(row, newStatus, statusText) {
  changeStatus(row.id, newStatus).then(() => {
    this.$message.success(`${statusText}成功`)
    this.refreshList()
  }).catch(error => {
    if (error && error.code) {
      this.$message.error(error.message || `${statusText}失败`)
    }
  })
}
```

#### 全局错误处理配置
项目中的API请求拦截器应配置为：
- 全局处理认证错误和网络错误
- 将业务错误直接传递给业务组件处理

```javascript
// 响应拦截器配置 (src/utils/request.js)
service.interceptors.response.use(
  response => {
    const res = response.data

    if (res.code !== 20000) {
      // 只在全局处理认证相关错误
      if (res.code === 50008 || res.code === 50012 || res.code === 50014) {
        Message.error(res.message || 'Authentication Error')
        // 处理登出逻辑...
      }
      
      // 其他业务错误直接返回给业务组件处理
      return Promise.reject(res)
    } else {
      return res
    }
  },
  error => {
    // 只处理网络级别错误
    Message.error('网络请求失败，请稍后重试')
    return Promise.reject(error)
  }
)
```

#### 消息显示时长指南
- 成功消息: 3000ms
- 警告消息: 4000ms
- 错误消息: 5000ms
- 信息消息: 3000ms

### 通用交互模式
- 表单提交后显示成功/失败提示
- 危险操作需要二次确认
- 长时间操作显示加载状态
- 操作完成后给予明确的反馈

### 弹窗和提示
- 使用Element UI的Message组件显示操作结果
- 使用MessageBox组件进行二次确认
- 使用Notification组件显示重要通知
- 使用Loading组件显示加载状态

## 代码质量规范

### 组件大小控制
- 单个组件模板不超过200行
- 单个组件脚本不超过300行
- 超出限制时应考虑进一步拆分

### 注释规范
- 组件顶部注释说明组件用途
- props和events需要注释说明用途
- 复杂方法需要注释说明逻辑

### 性能优化规范
- 合理使用computed属性缓存计算结果
- 避免在template中进行复杂计算
- 使用v-if/v-show来优化渲染性能
- 使用keep-alive缓存频繁切换的组件
- 大型列表使用虚拟滚动技术
- 路由懒加载减少初始加载体积

## 最佳实践

1. **使用计算属性**：将复杂计算放在computed中
2. **模块化API**：将API调用封装在单独的文件中
3. **配置预设**：使用预设配置简化常见操作
4. **文档完善**：在README.md中记录模块功能、组件说明和注意事项
5. **代码复用**：抽取可复用逻辑到mixins中
6. **响应式设计**：确保组件在不同屏幕尺寸下正常工作
7. **优雅降级**：处理异常情况和边界条件
8. **用户体验**：关注加载状态、错误提示和操作反馈

## 迁移策略
对于现有的大型组件，可以采用以下迁移策略：
1. 识别组件中的主要功能块
2. 从UI层开始拆分（先拆分模板和样式）
3. 逐步抽取业务逻辑到mixins
4. 重构主组件使用新拆分的子组件
5. 引入api目录和constants目录，集中管理API调用和常量

## 质量检查清单

开发完成后，请检查以下项目是否符合规范：

- [ ] 组件结构清晰，职责单一
- [ ] 组件间通信方式合理
- [ ] API调用集中管理在api目录
- [ ] 常量定义集中管理在constants目录
- [ ] 使用了适当的全局组件
- [ ] 表单、表格等复杂组件进行了合理拆分
- [ ] 业务逻辑抽取到mixins
- [ ] 代码注释完善
- [ ] 性能优化措施到位
- [ ] 交互体验符合规范
- [ ] 代码符合ESLint规范
- [ ] 组件大小控制在合理范围内
- [ ] README.md文档完整


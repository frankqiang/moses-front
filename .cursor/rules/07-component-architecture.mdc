---
description: 
globs: 
alwaysApply: true
---
# 组件化架构指南

## 适用范围
本规则适用于所有Vue组件开发，特别是复杂业务功能模块的组件设计，推荐按照此规范组织代码。

## 核心原则
- **组件化开发**：将大型组件拆分为小型、可复用的组件
- **关注点分离**：组件应专注于单一功能或职责
- **逻辑与UI分离**：使用mixins或Vuex管理逻辑，组件专注于UI渲染
- **命名统一**：组件、文件和目录命名应遵循统一规范

## 目录结构规范
每个功能模块应遵循以下目录结构：
```
module-name/
├── index.vue                 # 主页面（容器组件）
├── components/               # 子组件目录
│   ├── SearchForm.vue        # 搜索表单组件
│   ├── ActionBar.vue         # 操作按钮组件
│   ├── DataTable.vue         # 数据表格组件
│   ├── DetailForm.vue        # 详情/编辑表单组件
│   └── ImportDialog.vue      # 导入对话框组件（如需要）
└── mixins/                   # 混入目录
    ├── tableMixin.js         # 表格操作相关混入
    ├── formMixin.js          # 表单处理相关混入
    └── importExportMixin.js  # 导入导出相关混入（如需要）
```

## 组件设计规范

### 容器组件 (index.vue)
- 负责组合子组件、管理状态和数据流转
- 不应包含复杂UI逻辑，主要是组件组合和事件处理
- 包含数据加载和提交的核心方法
- 引入必要的mixins

### 搜索表单组件 (SearchForm.vue)
- 专注于搜索条件的收集和提交
- 通过props接收初始值，通过events发送搜索条件
- 提供搜索和重置功能

### 操作按钮组件 (ActionBar.vue)
- 集中管理页面的主要操作按钮
- 通过props接收状态（如禁用状态）
- 通过events触发操作

### 数据表格组件 (DataTable.vue)
- 负责数据展示、分页、排序等
- 封装表格常用操作（选择、编辑、状态切换等）
- 通过props接收数据，通过events发送用户操作

### 表单组件 (DetailForm.vue)
- 用于数据编辑和新增
- 封装表单验证和提交逻辑
- 支持对话框模式和页面模式

### 导入对话框组件 (ImportDialog.vue)
- 专注于文件上传和导入功能
- 展示导入结果和错误信息

## Mixin设计规范

### tableMixin
- 封装表格通用逻辑（选择、批量操作、状态变更）
- 不包含UI渲染逻辑

### formMixin
- 封装表单通用逻辑（验证、提交、重置）
- 提供统一的表单处理方法

### importExportMixin
- 封装导入导出通用逻辑
- 处理文件下载和上传的底层实现

## 组件通信规范

### Props命名规范
- 使用小驼峰命名法
- 提供默认值和类型验证
- 文档化prop的用途和限制

### Events命名规范
- 使用kebab-case（短横线）命名法
- 使用动词前缀如：`search`、`update`、`select`、`change`
- 确保事件名称明确表达意图

### 父子组件通信规范
- 父传子：使用props
- 子传父：使用自定义事件($emit)
- 复杂状态：考虑使用Vuex

## 代码质量规范

### 组件大小控制
- 单个组件模板不超过200行
- 单个组件脚本不超过300行
- 超出限制时应考虑进一步拆分

### 注释规范
- 组件顶部注释说明组件用途
- props和events需要注释说明用途
- 复杂方法需要注释说明逻辑

### 性能优化规范
- 合理使用computed属性缓存计算结果
- 避免在template中进行复杂计算
- 使用v-if/v-show来优化渲染性能

## 实际案例
请参考`src/views/master-data/bin-specification`目录作为组件化开发的标准示例。该示例展示了如何将一个复杂页面拆分为多个可维护的组件。

## 迁移策略
对于现有的大型组件，可以采用以下迁移策略：
1. 识别组件中的主要功能块
2. 从UI层开始拆分（先拆分模板和样式）
3. 逐步抽取业务逻辑到mixins
4. 重构主组件使用新拆分的子组件

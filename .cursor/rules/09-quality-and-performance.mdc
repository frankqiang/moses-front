---
description: 
globs: 
alwaysApply: true
---
# 代码质量、性能优化与文档规范

## 规则说明与适用范围
本规则定义了代码质量标准、性能优化实践、文档注释规范、样式指南和代码检查清单。所有代码提交都需遵循此质量标准。

## 性能优化实践

### 组件优化
- 合理使用computed属性缓存计算结果
- 避免在template中进行复杂计算
- 使用v-if/v-show来优化渲染性能
- 使用keep-alive缓存频繁切换的组件
- 大型列表使用虚拟滚动技术
- 路由懒加载减少初始加载体积

### 数据请求优化
- 使用防抖/节流避免频繁请求
- 本地缓存不常变化的数据
- 使用批量请求替代多个单独请求
- 合理设置请求超时时间

### 组件大小控制
- 单个组件模板不超过200行
- 单个组件脚本不超过300行
- 超出限制时应考虑进一步拆分

## 文档与注释规范

### 文件头部注释
每个文件顶部应有描述文件功能的注释：

```javascript
/**
 * 文件名称：module-name.js
 * 文件描述：简要说明该文件的功能和用途
 * 创建日期：YYYY-MM-DD
 * 修改记录：
 *   - YYYY-MM-DD: 修改内容描述
 */
```

### 函数注释
关键函数应有详细的JSDoc注释：

```javascript
/**
 * 函数名称
 * @param {类型} 参数名 - 参数说明
 * @param {类型} [可选参数] - 参数说明
 * @returns {类型} 返回值说明
 * @throws {错误类型} 可能抛出的错误说明
 */
```

### 复杂逻辑注释
复杂逻辑应添加详细注释：

```javascript
// 处理特殊业务逻辑
if (complexCondition) {
  // 这里使用特殊处理是因为需要满足业务规则XYZ
  // 步骤1: 处理A
  // 步骤2: 处理B
  // 步骤3: 处理C
}
```

## 样式指南

### 样式文件组织
- 全局样式位于 `src/styles` 目录
- 使用SCSS预处理器
- 组件样式使用scoped属性避免污染全局

### 样式规范
1. **CSS命名规范**
   - 使用BEM命名方法论（Block-Element-Modifier）
   - 类名使用小写字母和连字符
   - 避免使用ID选择器

2. **SCSS文件结构**
   ```scss
   // 1. 变量定义
   $primary-color: #409EFF;
   
   // 2. 混入定义
   @mixin button-style {
     padding: 8px 16px;
     border-radius: 4px;
   }
   
   // 3. 样式规则
   .component-name {
     // 样式定义
   }
   ```

3. **组件样式最佳实践**
   - 使用 `<style scoped>` 避免样式污染
   - 深度选择器使用 `::v-deep` 或 `/deep/`
   - 避免在组件中修改全局样式

4. **响应式设计**
   - 使用Element UI的栅格系统
   - 合理使用媒体查询
   - 确保在不同设备上的良好体验

## 安全性规范

### 安全性检查
- [ ] 用户输入是否进行了验证和过滤
- [ ] 敏感信息是否正确处理（不在前端暴露）
- [ ] API请求是否包含必要的认证信息
- [ ] 权限控制是否正确实现

### 数据安全
- 所有用户输入必须进行验证和过滤
- 敏感数据不应在前端存储或传输
- 使用HTTPS协议进行数据传输
- 实现适当的权限控制机制

## 代码评审与质量控制

### 提交前代码质量检查
- [ ] ESLint检查无错误
- [ ] 所有API调用都有错误处理
- [ ] 组件拆分合理，单一职责
- [ ] 常量已抽取，无魔法字符串
- [ ] 添加了必要的注释
- [ ] Mock数据真实有效
- [ ] 测试功能完整可用

### API请求检查点
- [ ] API文件是否放在正确的模块目录中
- [ ] 文件命名是否使用kebab-case
- [ ] API路径是否遵循`/mes`前缀规范
- [ ] 是否有适当的注释
- [ ] 请求格式是否符合规范
- [ ] 导入路径是否正确

### 错误检查与排错指南
如果遇到404错误：
1. 检查API路径是否包含`/mes`前缀
2. 确认前端API调用和Mock定义的路径是否一致
3. 检查路径拼写是否正确

### 性能优化检查点
- [ ] 大型列表是否使用了虚拟滚动或分页
- [ ] 频繁触发的事件是否添加了防抖/节流
- [ ] 图片资源是否进行了压缩优化
- [ ] 路由是否使用了懒加载
- [ ] 是否合理使用了组件缓存(keep-alive)

## 代码质量检查清单

### 开发完成检查清单
开发完成后，请检查以下项目是否符合规范：

#### 基础代码质量
- [ ] **模块化**：每个文件只有单一职责，相关功能放在一起  
- [ ] **注释完善**：文件顶部有说明，函数有完整JSDoc注释
- [ ] **命名规范**：变量、函数使用驼峰命名，常量使用全大写下划线
- [ ] **代码组织**：相关代码放在一起，逻辑流程清晰
- [ ] **代码格式**：符合ESLint规范

#### API与数据处理
- [ ] **错误处理**：所有API调用都有try/catch，提供友好错误信息
- [ ] **API封装**：统一的请求/响应处理，参数验证
- [ ] **常量抽取**：业务常量统一管理，避免魔法字符串
- [ ] **工具函数**：通用逻辑抽取为工具函数，避免代码重复

#### 组件与架构
- [ ] **组件通信**：组件间通信方式合理
- [ ] **表单和表格组件**：复杂组件进行了合理拆分
- [ ] **业务逻辑**：抽取到mixins或单独文件
- [ ] **状态管理**：合理使用Vuex进行状态管理

#### 性能与体验
- [ ] **性能优化**：使用防抖/节流，避免不必要的渲染和请求
- [ ] **交互体验**：符合规范，提供友好的用户反馈
- [ ] **响应式设计**：在不同设备上表现良好

#### Mock数据质量
- [ ] **数据真实性**：数据符合行业标准和现实情况
- [ ] **业务场景**：使用真实的设备型号、工艺参数、产品规格
- [ ] **时间逻辑**：时间数据符合实际工作时间和生产周期
- [ ] **数值合理性**：数量和参数在合理范围内
- [ ] **数据关联性**：保持数据间的业务逻辑关联性
- [ ] **流程正确性**：状态变更遵循真实业务流程

#### 文档与测试
- [ ] **文档完整**：提供充分的使用说明
- [ ] **需求文档**：创建了模块需求文档
- [ ] **功能测试**：已完成基本功能测试
- [ ] **兼容性测试**：在主要浏览器中测试通过

### 发布前最终检查
- [ ] 代码已通过ESLint检查
- [ ] 所有TODO注释已处理或移除
- [ ] 控制台无错误和警告信息
- [ ] 所有功能正常工作
- [ ] README文档已更新


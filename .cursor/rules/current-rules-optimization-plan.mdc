---
description: 
globs: 
alwaysApply: false
---
# 当前项目 Cursor Rules 优化实施方案

## 现状分析

### 需要优化的文件
1. **08-table-module-development.mdc** (28KB) - 🔴 严重超标
2. **common.mdc** (13KB) - 🟡 轻微超标  
3. **07-error-handling-and-state.mdc** (8.7KB) - 🟡 接近上限
4. **03-api-and-mock.mdc** (9.2KB) - 🟡 接近上限

### 保持现状的文件
- **06-modern-ui-design.mdc** (4.9KB) - ✅ 良好示例
- **01-development-workflow.mdc** (4.0KB) - ✅ 大小合适
- **02-eslint-standards.mdc** (4.0KB) - ✅ 大小合适

## 具体优化方案

### 第一优先级：拆分 `08-table-module-development.mdc`

**当前问题**：
- 文件过大（28KB），包含大量重复的完整组件示例
- 混合了规范、示例、配置等多种内容类型

**优化方案**：
```
08-table-module-development.mdc (6KB) 
├── core/table-module-patterns.mdc (4KB)    # 核心模式和规范
├── components/global-table-components.mdc (5KB)  # 全局组件使用
└── examples/table-module-complete.mdc (引用)    # 完整示例引用
```

**重构要点**：
1. 提取核心规范到主文件
2. 将全局组件使用指南独立
3. 完整代码示例改为文件引用
4. 保留关键的配置模式示例

### 第二优先级：精简 `common.mdc`

**当前问题**：
- RIPER-5协议过于详细，包含大量示例代码
- 与项目特定规范混合

**优化方案**：
```
common.mdc (5KB)
├── protocols/riper-5-core.mdc (4KB)      # 核心协议
├── protocols/riper-5-examples.mdc (引用)  # 详细示例
└── project-common-rules.mdc (3KB)        # 项目通用规则
```

### 第三优先级：模块化其他大文件

#### `07-error-handling-and-state.mdc` 拆分
```
patterns/error-handling.mdc (4KB)     # 错误处理模式
patterns/state-management.mdc (3KB)   # 状态管理模式
patterns/utils-development.mdc (2KB)  # 工具函数开发
```

#### `03-api-and-mock.mdc` 拆分
```
patterns/api-patterns.mdc (4KB)       # API设计模式
patterns/mock-patterns.mdc (3KB)      # Mock开发规范
examples/api-examples.mdc (引用)      # 完整API示例
```

## 实施步骤

### 步骤一：创建新的文件结构

1. 创建分类目录：
   ```bash
   mkdir -p .cursor/rules/core
   mkdir -p .cursor/rules/components  
   mkdir -p .cursor/rules/patterns
   mkdir -p .cursor/rules/examples
   mkdir -p .cursor/rules/protocols
   ```

2. 更新规则索引文件

### 步骤二：重构最大的文件

1. **重构表格模块规范**：
   - 提取核心模式（BaseTable使用、全局组件标准）
   - 移除重复的完整组件代码
   - 保留关键配置示例
   - 添加示例文件引用

2. **精简示例代码**：
   ```markdown
   <!-- 当前：50行完整组件代码 -->
   
   <!-- 优化后：关键模式 + 引用 -->
   ## 标准表格模块模式
   
   ```vue
   <template>
     <div class="table-module">
       <search-form-v2 :items="searchConfig" @search="handleSearch" />
       <table-toolbar @add="handleAdd" @refresh="getList" />
       <base-table :data="list" :columns="tableConfig" />
       <pagination :total="total" @pagination="getList" />
     </div>
   </template>
   ```
   
   **完整实现参考**：[设备管理模块](mdc:src/views/master-data/equipment/index.vue)
   ```

### 步骤三：统一代码示例标准

**精简原则**：
- 单个代码示例 ≤ 15行
- 关注模式而非实现细节
- 提供清晰的使用场景说明
- 添加扩展阅读链接

**示例对比**：
```markdown
<!-- ❌ 当前：冗长示例 -->
```javascript
// 50+ 行的完整组件实现
export default {
  name: 'CompleteComponent',
  // 大量代码...
}
```

<!-- ✅ 优化后：模式示例 -->
```javascript
// 标准数据加载模式
async getList() {
  try {
    this.loading = true
    const res = await api.getList(this.listQuery)
    this.list = res.data.items
    this.total = res.data.total
  } catch (error) {
    this.handleError(error)
  } finally {
    this.loading = false
  }
}
```

**参考实现**：[数据加载工具类](mdc:src/mixins/data-loader.js)
```

### 步骤四：建立引用体系

1. **标准引用格式**：
   ```markdown
   参考实现：[文件名](mdc:相对路径)
   配置示例：[配置文件](mdc:配置文件路径)
   完整示例：[示例组件](mdc:组件路径)
   ```

2. **创建示例索引**：
   ```markdown
   # 完整示例索引
   
   ## 表格模块
   - [设备管理](mdc:src/views/master-data/equipment/index.vue) - 标准表格模块
   - [工艺管理](mdc:src/views/master-data/process/index.vue) - 复杂表格模块
   
   ## 表单模块  
   - [用户表单](mdc:src/components/UserForm/index.vue) - 标准表单
   - [设备表单](mdc:src/views/master-data/equipment/components/EquipmentForm.vue) - 复杂表单
   ```

## 优化效果预期

### 文件大小对比
| 文件 | 当前大小 | 优化后 | 减少 |
|------|----------|--------|------|
| 08-table-module-development.mdc | 28KB | 6KB | -79% |
| common.mdc | 13KB | 5KB | -62% |
| 07-error-handling-and-state.mdc | 8.7KB | 拆分为3个4KB文件 | -25% |
| 03-api-and-mock.mdc | 9.2KB | 拆分为2个4KB文件 | -13% |

### 整体效果
- **总文件大小减少**: ~50%
- **查找效率提升**: 分类明确，快速定位
- **维护成本降低**: 模块化结构，独立维护
- **AI理解提升**: 文件精简，重点突出

## 实施时间表

- **第1周**: 创建新结构，重构表格模块规范
- **第2周**: 精简common.mdc，建立引用体系  
- **第3周**: 重构其他大文件，统一示例标准
- **第4周**: 测试验证，完善文档

## 后续维护建议

1. **新规则添加标准**：
   - 优先考虑加入现有文件
   - 单文件不超过8KB限制
   - 代码示例不超过15行

2. **定期审查机制**：
   - 每月检查文件大小
   - 及时清理过时示例
   - 更新文件引用链接

3. **团队协作规范**：
   - 修改前先评估影响范围
   - 大型重构需要团队讨论
   - 保持向后兼容性


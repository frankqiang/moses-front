---
alwaysApply: true
---

# src/utils 工具脚本统一规范

## 核心原则
从统一入口导入，避免重复实现，遵循既定模式

## 🛠️ 核心工具清单

### 📤 HTTP 请求工具
**[request.js](mdc:src/utils/request.js)** - 现代化axios实例
**功能**：自动Token刷新、请求去重、错误分类处理、重试机制
**导入**：`import service from '@/utils/request'`

### 🔧 通用工具函数
**[index.js](mdc:src/utils/index.js)** - 统一导出入口
**功能**：防抖节流、深拷贝、时间处理、参数格式化
**导入**：`import { debounce, throttle, cloneDeep, parseTime, formatQueryParams } from '@/utils'`

### 🔐 认证管理
**[auth.js](mdc:src/utils/auth.js)** - Token和认证状态管理
**功能**：Token存取、过期检测、记住登录状态
**导入**：`import { getToken, setTokens, removeToken, isTokenExpired } from '@/utils/auth'`

### 📊 表格配置存储
**[table-config-store.js](mdc:src/utils/table-config-store.js)** - 集中式表格配置管理
**功能**：表格列配置存储、版本控制、定期清理
**导入**：`import tableConfigStore from '@/utils/table-config-store'`

### 🎯 会话管理
**[sessionManager.js](mdc:src/utils/sessionManager.js)** - 会话超时和多标签页同步
**功能**：超时检测、提醒弹窗、多标签页状态同步
**导入**：`import sessionManager from '@/utils/sessionManager'`

### ✅ 验证工具
**[validate.js](mdc:src/utils/validate.js)** - 常用验证函数
**功能**：外部链接检测、用户名验证
**导入**：`import { isExternal, validUsername } from '@/utils/validate'`

### 📄 页面标题
**[get-page-title.js](mdc:src/utils/get-page-title.js)** - 动态页面标题设置
**功能**：根据路由自动设置页面标题

### 🎬 滚动动画
**[scroll-to.js](mdc:src/utils/scroll-to.js)** - 平滑滚动动画
**功能**：页面平滑滚动到指定位置

## 📋 使用规范

### 统一导入方式
```javascript
// ✅ 推荐
import { debounce, parseTime } from '@/utils'
import service from '@/utils/request'

// ❌ 禁止
import axios from 'axios'  // 直接使用axios
import debounce from 'lodash.debounce'  // 跳过统一入口
```

### 禁止重复实现
- 时间处理 → 使用 `parseTime`
- 防抖节流 → 使用 `debounce/throttle`
- HTTP请求 → 使用 `service`
- Token管理 → 使用 `auth.js` 工具

## 🔍 开发检查清单

- [ ] HTTP请求使用 `request.js`
- [ ] 时间格式化使用 `parseTime`
- [ ] 防抖节流使用统一工具
- [ ] Token管理使用 `auth.js`
- [ ] 表格配置使用 `table-config-store`
- [ ] 会话管理使用 `sessionManager`

---
description: 提供项目开发流程、构建命令、新页面添加、API请求和状态管理的完整指南。当进行新功能开发、API集成或状态管理时请求此规则。
globs: 
alwaysApply: false
---
# 开发指南

## 启动项目
```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

## 项目构建
```bash
# 构建生产环境
npm run build:prod

# 构建测试环境
npm run build:stage
```

## 添加新页面
1. 在 `src/views` 目录下创建新的页面组件
2. 在 `src/router/index.js` 中添加对应的路由配置
3. 如果需要添加到侧边栏菜单，确保设置了正确的 `meta` 信息

## API请求

### URL路径规范
**重要**: 所有API路径必须遵循以下规则：
1. 所有API路径必须以`/mes`作为前缀
2. 路径应按照业务模块组织，例如：`/mes/master-data/xxx`、`/mes/production/xxx`
3. 不要使用`/vue-admin-template`前缀

#### 正确的URL示例：
```
/mes/master-data/product/list
/mes/master-data/storage-location/create
/mes/production/work-order/detail/123
```

#### 错误的URL示例：
```
/master-data/product/list        // ❌ 缺少/mes前缀
/vue-admin-template/mes/xxx      // ❌ 不应使用/vue-admin-template前缀
```

### 目录结构规范
API目录必须按照业务模块组织，保持清晰的目录结构：

```
src/api/
├── module-name-1/            # 业务模块1目录
│   ├── feature-1.js          # 功能1的API
│   └── feature-2.js          # 功能2的API
├── module-name-2/            # 业务模块2目录
│   └── feature-3.js          # 功能3的API
└── user.js                   # 用户相关API（全局通用）
```

示例：主数据管理模块的API应放在 `src/api/master-data/` 目录下。

### API文件编写规范
```js
/**
 * 模块名称/功能名称API
 * 描述：该模块包含xxx功能的所有API请求
 * 创建日期：YYYY-MM-DD
 */

import request from '@/utils/request'

// API基础路径
const baseURL = '/mes/module-name/feature-name'

/**
 * 获取XX列表
 * @param {Object} query - 查询参数
 * @returns {Promise} 返回API响应
 */
export function getXXList(query) {
  return request({
    url: `${baseURL}/list`,
    method: 'get',
    params: query
  })
}
```

### 基本使用
- 使用 [src/utils/request.js](mdc:src/utils/request.js) 中封装的axios实例发送请求
- API请求导入示例：
```js
import { getXXList } from '@/api/module-name/feature'

// 在组件中使用
methods: {
  fetchData() {
    getXXList({ page: 1, limit: 10 }).then(response => {
      this.list = response.data.items
    })
  }
}
```

### 添加新模块/功能流程
1. 创建对应模块目录（若不存在）: `mkdir -p src/api/module-name`
2. 在目录下创建功能对应的js文件
3. 定义API基础路径，遵循URL规范：`const baseURL = '/mes/module-name/feature-name'`
4. 编写API请求函数并导出
5. 在组件中导入并使用

### 代码评审检查点
- API文件是否放在正确的模块目录中
- 文件命名是否使用kebab-case
- API路径是否遵循`/mes`前缀规范
- 是否有适当的注释
- 请求格式是否符合规范
- 导入路径是否正确

### 错误检查与排错指南
如果遇到404错误：
1. 检查API路径是否包含`/mes`前缀
2. 确认前端API调用和Mock定义的路径是否一致
3. 检查路径拼写是否正确

## 状态管理
- 全局状态使用Vuex管理，位于 `src/store` 目录
- 应用分为多个模块（user、app、settings等）
- 在组件中使用 `mapGetters` 和 `mapActions` 访问和修改状态

## 样式指南
- 全局样式位于 `src/styles` 目录
- 使用SCSS预处理器
- 组件样式使用scoped属性避免污染全局

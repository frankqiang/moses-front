---
description: 当需要开发新的API接口、测试数据流或模拟后端服务时请求此规则
globs: 
alwaysApply: false
---
# 规则类型：Agent Requested
# 说明：当需要开发新的API接口、测试数据流或模拟后端服务时请求此规则

# Mock数据和API规范

## Mock系统概述
本项目使用MockJS模拟后端API，方便前端开发和测试。模拟数据位于 `mock` 目录。项目的Mock服务使用CommonJS模块系统（require/module.exports），而非ES模块系统（import/export）。

## URL路径规范
**重要**: 所有API路径必须遵循以下规则：
1. 所有API路径必须以`/mes`作为前缀
2. 路径应按照业务模块组织，例如：`/mes/master-data/xxx`、`/mes/production/xxx`
3. 不要使用`/vue-admin-template`前缀

### 正确的URL示例：
```
/mes/master-data/product/list
/mes/master-data/storage-location/create
/mes/production/work-order/detail/123
```

### 错误的URL示例：
```
/master-data/product/list        // ❌ 缺少/mes前缀
/vue-admin-template/mes/xxx      // ❌ 不应使用/vue-admin-template前缀
```

## 目录结构规范
Mock目录必须按照业务模块组织，与API目录结构保持一致：

```
mock/
├── module-name-1/            # 业务模块1目录
│   ├── feature-1.js          # 功能1的Mock数据
│   └── feature-2.js          # 功能2的Mock数据
├── module-name-2/            # 业务模块2目录
│   └── feature-3.js          # 功能3的Mock数据
├── user.js                   # 用户相关Mock数据（全局通用）
├── utils.js                  # Mock工具函数
├── index.js                  # Mock入口文件
└── mock-server.js            # Mock服务器实现
```

示例：主数据管理模块的Mock数据应放在 `mock/master-data/` 目录下。

## 关键文件
- [mock/index.js](mdc:mock/index.js) - Mock服务器入口
- [mock/mock-server.js](mdc:mock/mock-server.js) - Mock服务器实现
- [mock/user.js](mdc:mock/user.js) - 用户相关的模拟数据和API
- [mock/utils.js](mdc:mock/utils.js) - 通用工具函数，包含参数处理等功能

## 使用方法
1. 在对应模块目录下创建Mock文件（如 `mock/module-name/feature.js`）
2. 定义API接口和返回数据
3. 在 `mock/index.js` 中引入新创建的Mock文件

## 模块系统规范
**重要提示**：项目使用CommonJS模块系统，而非ES模块系统。这意味着：

1. 使用`require()`引入模块，而非`import`
2. 使用`module.exports`导出，而非`export`或`export default`

### 正确示例（CommonJS）：
```js
// 引入依赖
const Mock = require('mockjs')
const { param2Obj } = require('../utils')

// 定义工具函数
function generateId() {
  return 'ID' + Math.floor(Math.random() * 10000)
}

// 导出API定义
module.exports = [
  // API定义...
]
```

### 错误示例（ES模块）：
```js
// ❌ 错误 - 不要使用import语法
import Mock from 'mockjs'  
import { param2Obj } from '../utils'

// ❌ 错误 - 不要使用export语法
export function generateId() {
  return 'ID' + Math.floor(Math.random() * 10000)
}

// ❌ 错误 - 不要使用export default语法
export default [
  // API定义...
]
```

## Mock文件编写规范
```js
/**
 * 模块名称/功能名称Mock数据
 * 描述：模拟xxx功能的后端API响应
 * 创建日期：YYYY-MM-DD
 */

const Mock = require('mockjs')

// 生成随机数据
const data = Mock.mock({
  'items|10': [{
    'id|+1': 1,
    'name': '@cname',
    // 其他字段...
  }]
})

// 导出API定义
module.exports = [
  // 获取XX列表
  {
    url: '/mes/module/feature/list',  // 注意：使用/mes前缀
    type: 'get',
    response: config => {
      const { query } = config
      // 处理查询参数...
      return {
        code: 20000,
        data: {
          total: data.items.length,
          items: data.items
        }
      }
    }
  }
]
```

## API路径定义规范
在前端API文件中定义基础路径时，应遵循以下规范：

```js
// API基础路径定义
const baseURL = '/mes/module-name/feature-name'  // 注意：必须包含/mes前缀

// 导出API函数
export function getList(params) {
  return request({
    url: `${baseURL}/list`,
    method: 'get',
    params
  })
}
```

## 工具函数使用
项目提供了通用工具函数，位于`mock/utils.js`，可以通过以下方式引入：

```js
const { param2Obj } = require('../utils')  // 相对路径引用
// 或
const { param2Obj } = require('/mock/utils')  // 绝对路径引用（从项目根目录）
```

常用工具函数：
- `param2Obj(url)` - 解析URL查询参数为对象

## 添加新模块/功能流程
1. 创建对应模块目录（若不存在）: `mkdir -p mock/module-name`
2. 在目录下创建功能对应的js文件
3. 在index.js中引入：
```js
const moduleFeature = require('./module-name/feature')

const mocks = [
  ...moduleFeature,
  // 其他模块...
]
```

## 代码评审检查点
- Mock文件是否放在正确的模块目录中
- 文件命名是否使用kebab-case
- 是否正确使用CommonJS语法（require/module.exports）
- API路径是否遵循`/mes`前缀规范
- 是否有适当的注释
- 响应格式是否符合规范
- 是否在index.js中正确引入

## 错误检查与排错指南
常见的错误包括：
1. 使用ES模块语法（import/export）而非CommonJS模块语法（require/module.exports）
2. 文件路径错误导致无法找到模块
3. 导出格式不正确（不是数组）
4. API路径前缀不一致（缺少`/mes`前缀）

如果遇到"Cannot find module"错误：
1. 检查模块路径是否正确
2. 确认是否使用了正确的导入语法（require而非import）
3. 确认文件是否存在且拼写正确

如果遇到404错误：
1. 检查API路径是否包含`/mes`前缀
2. 确认前端API调用和Mock定义的路径是否一致
3. 检查路径拼写是否正确

## 关闭Mock
在生产环境下，可以通过修改 `src/main.js` 中的相关代码来关闭Mock服务：
```js
if (process.env.NODE_ENV === 'production') {
  const { mockXHR } = require('../mock')
  mockXHR()
}
```

## Mock API 指南

## 概述
本指南详细说明了如何使用Mock数据系统进行前端开发和测试。Mock系统可以模拟后端API响应，使前端开发不依赖于后端服务。

## Mock系统架构
- 基于[mockjs](mdc:http:/mockjs.com)实现数据生成
- 使用Express中间件处理请求
- 支持动态路由和响应数据
- 可配置响应延迟和错误情况

## 目录结构
```
mock/
├── master-data/           # 主数据管理模块
│   ├── warehouse.js      # 仓库管理接口
│   ├── bin.js           # 料框管理接口
│   └── location.js      # 库位管理接口
├── inventory/            # 库存管理模块
│   ├── stock.js         # 库存接口
│   └── movement.js      # 库存移动接口
├── utils.js             # 工具函数
└── index.js             # Mock服务器入口
```

## 创建Mock接口

### 基本结构
```javascript
export default [
  {
    url: '/mes/api/warehouses',
    type: 'get',
    response: config => {
      return {
        code: 20000,
        data: {
          total: 100,
          items: [/* 数据项 */]
        }
      }
    }
  }
]
```

### 响应格式规范
```javascript
{
  code: 20000,      // 状态码：20000成功，50000失败
  data: {           // 响应数据
    // ... 具体数据
  },
  message: 'success' // 状态信息
}
```

### 数据生成
使用Mock.js生成随机数据：
```javascript
import Mock from 'mockjs'

const data = Mock.mock({
  'items|10': [{
    id: '@id',
    name: '@name',
    'status|1': ['enabled', 'disabled'],
    createTime: '@datetime'
  }]
})
```

## 使用指南

### 启用Mock
1. 在`vue.config.js`中配置：
```javascript
module.exports = {
  devServer: {
    before: require('./mock/mock-server.js')
  }
}
```

2. 在`main.js`中根据环境启用：
```javascript
if (process.env.NODE_ENV === 'development') {
  require('../mock')
}
```

### 编写Mock接口
1. 在对应模块目录创建接口文件
2. 定义请求路径、方法和响应数据
3. 在`mock/index.js`中注册接口

### 处理请求参数
```javascript
export default {
  url: '/mes/api/warehouses',
  type: 'get',
  response: config => {
    const { page = 1, limit = 10 } = config.query
    const items = data.filter((item, index) => {
      return index < limit * page && index >= limit * (page - 1)
    })
    return {
      code: 20000,
      data: {
        total: data.length,
        items: items
      }
    }
  }
}
```

## 最佳实践

### 数据一致性
- 使用统一的数据结构
- 保持与真实API相同的字段名
- 模拟真实的业务逻辑

### 错误处理
- 模拟各种错误情况
- 返回合适的错误码和信息
- 测试前端错误处理逻辑

### 性能模拟
- 设置合理的响应延迟
- 模拟大数据量场景
- 测试加载状态和分页

## 调试技巧

### 控制台输出
```javascript
response: config => {
  console.log('请求参数:', config.query)
  return {
    code: 20000,
    data: {}
  }
}
```

### 动态修改响应
```javascript
const debug = true
response: config => {
  if (debug) {
    return {
      code: 50000,
      message: '模拟错误'
    }
  }
  return {
    code: 20000,
    data: {}
  }
}
```

### 延迟响应
```javascript
response: config => {
  return new Promise(resolve => {
    setTimeout(() => {
      resolve({
        code: 20000,
        data: {}
      })
    }, 1000)
  })
}
```

# Mock API开发规范

## 接口路径管理和冲突避免

在开发Mock服务时，必须严格遵循以下规则，避免API路径冲突：

### 1. 接口唯一性原则
- **每个API路径必须且只能在一个Mock文件中定义**
- 不允许在多个文件中定义相同的API路径，这会导致接口被覆盖
- 使用[check-mock-api-duplicates.js](mdc:scripts/check-mock-api-duplicates.js)检查API重复问题

### 2. 模块化组织
- Mock文件按业务模块组织，例如：
  - [furnace-type.js](mdc:mock/master-data/furnace-type.js) - 炉型管理
  - [process-parameter.js](mdc:mock/master-data/process-parameter.js) - 工艺参数

### 3. 正确的跨模块引用
```javascript
// 正确做法：导入其他模块的数据
const { someData } = require('./other-module')

// 错误做法：重复定义API
// {
//   url: '/api/already-defined-elsewhere',
//   type: 'get', 
//   response: () => {...}
// }
```

## 数据结构管理

### 1. 保持一致性
- Mock返回的数据结构必须与前端组件期望的结构完全一致
- 字段命名应遵循统一规范，避免前后端字段名不一致
- 必要时添加数据适配层进行转换

### 2. 数据规范
- 提供合理的默认值和初始数据
- 模拟真实后端可能的各种响应状态
- 实现适当的错误处理和消息提示

## 代码质量和维护

### 1. 详细注释
- 为每个API接口添加清晰的注释
- 说明接口的用途、参数和返回值
- 记录修改历史和重要变更

### 2. 测试和调试
- 使用日志记录关键处理流程
- 实现合理的错误处理和边界情况
- 定期审查和更新Mock数据

## 检查清单

开发Mock服务时请确认：
- [ ] API路径没有在多个文件中重复定义
- [ ] 数据结构符合前端组件期望格式
- [ ] 提供了合理的测试数据和错误情况
- [ ] 添加了充分的注释说明
- [ ] 运行`npm run check-mock`验证无API冲突

## 完整规范
完整的Mock服务开发规范请参考[mock-development-rules.md](mdc:mock/mock-development-rules.md)

---
alwaysApply: true
---

# 全局组件优先使用规则

## 核心原则

在编码过程中，**必须优先使用 `src/components` 目录下的全局组件**，避免重复实现已有功能。复杂问题如果有成熟的第三方库解决方案，**必须使用第三方库**，不得自行实现。

## 📦 可用全局组件清单

### 🗂 表格相关组件

#### BaseTable - 配置驱动表格组件
- **功能**：基于Element UI的增强表格，支持虚拟滚动、错误处理、防抖优化
- **使用场景**：所有数据表格展示
- **核心特性**：
  - 配置驱动的列定义
  - 内置分页、状态标签、时间格式化
  - 支持10万+条数据的虚拟滚动
  - 完整的错误边界保护
  - 自动防抖处理


#### TableToolbar - 表格工具栏组件
- **功能**：完整的表格操作工具栏
- **包含功能**：搜索、刷新、导入导出、列设置、批量操作
- **使用场景**：所有需要表格操作的页面

#### Pagination - 增强分页组件
- **功能**：基于Element UI的增强分页，支持防抖、错误处理、性能监控
- **使用场景**：所有分页场景
- **现代化特性**：错误边界、防抖优化、智能重试、性能监控

### 📝 表单相关组件

#### SearchForm - 高级搜索表单组件
- **功能**：配置驱动的搜索表单，支持多种表单项类型
- **核心特性**：
  - 防抖搜索
  - 表单折叠展开
  - 动态组件渲染
  - 支持插槽自定义
- **使用场景**：所有搜索功能


#### EnhancedForm - 增强表单组件
- **功能**：专注表单数据处理和验证的增强组件
- **核心特性**：
  - 支持创建、更新、查看三种模式
  - 防抖保护、智能按钮状态
  - 键盘快捷键支持
  - 变更检测和自动保存
- **使用场景**：所有复杂表单场景

#### DialogForm - 弹窗表单组件
- **功能**：集成对话框的表单组件
- **使用场景**：弹窗中的表单操作

#### DrawerForm - 抽屉表单组件
- **功能**：集成抽屉的表单组件
- **使用场景**：侧边栏表单操作

### 🎛 操作相关组件

#### ActionButtons - 操作按钮组组件
- **功能**：现代化的操作按钮组，支持权限控制、防抖保护
- **核心特性**：
  - 预设按钮配置（表格操作、表单操作、审批操作）
  - 防抖保护、错误处理
  - 无障碍访问支持
- **使用场景**：表格操作列、表单操作、批量操作


#### ExportButton - 导出按钮组件
- **功能**：通用表格数据导出组件，支持Excel导出
- **现代化特性**：防抖保护、请求取消、智能重试、内存管理
- **使用场景**：所有数据导出需求

#### ImportButton - 导入按钮组件
- **功能**：文件导入组件
- **使用场景**：所有数据导入需求

#### RefreshButton - 刷新按钮组件
- **功能**：带防抖保护的刷新按钮
- **使用场景**：数据刷新操作

### 📊 数据展示组件

#### StatusTag - 状态标签组件
- **功能**：标准化的状态显示标签
- **使用场景**：状态字段展示

#### OverflowTagsPopover - 溢出标签组件
- **功能**：标签数量过多时的溢出处理
- **使用场景**：标签列表展示

### 🔧 通用功能组件

#### BatchAction - 批量操作组件
- **功能**：批量操作功能组件
- **使用场景**：表格批量操作

#### ColumnSettings - 列设置组件
- **功能**：表格列显示设置
- **使用场景**：表格个性化配置

#### ErrorHandler - 错误处理组件
- **功能**：统一错误处理
- **使用场景**：错误边界保护

## 🛠 第三方库使用原则

### 必须使用第三方库的场景

1. **复杂的UI交互**
   - 图表渲染：使用 ECharts、Chart.js
   - 富文本编辑：使用 TinyMCE、Quill
   - 日期时间选择：使用 moment.js、day.js
   - 拖拽排序：使用 Sortable.js、vue-draggable

2. **数据处理**
   - Excel操作：使用 xlsx、exceljs
   - PDF生成：使用 jsPDF、PDFKit
   - 文件上传：使用 vue-upload-component
   - 数据验证：使用 joi、yup

3. **工具函数**
   - 工具函数库：使用 lodash、ramda
   - 时间处理：使用 moment.js、day.js
   - 数字格式化：使用 numeral.js
   - 加密解密：使用 crypto-js

4. **性能优化**
   - 虚拟滚动：使用 vue-virtual-scroll-list
   - 图片懒加载：使用 vue-lazyload
   - 防抖节流：使用 lodash.debounce

## 📋 使用检查清单

### 开发前检查（必须）

- [ ] **搜索全局组件**：在 `src/components` 目录中查找是否有现成的组件
- [ ] **阅读组件文档**：查看组件的 README.md 了解功能和用法
- [ ] **评估功能匹配度**：确认组件功能是否满足需求（80%匹配即可使用）
- [ ] **检查第三方库**：复杂功能搜索是否有成熟的第三方库解决方案

### 组件使用检查

- [ ] **正确引入**：使用 `import` 正确引入组件
- [ ] **属性配置**：按照文档正确配置组件属性
- [ ] **事件监听**：正确监听组件事件
- [ ] **插槽使用**：需要自定义时正确使用组件提供的插槽

### 第三方库使用检查

- [ ] **版本选择**：选择稳定版本，避免beta版本
- [ ] **文档完整**：确保第三方库有完整的文档和示例
- [ ] **社区活跃**：选择维护活跃、社区支持好的库
- [ ] **体积考虑**：评估库的体积对项目的影响

## ❌ 禁止行为

### 不得重复实现

1. **表格功能**：不得使用原生 `el-table` 重复实现已有的表格功能
2. **表单功能**：不得重复实现搜索表单、增强表单的功能
3. **操作按钮**：不得重复实现操作按钮组的功能
4. **分页功能**：不得重复实现分页组件的功能
5. **导入导出**：不得重复实现导入导出功能

### 不得自行实现复杂功能

1. **图表渲染**：必须使用 ECharts 等成熟图表库
2. **富文本编辑**：必须使用成熟的富文本编辑器
3. **文件处理**：必须使用专门的文件处理库
4. **复杂算法**：必须使用成熟的算法库
5. **日期时间处理**：必须使用专门的日期库

## 🔍 组件查找方法

### 1. 目录结构查找
```bash
src/components/
├── BaseTable/          # 表格组件
├── SearchForm/         # 搜索表单
├── EnhancedForm/       # 增强表单
├── ActionButtons/      # 操作按钮
├── ExportButton/       # 导出按钮
├── Pagination/         # 分页组件
└── ...
```

### 3. 功能关键词搜索
- 表格 → BaseTable
- 搜索 → SearchForm
- 表单 → EnhancedForm、DialogForm、DrawerForm
- 按钮 → ActionButtons、ExportButton、ImportButton
- 分页 → Pagination
- 状态 → StatusTag


## 🚨 违规处理

### 发现重复实现时
1. **立即停止**当前开发
2. **查找对应**的全局组件或第三方库
3. **重构代码**使用正确的组件
4. **删除冗余**的自实现代码

### 代码审查要点
- 检查是否使用了全局组件
- 检查是否有重复实现的功能
- 检查第三方库的选择是否合理
- 检查组件使用是否正确

通过严格遵循这个规则，确保代码复用性、维护性和开发效率的最大化。

## 🎨 设计风格一致性要求

### 📋 参考标准模块
所有后续模块开发**必须严格按照 `user-management` 模块的设计风格和架构模式**，确保整个项目的一致性和可维护性。

### 🏗️ 模块架构标准
参考 [user-management模块](mdc:src/views/user-management) 的完整架构：

```
module-name/
├── api/                    # API接口层
│   ├── index.js           # 统一导出入口
│   └── module-name.js     # 具体API实现
├── components/            # 组件层
│   ├── ModuleTable.vue    # 表格组件
│   ├── ModuleSearch.vue   # 搜索组件
│   ├── ModuleFormDrawer.vue # 表单抽屉
│   └── ...Dialog.vue      # 各种对话框
├── constants/             # 常量配置层
│   ├── index.js          # 统一导出入口
│   ├── module-name.js    # 基础常量
│   ├── table-config.js   # 表格配置
│   ├── form-config.js    # 表单配置
│   ├── api-config.js     # API配置
│   └── messages-config.js # 消息配置
├── docs/                 # 文档目录
│   ├── 任务清单/         # 开发任务
│   ├── 接口文档/         # API文档
│   └── *.md             # 各种说明文档
├── utils/               # 工具函数（可选）
├── detail.vue           # 详情页面（可选）
└── index.vue            # 主页面入口
```

### 📝 文件命名规范

#### 组件命名
- **主表格组件**：`{Module}Table.vue`（如：`UserTable.vue`）
- **搜索组件**：`{Module}Search.vue` 或 `SearchForm.vue`
- **表单抽屉**：`{Module}FormDrawer.vue`
- **对话框组件**：`{Function}Dialog.vue`（如：`ResetPasswordDialog.vue`）

#### 常量文件命名
- **基础常量**：`{module-name}.js`（如：`user-management.js`）
- **表格配置**：`table-config.js`
- **表单配置**：`form-config.js`
- **API配置**：`api-config.js`
- **消息配置**：`messages-config.js`

#### API文件命名
- **API入口**：`api/index.js`
- **具体实现**：`api/{module-name}.js`

### 🔧 代码结构标准

#### 主页面组件结构
参考 [user-management/index.vue](mdc:src/views/user-management/index.vue) 的标准结构：
- 搜索表单组件
- 数据表格组件（带工具栏）
- 表单抽屉组件
- 各类对话框组件

#### 组件导入顺序
1. 子组件导入
2. API函数导入
3. 常量配置导入
4. 工具函数导入

#### 数据结构标准
- 列表数据：moduleList, listLoading, selectedModules
- 分页配置：pagination (page, limit, total)
- 搜索条件：searchQuery
- 排序条件：sortBy
- 抽屉表单：drawerVisible, drawerMode, currentModule

### 📊 表格组件设计标准

#### 必须使用的全局组件
- **TableToolbar**：表格工具栏（搜索、刷新、导入导出、列设置、批量操作）
- **BaseTable**：数据表格（替代el-table）
- **StatusTag**：状态标签显示
- **ActionButtons**：操作按钮组

### 🔍 搜索组件设计标准

#### 必须使用SearchForm组件
参考 [UserSearch.vue](mdc:src/views/user-management/components/UserSearch.vue) 的实现：
- 使用SearchForm全局组件
- 配置驱动的搜索表单项
- 从constants/form-config.js导入配置

### 📁 常量配置标准

#### constants/index.js 统一导出
参考 [user-management/constants/index.js](mdc:src/views/user-management/constants/index.js)：
- 基础常量（状态、枚举等）
- 表格配置 (table-config.js)
- 表单配置 (form-config.js)
- API配置 (api-config.js)
- 消息配置 (messages-config.js)

#### 表格配置标准
参考 [user-management/constants/table-config.js](mdc:src/views/user-management/constants/table-config.js)：
- TABLE_COLUMNS：列配置（prop、label、sortable、slotName等）
- STATUS_CONFIG：状态配置（typeMap、textMap）

### 📄 文档规范

#### 文件头注释标准
每个文件必须包含：文件名称、文件描述、创建日期、修改记录

#### 必需文档文件
- `docs/接口文档/{模块名}接口说明文档.md`
- `docs/任务清单/{模块名}前端开发任务清单.md`
- 各组件内的README.md文档

### 🎯 开发流程规范

#### 开发前检查清单
- [ ] **架构对齐**：目录结构严格按照user-management模块
- [ ] **组件复用**：优先使用已定义的全局组件
- [ ] **命名规范**：文件和组件命名遵循既定规范
- [ ] **配置分离**：常量配置独立到constants目录
- [ ] **文档完整**：API文档和开发文档齐全

#### 代码风格要求
- **组件拆分**：页面组件化，功能单一职责
- **配置驱动**：表格和表单使用配置文件驱动
- **统一导入**：通过index.js统一导出和导入
- **错误处理**：参考user-management的错误处理模式
- **样式规范**：使用scoped样式，响应式设计

#### 质量保证标准
- **功能一致性**：相同功能的实现方式必须一致
- **用户体验**：交互模式和视觉风格保持统一
- **代码复用**：最大化利用已有组件和工具函数
- **文档维护**：及时更新相关文档

### 🚨 一致性检查清单

开发完成后必须检查：
- [ ] 目录结构与user-management模块完全一致
- [ ] 组件命名遵循既定规范
- [ ] 使用了正确的全局组件（BaseTable、SearchForm、ActionButtons等）
- [ ] 常量配置独立且结构统一
- [ ] API调用模式与user-management一致
- [ ] 错误处理和消息提示模式一致
- [ ] 文档格式和内容完整性符合标准

通过严格遵循user-management模块的设计风格，确保整个项目的架构一致性、代码可维护性和团队开发效率。

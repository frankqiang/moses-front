---
alwaysApply: true
description: 开发阶段配置规则 - 权限检查和API数据源
---

# 开发阶段配置规则

## 🚀 开发环境权限策略

### 权限检查处理原则
**开发阶段不编写任何权限检查代码**，所有功能页面和组件都应该可以直接访问和测试。

#### 权限相关代码处理方式
1. **页面级权限**：不编写 `checkPagePermission()` 等权限检查方法
2. **组件级权限**：不使用 `v-if="hasPermission()"` 等权限控制指令
3. **路由权限**：路由配置中不设置 `meta.roles` 和 `meta.permissions` 限制
4. **按钮权限**：所有操作按钮直接显示，不进行权限过滤

#### 生产环境权限实现
在开发完成准备上线时，统一添加完整的权限检查体系。

## 📡 API数据源配置

### Mock服务已弃用
**重要**：本项目已完全弃用Mock服务，所有数据都来源于后端真实API接口。

#### API配置要求
1. **数据源**：所有数据必须从接口文档中定义的后端API获取
2. **Mock清理**：移除所有Mock相关的配置和代码
3. **接口调用**：确保所有API调用都指向真实的后端服务

#### 接口文档位置
- 组织结构管理：[组织结构管理模块接口文档.md](mdc:src/views/organization-structure/docs/接口文档/组织结构管理模块接口文档.md)
- 角色管理：[角色管理模块接口文档.md](mdc:src/views/role-management/docs/接口文档/角色管理模块接口文档.md)
- 其他模块的接口文档位于各自的 `docs/接口文档/` 目录下

#### API调用标准
所有数据获取必须通过真实后端API，不使用Mock数据。

## 📝 消息提示规范

### 统一响应格式遵循
**重要**：严格按照接口文档中定义的统一响应格式处理前端消息提示，不得硬编码消息内容。

#### 成功消息处理原则
1. **优先使用后端消息**：所有成功操作必须使用 `response.message` 字段
2. **备用消息规范**：仅在后端未返回消息时使用符合业务语义的备用消息
3. **禁止硬编码**：不允许忽略后端返回的消息而使用自定义文本

**正确示例**：
```javascript
// ✅ 正确：使用后端返回的消息
const response = await deleteRole(roleId)
this.$message.success(response.message || '删除角色成功')

// ❌ 错误：硬编码消息
await deleteRole(roleId)
this.$message.success('删除成功')
```

#### 错误消息处理原则
1. **错误码优先**：根据 `error.code` 进行错误类型判断和消息展示
2. **后端消息优先**：优先显示 `error.message` 中的用户友好错误描述
3. **分类处理**：按照错误码前缀进行分类处理（AUTH_、VAL_、ROLE_、SYS_等）

**正确示例**：
```javascript
// ✅ 正确：使用后端错误码和消息
catch (error) {
  if (error.code === 'ROLE_SYSTEM_CANNOT_DELETE') {
    this.$message.error(error.message || '系统角色不能删除')
  } else {
    this.$message.error(error.message || '操作失败，请稍后重试')
  }
}
```

#### 统一响应格式标准
参考各模块接口文档开头的"统一响应格式"章节：

**成功响应格式**：
- `success: true`
- `data: {}` - 业务数据
- `message: "操作成功的描述信息"` - **必须使用此字段**
- `meta: {}` - 元数据信息

**错误响应格式**：
- `success: false`
- `error.code` - 标准化错误码
- `error.message` - 用户友好错误描述 - **必须使用此字段**
- `error.details` - 错误详细信息
- `meta: {}` - 元数据信息

## 🛠️ 开发环境配置检查清单

### 开发阶段要求
- [ ] **无权限检查代码**：页面和组件中不编写任何权限检查逻辑
- [ ] **Mock服务已移除**：确认没有使用任何Mock数据源
- [ ] **API接口正确**：所有数据获取都通过真实后端API
- [ ] **接口文档对齐**：API调用与接口文档保持一致
- [ ] **消息提示规范**：所有成功/错误消息使用后端返回的 `message` 字段，不硬编码

### 生产环境准备
- [ ] **权限体系实现**：统一添加完整的权限检查代码
- [ ] **环境变量配置**：确保生产环境的API地址正确配置
- [ ] **错误处理完善**：确保所有API调用都有完整的错误处理


## ⚠️ 重要提醒

1. **开发阶段**：专注业务功能实现，不编写权限检查代码
2. **生产准备**：统一实现完整的权限检查体系
3. **部署前确认**：确保生产环境配置正确
4. **接口对齐**：定期检查API调用与接口文档的一致性
5. **消息一致性**：确保前端提示消息与后端响应消息完全一致，避免用户混淆

---

**此规则适用于整个开发阶段，项目准备上线前统一实现权限检查功能。**

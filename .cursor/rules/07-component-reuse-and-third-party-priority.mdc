---
description: 
globs: **/*.vue,**/*.js
alwaysApply: false
---
# 组件复用与第三方库优先原则

## 规则类型：Auto Attached
**匹配模式**: `**/*.{vue,js,ts}`
**说明**: 所有Vue组件和JavaScript/TypeScript文件都需要遵循组件复用和第三方库优先原则

## 核心原则

### 🔄 全局组件优先原则
**避免重复造轮子，优先使用已有的全局组件**

1. **开发前必须检查**: 在开发新功能前，必须先检查是否有现成的全局组件可以使用
2. **组件目录参考**: 查看 [src/components](mdc:src/components) 目录下的所有全局组件
3. **功能扩展**: 如果现有组件功能接近但不完全匹配，优先考虑扩展现有组件而不是新建

### 📦 第三方库优先原则
**成熟的第三方库优于自实现**

1. **工具函数**: 优先使用成熟的第三方库（如lodash）而不是自己实现
2. **UI组件**: 基于Element UI进行扩展，而不是完全自定义
3. **通用功能**: 如时间处理、数据验证、图表等，优先选择社区成熟方案

## 必须使用的全局组件清单

### 📊 表格相关组件
- **[BaseTable](mdc:src/components/BaseTable/index.vue)** - 基础表格组件
  - 使用场景: 所有数据列表页面
  - 特性: 虚拟滚动、响应式、错误处理
  - 禁止: 直接使用 `el-table` 创建复杂表格

- **[TableToolbar](mdc:src/components/TableToolbar/index.vue)** - 表格工具栏
  - 使用场景: 表格上方的操作区域
  - 特性: 搜索、刷新、导入导出、列设置、批量操作
  - 禁止: 自实现表格工具栏功能

- **[Pagination](mdc:src/components/Pagination/index.vue)** - 分页组件
  - 使用场景: 所有需要分页的列表
  - 特性: 响应式、配置灵活、自动滚动
  - 禁止: 直接使用 `el-pagination`

### 🏷️ 状态与标签组件
- **[StatusTag](mdc:src/components/StatusTag/index.vue)** - 状态标签
  - 使用场景: 显示各种状态信息
  - 特性: 预设配置、自定义映射、现代化交互
  - 禁止: 使用 `el-tag` 显示业务状态

### 🔧 操作按钮组件
- **[ActionButtons](mdc:src/components/ActionButtons/index.vue)** - 操作按钮组
  - 使用场景: 表格行操作、卡片操作等
  - 特性: 预设配置、权限控制、防抖保护
  - 禁止: 自实现按钮组功能

- **[BatchAction](mdc:src/components/BatchAction/index.vue)** - 批量操作
  - 使用场景: 表格批量操作功能
  - 特性: 多选确认、操作分组、错误处理

- **[RefreshButton](mdc:src/components/RefreshButton/index.vue)** - 刷新按钮
  - 使用场景: 需要刷新功能的页面
  - 特性: 防抖保护、加载状态、错误重试

### 📥📤 导入导出组件
- **[ImportButton](mdc:src/components/ImportButton/index.vue)** - 导入按钮
  - 使用场景: 数据导入功能
  - 特性: 文件验证、模板下载、进度显示

- **[ExportButton](mdc:src/components/ExportButton/index.vue)** - 导出按钮
  - 使用场景: 数据导出功能
  - 特性: 多格式支持、筛选导出、下载管理

### ⚙️ 配置组件
- **[ColumnSettings](mdc:src/components/ColumnSettings/index.vue)** - 列设置
  - 使用场景: 表格列显示控制
  - 特性: 拖拽排序、显示隐藏、配置保存

## 必须使用的第三方库清单

### 🛠️ 工具函数库
```javascript
// 从统一入口导入工具函数
import { debounce, throttle, cloneDeep } from '@/utils'

// ✅ 正确：使用第三方库
const debouncedSearch = debounce(this.handleSearch, 300)

// ❌ 错误：自实现防抖
function myDebounce(fn, delay) { /* ... */ }
```

**必须使用的工具库**:
- `lodash.debounce` - 防抖函数
- `lodash.throttle` - 节流函数  
- `lodash.clonedeep` - 深拷贝
- `moment.js` 或 `dayjs` - 时间处理
- `echarts` - 图表库

### 📊 图表组件
```javascript
// ✅ 正确：使用ECharts
import { echarts } from '@/utils'
const chart = echarts.init(container)

// ❌ 错误：自实现图表
// 不要自己用Canvas或SVG画图表
```

### 📝 表单验证
```javascript
// ✅ 正确：使用Element UI的验证规则
rules: {
  email: [
    { required: true, message: '邮箱不能为空', trigger: 'blur' },
    { type: 'email', message: '邮箱格式不正确', trigger: 'blur' }
  ]
}

// ❌ 错误：自实现验证逻辑
// validateEmail(email) { /* 自定义邮箱验证 */ }
```

## 开发流程规范

### 1. 功能分析阶段
在开始编码前，必须完成以下检查：

```markdown
## 功能开发检查清单
- [ ] 检查是否有现成的全局组件可直接使用
- [ ] 检查现有组件是否可通过配置满足需求
- [ ] 搜索是否有合适的第三方库
- [ ] 评估扩展现有组件 vs 新建组件的成本
- [ ] 确认选择的方案符合项目规范
```

### 2. 组件选择决策树
```
需要实现功能
├── 是否有完全匹配的全局组件？
│   ├── 是 → 直接使用全局组件
│   └── 否 → 继续
├── 是否有功能相近的全局组件？
│   ├── 是 → 扩展现有组件
│   └── 否 → 继续  
├── 是否有合适的第三方库？
│   ├── 是 → 集成第三方库
│   └── 否 → 继续
└── 确认必须自实现 → 新建组件（需要评审）
```

### 3. 代码实现规范

#### ✅ 正确示例
```vue
<template>
  <div class="product-list">
    <!-- 使用全局表格工具栏 -->
    <table-toolbar
      :enable-batch-actions="true"
      :enable-export="true"
      :enable-column-settings="true"
      @refresh="getList"
      @export="handleExport"
    />
    
    <!-- 使用全局基础表格 -->
    <base-table
      :data="tableData"
      :columns="columns"
      :loading="loading"
      @selection-change="handleSelectionChange"
    />
    
    <!-- 使用全局分页组件 -->
    <pagination
      v-show="total > 0"
      :total="total"
      :page.sync="query.page"
      :limit.sync="query.limit"
      @pagination="getList"
    />
  </div>
</template>

<script>
// 使用第三方工具函数
import { debounce } from '@/utils'

export default {
  created() {
    // 使用防抖处理搜索
    this.debouncedSearch = debounce(this.handleSearch, 300)
  }
}
</script>
```

#### ❌ 错误示例
```vue
<template>
  <div class="product-list">
    <!-- 错误：自实现工具栏 -->
    <div class="custom-toolbar">
      <el-button @click="refresh">刷新</el-button>
      <el-button @click="exportData">导出</el-button>
    </div>
    
    <!-- 错误：直接使用el-table而不是BaseTable -->
    <el-table :data="tableData">
      <!-- ... -->
    </el-table>
    
    <!-- 错误：自实现分页 -->
    <div class="custom-pagination">
      <el-button @click="prevPage">上一页</el-button>
      <span>{{ currentPage }}</span>
      <el-button @click="nextPage">下一页</el-button>
    </div>
  </div>
</template>

<script>
export default {
  methods: {
    // 错误：自实现防抖
    debounce(fn, delay) {
      let timer = null
      return function(...args) {
        if (timer) clearTimeout(timer)
        timer = setTimeout(() => fn.apply(this, args), delay)
      }
    }
  }
}
</script>
```

## 扩展现有组件指南

### 何时扩展 vs 新建
- **扩展现有组件**：功能相似度 > 70%
- **新建组件**：功能完全不同，且确认无合适的第三方库

### 扩展方式
1. **属性扩展**：添加新的props来支持新功能
2. **插槽扩展**：使用slot提供自定义内容区域
3. **事件扩展**：添加新的事件来支持新的交互
4. **混入扩展**：通过mixin添加通用功能

## 第三方库集成规范

### 1. 库选择标准
- **社区活跃度**：GitHub stars > 1000，最近6个月有更新
- **文档完善度**：有详细的API文档和使用示例
- **浏览器兼容性**：支持项目目标浏览器
- **包大小**：合理的打包体积，支持按需引入
- **TypeScript支持**：提供类型定义文件

### 2. 集成步骤
```bash
# 1. 安装依赖
npm install package-name

# 2. 在utils/index.js中统一导出
export { default as packageName } from 'package-name'

# 3. 在需要的地方导入使用
import { packageName } from '@/utils'
```

### 3. 推荐的第三方库

#### 工具类库
- **lodash.*** - 各种工具函数（按需安装）
- **dayjs** - 轻量级时间处理库
- **validator** - 数据验证库
- **crypto-js** - 加密解密库

#### UI增强库
- **echarts** - 图表库
- **vue-draggable** - 拖拽排序
- **vue-virtual-scroll-list** - 虚拟滚动
- **vue-lazyload** - 图片懒加载

#### 功能库
- **file-saver** - 文件下载
- **xlsx** - Excel文件处理
- **qrcode** - 二维码生成
- **print-js** - 打印功能

## 违规检查与处罚

### 代码审查检查项
- [ ] 是否使用了合适的全局组件
- [ ] 是否优先使用了第三方库
- [ ] 是否存在重复实现已有功能
- [ ] 新建组件是否有充分理由

### 常见违规行为
1. **重复造轮子**：实现已有的工具函数或组件功能
2. **忽略全局组件**：在有合适全局组件的情况下自实现
3. **过度自定义**：不必要的自定义实现
4. **未使用工具函数**：自实现防抖、节流等通用功能

## 例外情况

### 允许自实现的情况
1. **性能关键场景**：第三方库性能不足以满足需求
2. **特殊业务逻辑**：业务逻辑过于复杂，无法通过配置实现
3. **依赖冲突**：第三方库与现有技术栈存在冲突
4. **安全考虑**：第三方库存在安全风险

### 申请流程
如需违反此规范，需要：
1. 提供详细的技术分析报告
2. 说明无法使用现有方案的具体原因
3. 评估自实现的维护成本
4. 经过技术负责人审批

## 持续改进

### 定期评估
- **月度评估**：检查是否有新的优秀第三方库可以替换现有实现
- **季度优化**：整理和优化全局组件，移除冗余功能
- **年度升级**：升级主要依赖库到最新稳定版本

### 反馈机制
- 发现更好的第三方库时，及时更新规范
- 全局组件功能不足时，优先扩展而不是新建
- 收集开发者使用反馈，持续优化组件API

---

**记住：好的代码不是写出来的，而是组合出来的。优先使用成熟的组件和库，把精力集中在业务逻辑的实现上。**


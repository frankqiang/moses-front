---
description:
globs: **/*.vue,**/*.js
alwaysApply: false
---
# 现代前端开发范式与组件复用规范

## 规则说明
本规则专门针对现代前端开发的最佳范式，重点关注性能优化、错误处理、用户体验、代码健壮性和组件复用。所有组件开发必须遵循这些现代化标准，确保应用的高性能、高可用性和优秀的用户体验。

## 🎯 核心范式原则

### 🔄 组件复用优先原则
**避免重复造轮子，优先使用已有的全局组件**

1. **开发前必须检查**: 在开发新功能前，必须先检查 [src/components](mdc:src/components) 目录下的全局组件
2. **功能扩展**: 如果现有组件功能接近但不完全匹配，优先考虑扩展现有组件而不是新建
3. **决策树**: 全局组件 → 扩展现有组件 → 第三方库 → 自实现（需评审）

### 📦 第三方库优先原则
**成熟的第三方库优于自实现**

- **工具函数**: 使用 `lodash.*`、`dayjs` 等成熟库而不是自己实现
- **UI组件**: 基于 Element UI 扩展，而非完全自定义
- **专用功能**: 图表用 `echarts`，拖拽用 `vue-draggable`，时间处理用 `dayjs`
- **统一导入**: 从 `@/utils` 统一导入第三方工具

### 🚀 性能优先范式
- **虚拟化渲染**：大数据集合（>1000项）必须实现虚拟滚动
- **防抖节流机制**：用户交互必须添加防抖保护（300ms标准）
- **懒加载策略**：按需加载资源和组件
- **缓存优化**：计算结果和API响应智能缓存

### 🛡️ 容错设计范式
- **错误边界保护**：每个功能模块实现错误隔离
- **优雅降级**：异常场景提供备用方案
- **重试机制**：网络和临时错误自动恢复
- **状态一致性**：确保UI状态与数据状态同步
- **依赖管理**：确保所有第三方依赖正确安装和版本管理

### 📱 体验优化范式
- **即时反馈**：所有操作提供实时状态反馈
- **渐进增强**：核心功能优先，高级功能逐步加载
- **无障碍支持**：遵循WCAG标准的可访问性设计
- **响应式适配**：多设备尺寸完美适配
- **向后兼容**：新功能通过特性开关渐进式启用

## 📋 必须使用的全局组件清单

### 表格系统
- **[BaseTable](mdc:src/components/BaseTable/index.vue)** - 替代 `el-table`，支持虚拟滚动、响应式、错误处理
- **[TableToolbar](mdc:src/components/TableToolbar/index.vue)** - 表格工具栏，包含搜索、刷新、导入导出、列设置、批量操作
- **[Pagination](mdc:src/components/Pagination/index.vue)** - 替代 `el-pagination`，支持响应式、自动滚动

### 操作组件
- **[ActionButtons](mdc:src/components/ActionButtons/index.vue)** - 操作按钮组，支持预设配置、权限控制、防抖保护
- **[StatusTag](mdc:src/components/StatusTag/index.vue)** - 替代 `el-tag` 显示业务状态，支持预设配置、自定义映射
- **[RefreshButton](mdc:src/components/RefreshButton/index.vue)** - 刷新功能，支持防抖保护、加载状态、错误重试

### 数据交换
- **[ImportButton](mdc:src/components/ImportButton/index.vue)** - 数据导入，支持文件验证、模板下载、进度显示
- **[ExportButton](mdc:src/components/ExportButton/index.vue)** - 数据导出，支持多格式、筛选导出、下载管理
- **[BatchAction](mdc:src/components/BatchAction/index.vue)** - 批量操作，支持多选确认、操作分组、错误处理
- **[ColumnSettings](mdc:src/components/ColumnSettings/index.vue)** - 列设置，支持拖拽排序、显示隐藏、配置保存

## 🛠️ 第三方库集成标准

### 必须使用的工具库
参考项目中[utils/index.js](mdc:src/utils/index.js)的实现标准：

```javascript
// ✅ 正确：从统一入口导入
import { debounce, throttle, cloneDeep, echarts } from '@/utils'

// ❌ 错误：自实现工具函数
function myDebounce(fn, delay) { /* 禁止 */ }
```

**核心工具库**:
- `lodash.debounce/throttle` - 防抖节流
- `echarts` - 图表组件
- `dayjs` - 时间处理
- `vue-draggable` - 拖拽功能

### 第三方库选择原则
1. **优先使用专用库**：如lodash.debounce而非完整lodash
2. **版本锁定**：在package.json中明确版本号
3. **按需引入**：避免全量引入大型库
4. **兼容性检查**：确保与项目Vue版本兼容
5. **文档齐全**：选择有完善文档和社区支持的库

## 💻 技术实现标准

### 虚拟滚动实现
大数据列表（>1000项）必须使用虚拟滚动，参考 [BaseTable组件](mdc:src/components/BaseTable/index.vue)：

```javascript
computed: {
  shouldVirtualize() {
    return this.data.length > 1000
  }
}
```

### 防抖节流优化策略
```javascript
// 组件防抖使用标准
created() {
  this.debouncedSearch = debounce(this.handleSearch, 300)
  this.debouncedSave = debounce(this.handleSave, 500)
}
```



### 向后兼容性设计
参考[ActionButtons组件](mdc:src/components/ActionButtons/index.vue)的兼容性设计：

```javascript
// 向后兼容的特性开关
props: {
  enableModernFeatures: { type: Boolean, default: false }
},
computed: {
  effectiveButtons() {
    return this.enableModernFeatures && this.preset
      ? this.getPresetButtons(this.preset)
      : this.buttons // 兼容传统用法
  }
}
```

## 📊 代码实现规范

### ✅ 正确示例
```vue
<template>
  <div class="product-list">
    <!-- 使用全局组件 -->
    <table-toolbar :enable-batch-actions="true" @refresh="getList" />
    <base-table :data="tableData" :columns="columns" />
    <pagination :total="total" :page.sync="query.page" @pagination="getList" />
  </div>
</template>

<script>
import { debounce } from '@/utils'

export default {
  created() {
    this.debouncedSearch = debounce(this.handleSearch, 300)
  }
}
</script>

```
### 滚动动画工具函数
**重要**: 所有需要滚动功能的页面必须使用全局滚动动画工具函数。

```js
import { scrollTo } from '@/utils/scroll-to'

// 滚动到顶部
scrollTo(0, 500)

// 滚动到指定位置，带回调
scrollTo(400, 800, () => console.log('滚动完成'))
```

### ❌ 错误示例
```vue
<template>
  <!-- 错误：自实现已有组件功能 -->
  <div class="custom-toolbar">
    <el-button @click="refresh">刷新</el-button>
  </div>
  <!-- 错误：直接使用el-table而不是BaseTable -->
  <el-table :data="tableData"></el-table>
</template>

<script>
export default {
  methods: {
    // 错误：自实现工具函数
    debounce(fn, delay) { /* 禁止 */ }
  }
}
</script>
```

## 🔧 质量检查清单

### 强制检查项
- [ ] **组件复用**: 使用了合适的全局组件
- [ ] **第三方库**: 优先使用了成熟的第三方库
- [ ] **防抖保护**: 用户交互添加了防抖机制
- [ ] **错误处理**: 实现了完善的错误处理
- [ ] **性能优化**: 大数据使用了虚拟滚动
- [ ] **ESLint合规**: 代码通过ESLint检查

### 性能基准要求
- **首屏渲染（FCP）**：< 1.5秒
- **交互响应时间**：< 100ms
- **列表滚动帧率**：保持60fps
- **内存使用增长**：< 10MB/小时

## 🚫 违规行为与例外

### 常见违规行为
1. **重复造轮子**：实现已有的工具函数或组件功能
2. **忽略全局组件**：自实现已有的全局组件功能
3. **直接使用基础组件**：用 `el-table` 而不是 `BaseTable`
4. **自实现通用功能**：防抖、节流、时间处理等

### 允许例外的情况
1. **性能关键**: 第三方库性能不足
2. **特殊业务**: 业务逻辑过于复杂
3. **依赖冲突**: 与现有技术栈冲突
4. **安全考虑**: 第三方库存在安全风险

**申请例外需要**: 技术分析报告 + 成本评估 + 技术负责人审批

## 🔄 组件扩展指南

### 扩展 vs 新建决策
- **扩展现有组件**: 功能相似度 > 70%
- **新建组件**: 功能完全不同且无合适第三方库

### 扩展方式
1. **属性扩展**: 添加新props支持新功能
2. **插槽扩展**: 使用slot提供自定义区域
3. **事件扩展**: 添加新事件支持新交互
4. **混入扩展**: 通过mixin添加通用功能

## 📈 持续改进

### 定期维护
- **月度评估**: 检查新的优秀第三方库
- **季度优化**: 整理全局组件，移除冗余功能
- **年度升级**: 升级主要依赖到最新稳定版

### 反馈机制
- 发现更好的第三方库时，及时更新规范
- 全局组件功能不足时，优先扩展而不是新建
- 收集开发者反馈，持续优化组件API

---

**核心理念：好的代码不是写出来的，而是组合出来的。优先使用成熟的组件和库，把精力集中在业务逻辑的实现上。**


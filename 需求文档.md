# 铝箔退火炉车间MES系统功能开发文档

**版本:** 2.0
**日期:** 2023-10-28

---

## 目录

1.  [引言](#1-引言)
    1.1. [文档目的](#11-文档目的)
    1.2. [系统范围](#12-系统范围)
    1.3. [读者对象](#13-读者对象)
    1.4. [术语定义](#14-术语定义)
    1.5. [假设与约束](#15-假设与约束)
2.  [系统概述](#2-系统概述)
    2.1. [系统描述](#21-系统描述)
    2.2. [系统目标](#22-系统目标)
    2.3. [主要特点](#23-主要特点)
    2.4. [系统架构](#24-系统架构)
    2.5. [核心业务实体](#25-核心业务实体)
3.  [功能需求](#3-功能需求)
    3.1. [主数据管理模块 (MDM)](#31-主数据管理模块)
        3.1.1. [料框规格管理](#311-料框规格管理)
        3.1.2. [铝箔产品管理](#312-铝箔产品管理)
        3.1.3. [物料编码管理](#313-物料编码管理)
        3.1.4. [库位主数据管理](#314-库位主数据管理)
        3.1.5. [设备主数据管理](#315-设备主数据管理)
        3.1.6. [工艺参数管理](#316-工艺参数管理)
        3.1.7. [质量标准配置](#317-质量标准配置)
    3.2. [库存管理模块 (INV)](#32-库存管理模块)
        3.2.1. [料框/料垛管理](#321-料框料垛管理)
        3.2.2. [库存状态管理](#322-库存状态管理)
        3.2.3. [物料位置追踪](#323-物料位置追踪)
        3.2.4. [库位分配优化](#324-库位分配优化)
    3.3. [生产任务管理模块 (PROD)](#33-生产任务管理模块)
        3.3.1. [生产计划管理](#331-生产计划管理)
        3.3.2. [退火任务管理](#332-退火任务管理)
        3.3.3. [退火炉排程](#333-退火炉排程)
        3.3.4. [物流任务生成](#334-物流任务生成)
    3.4. [执行与监控模块 (EXEC)](#34-执行与监控模块)
        3.4.1. [退火过程监控](#341-退火过程监控)
        3.4.2. [行车作业跟踪](#342-行车作业跟踪)
        3.4.3. [料车作业跟踪 (AGV/RGV)](#343-料车作业跟踪-agvrgv)
        3.4.4. [炉次过程管理](#344-炉次过程管理)
        3.4.5. [设备状态监控](#345-设备状态监控)
        3.4.6. [质量检验执行](#346-质量检验执行)
    3.5. [报表与看板模块 (REPORT)](#35-报表与看板模块)
        3.5.1. [生产分析报表](#351-生产分析报表)
        3.5.2. [质量分析报表](#352-质量分析报表)
        3.5.3. [设备分析报表](#353-设备分析报表)
        3.5.4. [实时监控看板](#354-实时监控看板)
        3.5.5. [数据分析工具](#355-数据分析工具)
    3.6. [用户权限管理模块 (USER)](#36-用户权限管理模块)
        3.6.1. [用户管理](#361-用户管理)
        3.6.2. [组织结构管理](#362-组织结构管理)
        3.6.3. [角色定义](#363-角色定义)
        3.6.4. [权限配置](#364-权限配置)
        3.6.5. [安全策略](#365-安全策略)
4.  [接口设计](#4-接口设计)
    4.1. [外部系统接口](#41-外部系统接口)
        4.1.1. [ERP系统集成](#411-erp系统集成)
        4.1.2. [质量管理系统(QMS)集成](#412-质量管理系统qms集成)
        4.1.3. [仓储系统(WMS)集成](#413-仓储系统wms集成)
    4.2. [设备接口](#42-设备接口)
        4.2.1. [PLC 集成 (西门子 S7-1200)](#421-plc-集成-西门子-s7-1200)
        4.2.2. [其他设备接口](#422-其他设备接口)
5.  [非功能需求](#5-非功能需求)
    5.1. [性能需求](#51-性能需求)
    5.2. [易用性需求](#52-易用性需求)
    5.3. [可靠性需求](#53-可靠性需求)
    5.4. [安全性需求](#54-安全性需求)
6.  [数据字典 (示例)](#6-数据字典-示例)
7.  [部署概述](#7-部署概述)

---

## 1. 引言

### 1.1. 文档目的

本文档旨在详细描述铝箔退火炉车间制造执行系统（MES）的功能需求、设计细节、接口规范及非功能性要求。其主要目的是作为系统开发、测试、实施和验收的核心依据，确保开发团队、项目管理人员、业务方及其他相关方对系统功能有统一、清晰且具体的理解，最大限度地减少需求误解，保障项目顺利交付。

### 1.2. 系统范围

本MES系统专注于铝箔退火炉车间的生产执行层面，管理和追踪物料（以料框/料垛形式存在的铝箔卷）从进入车间到离开车间的全过程。系统覆盖的核心流程与实体包括：
*   **物料管理:** 料框/料垛的注册、状态跟踪（待退火、退火中、已退火、待检等）、位置追踪（库位、备料台、料车、炉内）、组垛与拆垛操作。
*   **生产计划与调度:** 接收生产指令，生成退火任务，对退火炉（考虑容量约40T）进行智能排程，生成配套的物流任务（行车、自动料车）。
*   **生产执行与监控:** 指导和确认装/出炉操作，实时监控退火炉工艺参数（温度曲线等），跟踪行车及自动料车（含备料台交互）的作业。
*   **质量管理:** 执行在线或离线的质量检验录入，结果判定与关联追溯。
*   **设备管理:** 监控退火炉、行车、自动料车的实时状态与基础OEE计算。
*   **数据可视化:** 提供实时看板和生产、质量、设备分析报表。
*   **集成:** 与上层ERP系统（同步订单、反馈实绩）、QMS系统（同步标准、反馈结果）、WMS系统（库存协同）以及底层西门子S7-1200 PLC（工艺控制与数据采集）、行车/自动料车控制系统、扫码设备等进行数据交互。

**不在此范围内的内容（需明确）：**
*   详细的财务成本核算（MES提供基础数据给ERP）。
*   人力资源管理。
*   完整的设备维护管理系统（CMMS/EAM），MES仅做基础状态监控和维护提醒。
*   胚料铝卷入库、轧制、分切、包装、成品出库等退火前后工序的详细管理（仅做接口数据交互）。

### 1.3. 读者对象

本文档的主要读者包括：
*   **系统开发工程师:** 根据本文档进行详细设计和编码实现。
*   **系统测试工程师:** 基于本文档编写测试用例并执行测试。
*   **项目管理人员:** 了解系统范围、功能和进度，进行项目管控。
*   **业务需求分析师:** 确认需求是否被准确理解和描述。
*   **系统实施顾问:** 指导系统的部署、配置和用户培训。
*   **最终用户代表 (如车间主管、调度员、操作员):** 确认功能是否满足实际业务需求。

### 1.4. 术语定义

| 术语 | 英文缩写/全称 | 解释 |
|---|---|---|
| MES | Manufacturing Execution System | 制造执行系统 |
| ERP | Enterprise Resource Planning | 企业资源计划 |
| QMS | Quality Management System | 质量管理系统 |
| WMS | Warehouse Management System | 仓库管理系统 |
| PLC | Programmable Logic Controller | 可编程逻辑控制器（特指西门子S7-1200） |
| OPC UA | OPC Unified Architecture | OPC统一架构（工业通讯协议，用于与PLC等设备安全可靠通讯） |
| **料框** | Bin / Basket | 承载铝箔卷进行退火的标准金属框架，具有唯一二维码标识。 |
| **料垛** | Stack | 由多个规格、产品、状态相同的料框垂直堆叠组成的单元，具有唯一标识（可选），作为整体进行物流。 |
| **退火炉** | Annealing Furnace | 对铝箔卷进行热处理的设备，本系统涉及的炉型容量约40T。 |
| **行车** | Overhead Crane | 车间顶部用于垂直升降和水平移动重物的起重设备，用于吊运料框/料垛。 |
| **自动料车** | AGV / RGV | 自动导引或有轨导引的物料搬运车，用于地面水平运输，特别是与备料台和退火炉的自动交互。 |
| **备料台** | Staging Table / Preparation Platform | 退火炉附近用于临时存放待装炉或刚出炉物料的平台，可与自动料车交互。 |
| **库位** | Storage Location | 车间内（如待退火区、已退火区）用于存放料框/料垛的具体物理位置，具有唯一编号。 |
| **铝箔产品** | Aluminum Foil Product | 指不同牌号、规格（厚度、宽度）的铝箔卷。 |
| **批次号** | Batch Number / Lot Number | 用于标识一批生产物料的编号，用于追溯。 |
| **温度曲线** | Temperature Profile | 退火过程中炉温随时间变化的预设曲线（包含升温段、保温段、降温段）。 |
| **工艺参数** | Process Parameter | 控制退火过程的技术设定值（如温度、时间、升降温速率、保护气氛类型及流量等）。 |
| **退火任务** | Annealing Task | MES下达给特定退火炉执行一次退火操作的指令，包含物料清单和工艺要求。 |
| **物流任务** | Logistics Task | MES生成的物料搬运指令，由行车或自动料车执行。 |
| OEE | Overall Equipment Effectiveness | 设备综合效率，衡量设备利用情况的关键指标。 |
| MTBF | Mean Time Between Failures | 平均故障间隔时间。 |
| MTTR | Mean Time To Repair | 平均修复时间。 |
| **单/双零箔** | Single/Double Zero Foil | 铝箔按厚度分类，是本系统处理的主要产品类型。 |

### 1.5. 假设与约束

*   **假设:**
    *   车间内提供稳定可靠的有线以太网和（或）工业级无线网络覆盖，满足设备（PLC、AGV、PDA等）的通讯需求。
    *   西门子S7-1200 PLC已编程实现对退火炉的基本控制逻辑，并能通过OPC UA提供约定的数据接口。
    *   行车和自动料车具备其独立的控制系统，并能提供与其他系统交互的接口（协议待确认）。
    *   所有料框均粘贴有规范、清晰、耐用的二维码标签，内容包含唯一料框ID。
    *   操作人员（调度员、行车工、炉前工、质检员）经过培训，能够按照MES系统的指导和要求进行操作（包括扫码、确认等）。
    *   客户已有ERP、QMS、WMS系统，并能提供相应的集成接口（API或数据库视图）。
*   **约束:**
    *   MES系统需优先适配西门子S7-1200 PLC，通讯协议首选OPC UA。详细的PLC数据点表（DB地址、数据类型、读写权限、频率等）是本MES成功实施的关键依赖，需由自动化工程师提供并确认。
    *   MES需能与客户现有的行车控制系统和双向自动料车控制系统集成。接口协议和数据格式需与设备供应商确认并遵循。
    *   系统开发需在特定的预算和时间范围内完成（具体待定）。
    *   系统初期将在客户本地服务器环境部署（硬件配置要求待定）。
    *   系统主要用户界面语言为简体中文，需要提供英文界面切换选项。
    *   系统主要处理单零箔和双零箔的退火业务。

## 2. 系统概述

### 2.1. 系统描述

铝箔退火炉车间MES系统是一个集成了车间物料追踪、生产调度、过程监控、质量记录和设备状态管理的信息化系统。它旨在打通企业计划层（ERP）与车间自动化控制层（PLC、行车、自动料车等）之间的信息壁垒，实现退火生产过程的精细化、透明化和智能化管理。通过实时采集生产数据，结合优化算法和规则引擎，系统能够优化资源（退火炉、物流设备）利用，确保工艺稳定执行，提高生产效率和产品质量，并为管理决策提供准确的数据支持。

### 2.2. 系统目标

*   **全程可视化追溯:** 实现铝箔料框/料垛从进入退火车间到离开的全生命周期（状态、位置、工艺、质量）信息透明化与可追溯。
*   **计划精准执行:** 将ERP生产订单转化为可执行的退火任务和物流任务，通过智能排程优化退火炉和物流设备的利用，减少等待和空闲时间。
*   **工艺合规保证:** 实时监控退火过程关键参数（特别是温度曲线），与设定的标准工艺进行比对，及时发现偏差并报警，确保退火质量。
*   **物流高效协同:** 实现行车和自动料车（含与备料台交互）的自动化任务调度与路径优化（若控制系统支持），提升车间内部物流效率。
*   **设备状态可视:** 实时监控退火炉、行车、自动料车的运行状态，记录故障信息，为计算OEE和支持维护提供数据。
*   **数据驱动决策:** 提供实时的车间看板和多维度分析报表（生产、质量、设备），为生产管理和持续改进提供数据支撑。

### 2.3. 主要特点

*   **基于二维码的物料追踪:** 以料框二维码为载体，实现物料单元的精准识别与追踪。
*   **料框与料垛管理:** 支持料框的独立管理以及基于规则的组垛/拆垛操作，适应车间实际作业模式。
*   **退火炉智能排程:** 基于规则和优化算法，综合考虑设备、工艺、物料、时间等约束，生成优化的炉次计划。
*   **实时工艺监控与报警:** 图形化展示实际温度曲线与设定曲线的对比，关键参数超限自动报警。
*   **自动化物流调度:** 集成行车和自动料车控制系统，实现物流任务的自动下发与状态跟踪。
*   **与自动化设备深度集成:** 通过OPC UA等标准协议与PLC、扫码器、AGV系统等无缝连接。
*   **模块化与可扩展性:** 系统采用模块化设计，便于未来功能扩展或与其他系统集成。
*   **友好的用户界面:** 针对车间操作环境优化设计，提供PC端和移动端（PDA）界面。

### 2.4. 系统架构

系统采用分层模块化架构，主要包括用户界面层、业务逻辑层、数据访问层和集成接口层。

```svg
digraph MES_Architecture_V2 {
  rankdir=TB;
  node [shape=box, style=rounded, fontname="Microsoft YaHei", fontsize=10];
  edge [fontsize=9];

  subgraph cluster_UI {
    label="用户界面层 (User Interface Layer)";
    bgcolor="lightblue";
    PC_Client [label="PC Web端 (管理/监控/报表)"];
    PDA_Client [label="手持终端App (操作/扫描)"];
    Dashboard [label="车间看板 (大屏显示)"];
  }

  subgraph cluster_Business {
    label="业务逻辑层 (Business Logic Layer)";
    bgcolor="lightcyan";
    MDM_Service [label="主数据管理服务 (MDM)"];
    INV_Service [label="库存管理服务 (INV)"];
    PROD_Service [label="生产任务服务 (PROD)"];
    EXEC_Service [label="执行与监控服务 (EXEC)"];
    REPORT_Service [label="报表与看板服务 (REPORT)"];
    USER_Service [label="用户权限服务 (USER)"];
    SCHED_Engine [label="排程引擎"];
    LOGI_Dispatch [label="物流调度模块"];

    PROD_Service -> SCHED_Engine [label="调用排程"];
    SCHED_Engine -> PROD_Service [label="返回计划"];
    PROD_Service -> LOGI_Dispatch [label="生成物流任务"];
    LOGI_Dispatch -> EXEC_Service [label="下发任务"];
  }

  subgraph cluster_Data {
    label="数据访问与存储层 (Data Access & Storage Layer)";
    bgcolor="lightgrey";
    DB [label="关系型数据库 (主数据, 事务数据, 状态) \n (如 PostgreSQL, SQL Server)"];
    TSDB [label="时序数据库 (可选, 工艺过程数据)\n (如 InfluxDB, TimescaleDB)"];
    Cache [label="缓存 (如 Redis)\n (提高查询性能)"];
  }

  subgraph cluster_Integration {
    label="集成接口层 (Integration Layer)";
    bgcolor="lightyellow";
    ERP_Adapter [label="ERP接口适配器"];
    QMS_Adapter [label="QMS接口适配器"];
    WMS_Adapter [label="WMS接口适配器"];
    OPCUA_Client [label="OPC UA客户端 (连接PLC)"];
    Device_Adapter [label="设备接口适配器 (行车, AGV, 扫描器, 仪表)"];
  }

  subgraph cluster_External_Systems {
      label = "外部系统与设备";
      style = dashed;
      ERP [label="ERP系统"];
      QMS [label="QMS系统"];
      WMS [label="WMS系统"];
      PLC [label="退火炉 PLC (S7-1200)"];
      Crane_Ctrl [label="行车控制系统"];
      AGV_Ctrl [label="自动料车控制系统"];
      Scanner [label="二维码扫描器 (PDA/固定式)"];
      Instrument [label="电子秤/检验仪器"];
  }

  // Connections
  PC_Client -> {MDM_Service, INV_Service, PROD_Service, EXEC_Service, REPORT_Service, USER_Service};
  PDA_Client -> {INV_Service, EXEC_Service, USER_Service};
  Dashboard -> REPORT_Service;

  {MDM_Service, INV_Service, PROD_Service, EXEC_Service, REPORT_Service, USER_Service} -> DB;
  EXEC_Service -> TSDB [style=dashed, label="可选"];
  {MDM_Service, INV_Service, PROD_Service, EXEC_Service, REPORT_Service} -> Cache [style=dashed];

  MDM_Service -> DB;
  INV_Service -> DB;
  PROD_Service -> DB;
  EXEC_Service -> DB;
  REPORT_Service -> DB;
  USER_Service -> DB;

  ERP_Adapter -> ERP [dir=both];
  QMS_Adapter -> QMS [dir=both];
  WMS_Adapter -> WMS [dir=both];
  OPCUA_Client -> PLC [dir=both];
  Device_Adapter -> {Crane_Ctrl, AGV_Ctrl, Scanner, Instrument} [dir=both];

  ERP_Adapter -> PROD_Service;
  PROD_Service -> ERP_Adapter; // Feedback实绩
  QMS_Adapter -> {MDM_Service, EXEC_Service}; // 同步标准, 反馈结果
  EXEC_Service -> QMS_Adapter;
  WMS_Adapter -> INV_Service [dir=both]; // 库存同步/调拨
  OPCUA_Client -> EXEC_Service [label="实时数据"];
  EXEC_Service -> OPCUA_Client [label="指令/配方"];
  Device_Adapter -> LOGI_Dispatch [label="物流状态"];
  LOGI_Dispatch -> Device_Adapter [label="物流指令"];
  Device_Adapter -> EXEC_Service [label="扫码/称重数据"];

}
```

### 2.5. 核心业务实体

*   **料框 (Bin):** 系统中物料追踪的基本物理单元。
    *   关键属性: 料框ID (唯一, 来自二维码), 料框规格代码 (关联规格), 当前状态 (如待退火, 退火中), 当前位置 (库位ID/设备ID), 关联产品代码, 关联批次号, 重量, 关联料垛ID (若在垛中), 注册时间, 最后更新时间。
*   **料垛 (Stack):** 由多个料框组成的逻辑和物理单元。
    *   关键属性: 料垛ID (唯一, 可选), 包含料框ID列表, 料垛规格代码, 料垛状态 (同步成员料框状态), 当前位置, 总重量, 组垛时间, 拆垛时间 (若已拆)。
*   **退火炉 (Annealing Furnace):** 核心生产设备。
    *   关键属性: 设备ID (唯一), 设备名称, 型号, 容量 (如40T), 当前状态 (空闲, 运行中, 故障, 维护), 当前炉次号, 关联退火任务ID, 实时工艺参数 (温度数组, 压力, 气氛流量等), 通讯接口地址 (PLC地址)。
*   **行车 (Overhead Crane):** 物流设备。
    *   关键属性: 设备ID (唯一), 名称, 型号, 额定起重量, 当前状态 (空闲, 作业中, 故障), 当前位置 (坐标或区域), 载荷状态 (空载/负载), 当前执行任务ID, 通讯接口地址。
*   **自动料车 (AGV/RGV):** 物流设备。
    *   关键属性: 设备ID (唯一), 名称, 型号, 载重能力, 当前状态 (空闲, 作业中, 充电中, 故障), 当前位置, 电池电量, 当前执行任务ID, 通讯接口地址。
*   **库位 (Storage Location):** 存储位置。
    *   关键属性: 库位ID (唯一), 所属库区 (待退火区/已退火区/缓存区/质检区), 库位类型 (地面/货架), 尺寸限制, 承重限制, 适用料框规格, 占用状态 (空闲/占用/预留/禁用), 占用物料ID (料框/料垛)。
*   **退火任务 (Annealing Task):** 生产执行指令。
    *   关键属性: 任务ID (唯一), 关联生产订单号, 计划执行炉号, 待退火料框/料垛ID列表, 关联产品代码, 关联工艺参数模板ID, 计划开始/结束时间, 实际开始/结束时间, 任务状态 (待排程, 已排程, 执行中, 完成, 异常), 优先级。
*   **物流任务 (Logistics Task):** 物料搬运指令。
    *   关键属性: 任务ID (唯一), 任务类型 (入库/出库/转运/装炉/出炉), 物料ID (料框/料垛), 起始位置ID, 目标位置ID, 计划执行设备类型 (行车/料车), 指派设备ID (若已分配), 优先级, 任务状态 (待分配, 已分配, 执行中, 完成, 失败)。

## 3. 功能需求

本章节详细描述MES系统的各项功能需求，按模块划分。

### 3.1. 主数据管理模块 (MDM)

**核心功能:** 统一管理系统运行所需的基础静态数据，确保数据一致性、准确性，为其他模块提供数据支持。

#### 3.1.1. 料框规格管理 (Bin Specification Management)

*   **功能描述:** 维护车间使用的各种料框的基础信息。
*   **实现逻辑:**
    *   提供数据维护界面 (CRUD - 创建, 读取, 更新, 禁用)。
    *   记录字段: `规格代码` (唯一, 如 LK001), `规格名称` (如 标准料框), `尺寸` (长/宽/高, 单位cm), `最大载重` (kg), `材质`, `适用产品类型` (可多选, 关联产品主数据), `最大堆叠层数` (整数), `供应商信息` (可选), `状态` (启用/禁用)。
    *   **校验规则:** 规格代码全局唯一；尺寸、载重、堆叠层数必须为正数；逻辑删除，已在用的规格（有对应料框实例存在）不能物理删除，只能设为“禁用”。
*   **操作流程:**
    1.  管理员登录系统，导航至“主数据 -> 料框规格”。
    2.  查看规格列表，可按代码、名称等搜索或排序。
    3.  点击“新增”，填写规格详情，选择适用产品类型，保存。
    4.  选择已有规格，点击“编辑”，修改信息后保存。
    5.  选择不再使用的规格，点击“禁用”。
*   **数据需求:** 输入/输出上述记录字段。
*   **界面交互要点:** 列表使用表格展示，支持筛选和排序；编辑表单布局清晰，必填项明确；适用产品类型使用多选控件（如穿梭框）。
*   **关键业务规则:** 只有“启用”状态的规格才能在后续料框注册时被引用。

#### 3.1.2. 铝箔产品管理 (Aluminum Foil Product Management)

*   **功能描述:** 管理车间生产或处理的铝箔产品的基础信息。
*   **实现逻辑:**
    *   提供数据维护界面 (CRUD)。
    *   记录字段: `产品编码` (唯一, 如 AF-1100-H18-0.006x1200), `产品名称` (如 1100合金H18态双零箔), `原材料类型`, `合金牌号` (如 1100, 8011), `状态/硬度` (如 H18, O), `规格-厚度` (mm), `规格-宽度` (mm), `单位重量` (kg/卷 或 kg/m²), `关联标准退火工艺模板ID` (引用`3.1.6`, 可多个备选), `关联质量标准ID` (引用`3.1.7`), `产品生命周期状态` (试产/量产/停产)。
    *   **校验规则:** 产品编码全局唯一；厚度、宽度、单位重量为正数；必须关联至少一个“生效”状态的工艺模板和质量标准。
*   **操作流程:**
    1.  管理员登录，导航至“主数据 -> 产品管理”。
    2.  查看产品列表，可按牌号、规格、状态筛选。
    3.  点击“新增”，填写产品信息，关联工艺模板和质量标准，保存。
    4.  选择已有产品，编辑信息或更新生命周期状态。
*   **数据需求:** 输入/输出上述记录字段。
*   **界面交互要点:** 产品列表清晰展示关键规格；编辑表单结构化；关联工艺模板和质量标准时提供下拉选择或搜索弹出框。
*   **关键业务规则:** 只有“量产”状态的产品才能用于创建生产计划或退火任务。

#### 3.1.3. 物料编码管理 (Material Code Management)

*   **功能描述:** 定义料框ID的生成规则及二维码内容。
*   **实现逻辑:**
    *   提供配置界面设定料框ID的生成规则。**强制规则：** 系统必须保证生成的料框ID全局唯一。
    *   **规则选项 (示例):**
        *   纯流水号 (如 8位数字，自动递增)。
        *   前缀 + 流水号 (如 `BIN-` + 6位流水号)。
        *   可配置流水号长度、起始值、步长。
    *   **二维码内容定义:** 明确二维码中编码的信息。**推荐:** 仅包含唯一的 `料框ID` 字符串。避免包含易变信息。
    *   **二维码参数配置:** (可选) 配置生成二维码的参数，如尺寸、纠错级别 (建议 M 或 Q)。
    *   **规则版本管理:** (可选) 记录编码规则的变更历史。
*   **操作流程:**
    1.  系统管理员进入“系统设置 -> 编码规则”。
    2.  选择“料框编码规则”。
    3.  配置生成规则参数。
    4.  定义二维码内容（通常固定为料框ID）。
    5.  保存规则。
*   **数据需求:** 输入/输出编码规则配置参数。
*   **界面交互要点:** 提供规则预览功能。
*   **关键业务规则:** 料框ID由系统根据配置的规则在注册时**自动生成**并分配。规则一旦启用并生成数据，不应随意修改，以保证历史数据一致性。

#### 3.1.4. 库位主数据管理 (Storage Location Master Data Management)

*   **功能描述:** 定义和管理车间内所有物理存储位置的信息。
*   **实现逻辑:**
    *   提供界面定义**库区** (如 `DA`-待退火区, `YA`-已退火区, `HC`-缓存区, `ZJ`-质检区, `BLT`-备料台区域) 和**库位**。
    *   记录字段 (库位): `库位ID` (唯一, 如 DA-01-01-A), `所属库区代码`, `库位类型` (地面堆垛区/固定货架/备料台工位/炉口虚拟位), `坐标` (X, Y, Z, 可选，用于可视化和路径计算), `尺寸限制` (长/宽/高, cm, 可选), `承重限制` (kg), `适用料框规格代码` (可多选或不限), `占用状态` (空闲/占用/预留/禁用/维护), `最大堆叠高度` (仅地面库位, 如3层)。
    *   **批量生成:** 支持按规则 (如 `库区代码-行号-列号-层号`) 批量创建库位。
    *   **可视化配置:** 提供工具将库位数据与上传的车间布局图进行关联映射，支持拖拽放置和属性设置。
    *   **校验规则:** 库位ID全局唯一；承重限制为正数。
*   **操作流程:**
    1.  管理员进入“主数据 -> 库位管理”。
    2.  先定义库区。
    3.  选择库区，使用“批量生成”或“单个新增”创建库位，填写属性。
    4.  (可选) 在“库位地图配置”界面，将库位拖拽到地图上的对应位置。
    5.  保存。
*   **数据需求:** 输入/输出库区和库位记录字段。
*   **界面交互要点:** 库位列表支持按库区、状态筛选；提供可视化地图编辑器；批量生成工具提供预览。
*   **关键业务规则:** 只有`状态`为“空闲”且`类型/规格/承重`匹配的库位才能被`3.2.4 库位分配优化`算法选中。`占用状态`需实时更新。

#### 3.1.5. 设备主数据管理 (Equipment Master Data Management)

*   **功能描述:** 管理车间内关键生产及物流设备的基础档案。
*   **实现逻辑:**
    *   区分设备类型进行管理 (退火炉, 行车, 自动料车, 备料台)。
    *   **退火炉档案:** `设备ID` (唯一), `名称`, `型号`, `规格` (额定容量如40T, 炉膛尺寸), `最大工作温度`, `额定功率`, `安装日期`, `维护周期建议`, `关联PLC通讯地址/节点ID` (关键!)。
    *   **行车档案:** `设备ID` (唯一), `名称`, `型号`, `额定起重量`, `运行速度范围`, `服务范围` (坐标限制或区域列表), `关联控制系统接口地址/参数` (关键!)。
    *   **自动料车档案:** `设备ID` (唯一), `名称`, `型号`, `载重能力`, `运行速度`, `导航方式` (如激光/磁导), `电池容量`, `低电量阈值`, `充电策略`, `关联控制系统接口地址/参数` (关键!)。
    *   **备料台档案:** `设备ID` (唯一), `名称/编号`, `位置描述/坐标`, `承重能力`, `可存放料框/料垛数`, `关联退火炉` (可选)。
    *   提供CRUD界面。
    *   **校验规则:** 设备ID全局唯一。通讯接口参数必须按要求填写。
*   **操作流程:**
    1.  管理员进入“主数据 -> 设备管理”。
    2.  选择设备类型，查看列表。
    3.  新增或编辑设备，填写详细信息，特别是通讯接口参数。
    4.  保存。
*   **数据需求:** 输入/输出各类型设备的记录字段。
*   **界面交互要点:** 按设备类型分类显示和管理。表单根据不同设备类型显示不同的特定参数字段。
*   **关键业务规则:** 设备主数据是排程、任务分配、状态监控和接口通讯的基础。通讯参数的准确性至关重要。

#### 3.1.6. 工艺参数管理 (Process Parameter Management)

*   **功能描述:** 集中管理铝箔退火的标准工艺配方模板。
*   **实现逻辑:**
    *   建立工艺模板库，支持版本控制。
    *   记录字段 (工艺模板): `模板ID` (唯一), `模板名称`, `版本号`, `状态` (草稿/待审批/生效/历史), `创建/修改人/时间`, `审批记录`, `适用产品范围` (关联`3.1.2`产品编码/牌号/规格范围)。
    *   **工艺参数详情:**
        *   **温度曲线:** 定义为多段（段数可变）。每段包含: `段序号`, `段类型` (升温/保温/降温/快速冷却), `目标温度` (°C), `持续时间` (分钟/小时), `升/降温速率` (°C/小时, 升降温段需要)。 **需提供可视化曲线编辑器/查看器。**
        *   **保护气氛:** `气氛类型` (如 纯氮气, 氢氮混合气), `流量设定值` (m³/h, 可为范围), `压力设定值` (Pa/mbar, 可选)。
        *   **循环风机:** `频率设定值` (Hz, 可为范围或分段设定)。
        *   其他特定参数 (根据炉型添加)。
    *   **版本与审批:** 支持创建新版本进行修改。修改后提交审批（审批流程可配置，如一级审批、二级审批）。只有“生效”状态的模板才能被生产任务引用。
    *   **校验规则:** 模板ID+版本号唯一。温度、时间、速率等参数必须在合理范围及设备能力内。
*   **操作流程:**
    1.  工艺工程师登录，进入“主数据 -> 工艺管理”。
    2.  创建新模板或基于现有模板创建新版本。
    3.  定义适用产品范围。
    4.  使用温度曲线编辑器设置各阶段参数。
    5.  录入气氛、风机等其他参数。
    6.  保存为“草稿”或“提交审批”。
    7.  审批人在系统中收到通知，审核通过则状态变为“生效”，驳回则退回修改。
*   **数据需求:** 输入/输出工艺模板及详细参数字段。
*   **界面交互要点:** 可视化温度曲线编辑器/查看器是核心。模板列表按状态、产品筛选。提供版本历史查看与对比功能。审批流程界面清晰。
*   **关键业务规则:** 生产任务必须关联一个“生效”状态的工艺模板。工艺参数修改必须遵循版本和审批管理。

#### 3.1.7. 质量标准配置 (Quality Standard Configuration)

*   **功能描述:** 配置不同产品的质量检验标准和判定规则。
*   **实现逻辑:**
    *   首先定义**检验项目库**: `项目代码`, `项目名称` (如 硬度, 抗拉强度, 延伸率, 表面质量), `单位`, `检测方法描述`, `数据类型` (数值/布尔/文本)。
    *   然后配置**产品质量标准**: 关联`产品编码` (来自`3.1.2`)，为该产品选择需要检验的`检验项目代码`，并设定`标准值` (如具体数值、上限、下限、范围)、`合格判定规则` (如 `结果 >= 下限 AND 结果 <= 上限` 则合格)。
    *   **抽检规则 (可选):** 配置抽检比例或首/中/末件检验规则。
    *   支持质量标准的版本管理（类似工艺参数）。
    *   **校验规则:** 检验项目代码唯一。数值标准的上限必须大于等于下限。
*   **操作流程:**
    1.  质量工程师登录，进入“主数据 -> 质量标准”。
    2.  维护检验项目库。
    3.  选择目标产品（或产品类别），创建或编辑其质量标准。
    4.  从项目库中添加检验项目，设定标准值和判定规则。
    5.  配置抽检规则（若需要）。
    6.  保存标准（可能需要审批流程）。
*   **数据需求:** 输入/输出检验项目、产品质量标准配置记录。
*   **界面交互要点:** 检验项目库管理界面。产品-质量标准关联配置界面（可用表格或矩阵）。
*   **关键业务规则:** `3.4.6 质量检验执行`模块将依据此处配置的“生效”状态的标准进行检验和自动判定。

### 3.2. 库存管理模块 (INV)

**核心功能:** 实时管理和追踪退火车间内所有料框/料垛的状态、位置和属性。

#### 3.2.1. 料框/料垛管理 (Bin/Stack Management)

*   **功能描述:** 实现料框/料垛从入车间登记、组垛/拆垛到状态变更的全生命周期管理。
*   **实现逻辑:**
    *   **料框注册 (Bin Registration):**
        1.  **场景:** 通常在物料从上游工序（如分切）转运到退火车间入口时执行。
        2.  **操作:** 操作员使用手持终端 (PDA) 扫描料框上的二维码。
        3.  **系统处理:**
            *   解析二维码获取 `料框ID`。
            *   **自动生成ID:** 如果是新料框首次入系统，根据`3.1.3`规则生成唯一ID并提示操作员贴标（或者假设标签已预先贴好，系统验证ID格式和唯一性）。如果ID已存在但状态允许（如 `空闲`），则继续。若ID已存在且状态不对，报错。
            *   **关联规格:** 根据ID规则或查询主数据，自动关联并显示`料框规格`信息 (来自`3.1.1`)。
            *   **录入信息:** 操作员需录入/确认 `铝箔产品代码` (来自`3.1.2`)、`批次号`、`重量` (kg)。
            *   **校验:** 重量 > 0 且 <= 料框最大载重。产品代码有效。
            *   **记录:** 自动记录操作员、时间、初始位置（可默认为入口虚拟位或由操作员选择）。
            *   **状态设置:** 将料框状态设为初始状态，如 `待入库(退火)`。
            *   **保存与反馈:** 保存料框数据，PDA提示“料框 [ID] 注册成功”。
    *   **料框状态跟踪 (Bin Status Tracking):**
        *   建立料框状态机模型，明确定义所有可能的状态及转换条件。示例状态流：
            `空闲` → `待入库(退火)` → `待退火` → `待备料` → `待装炉(备料台/料车)` → `装炉中` → `退火中` → `待出炉` → `已退火` → `待检验` → `已检验(合格/不合格)` → `待出库(包装)` → `已出库` → (若料框循环使用) → `空闲`。
        *   状态转换触发条件包括：扫码确认（入库、出入炉、检验）、物流任务完成（移动到特定区域）、PLC信号（退火开始/结束）。
        *   每次状态转换必须记录时间戳、操作员/系统事件、前后状态、位置变更（若有）。非法状态转换必须被阻止。
    *   **料垛管理 (Stack Management):**
        *   **组垛 (Stacking):**
            1.  **场景:** 在待退火区，将符合条件的料框堆叠起来。
            2.  **规则:** 待组垛的料框必须具有相同的`料框规格代码`、`铝箔产品代码`、`批次号`（此项根据业务需求确认是否强制）、并且处于**相同的状态**（如都是 `待退火`）。堆叠层数不得超过该规格的`最大堆叠层数`。
            3.  **操作:** 操作员在PDA选择“组垛”，（可选）扫描一个预制的`料垛ID`标签，然后逐一扫描要组成此垛的料框二维码（从下往上）。
            4.  **系统处理:** 实时校验每个料框是否符合组垛规则。显示已添加列表和当前层数。全部添加后，操作员确认。系统保存料垛记录（`料垛ID`、成员`料框ID列表`、总重、状态、位置），并更新所有成员料框的`关联料垛ID`字段。
        *   **拆垛 (Destacking):**
            1.  **场景:** 出炉后、检验前或转运到包装前。
            2.  **操作:** 操作员扫描`料垛ID`（或垛上任一料框），选择“拆垛”。
            3.  **系统处理:** 解除料垛记录与成员料框的关联（料垛记录可标记为“已拆垛”），成员料框恢复独立状态，其`关联料垛ID`字段清空。
        *   料垛作为一个整体进行位置追踪、库位分配、物流任务调度和炉次管理中的装出炉操作。扫描料垛ID等同于操作其包含的所有料框。
*   **操作流程:** 详细描述料框注册、状态变更（如扫码确认出炉）、组垛、拆垛的具体步骤。
*   **数据需求:** 料框表、料垛表、状态变更历史表。
*   **界面交互要点:** PDA界面设计需适配工业环境，操作步骤清晰引导，扫码反馈及时。组垛时实时校验并显示结果。
*   **关键业务规则:** 料框ID唯一性；状态转换严格按流程；组垛规则严格执行；料垛操作影响其所有成员料框。

#### 3.2.2. 库存状态管理 (Inventory Status Management)

*   **功能描述:** 提供车间实时库存（料框/料垛）信息的查询、统计和可视化。
*   **实现逻辑:**
    *   **实时库存查询:** 提供多条件组合查询功能：库区、库位、料框/料垛ID、产品代码/牌号/规格、批次号、状态、库龄范围等。结果以列表形式展示，包含关键信息。
    *   **库存可视化:** 在车间电子地图 (基于`3.1.4`配置) 上实时展示库位占用情况。使用不同颜色区分库位状态（空闲-绿，占用-蓝，预留-黄，禁用-红）。鼠标悬停或点击库位可显示详细信息（库位ID、存放物料ID、产品、状态、入库时间）。
    *   **库存统计分析:** 按需生成统计报表/图表：
        *   按库区/状态统计料框/料垛数量和总重量。
        *   按产品牌号/规格统计库存分布。
        *   计算平均库龄、库存周转率（需定义计算周期和方法）。
    *   **库存预警:** 配置预警规则，例如：
        *   `待退火区` 占用率 > 90%。
        *   料框/料垛在 `待退火` 状态停留超过 48 小时。
        *   `已检验(不合格)` 料框/料垛数量 > 10 个。
        *   触发预警时，系统在看板、消息中心或通过邮件/短信（需配置）通知相关人员。
*   **操作流程:** 用户通过PC Web端访问库存查询、统计或可视化地图界面，设置条件或浏览。预警由系统后台自动监控触发。
*   **数据需求:** 实时查询库存相关表，预警规则配置表。
*   **界面交互要点:** 查询条件组合灵活；结果列表支持排序、导出；地图支持缩放、平移、信息点选；统计图表类型多样（饼图、柱状图等）；预警信息醒目。
*   **关键业务规则:** 库存数据必须实时准确反映物料状态和位置。预警规则需灵活可配。

#### 3.2.3. 物料位置追踪 (Material Location Tracking)

*   **功能描述:** 实时追踪并记录每个料框/料垛在车间内的物理位置及其移动轨迹。
*   **实现逻辑:**
    *   **位置更新源:**
        *   **自动化设备:** 行车/自动料车完成`物流任务`后，通过`4.2.2`接口上报任务完成状态，包含物料ID和最终位置ID（库位、备料台、炉口）。
        *   **PLC:** 退火炉装炉/出炉完成时，通过`4.2.1`接口确认物料进入/离开炉膛（虚拟位置）。
        *   **人工扫描确认:** 在关键节点（如入库口、质检台、出库口，或自动化程度不高的转运点），操作员使用PDA扫描`料框/料垛ID`和`目标位置ID`（库位二维码或设备标签）进行确认。
    *   **位置变更记录:** 每次位置变化，系统记录：`物料ID` (料框/料垛), `时间戳`, `旧位置ID`, `新位置ID`, `触发类型` (物流任务完成/PLC信号/人工扫描), `操作设备ID` (若适用), `操作员ID` (若适用)。
    *   **位置查询:** 提供功能，输入`料框/料垛ID`可查询其当前所在位置。
    *   **历史轨迹查询:** 提供功能，输入`料框/料垛ID`可查询其在指定时间范围内的所有位置变更记录，并可在地图上可视化展示移动路径。
*   **操作流程:** 位置更新主要由系统自动处理（接口触发）或操作员按需扫描。查询功能由用户在PC或PDA端发起。
*   **数据需求:** 物料位置变更历史表。
*   **界面交互要点:** 当前位置信息集成在库存查询结果中。历史轨迹查询结果清晰，地图可视化直观。
*   **关键业务规则:** 位置信息必须与物料状态同步更新。确保位置数据来源的准确性和及时性。

#### 3.2.4. 库位分配优化 (Storage Location Assignment Optimization)

*   **功能描述:** 在物料入库或工序间转运需要入库位时，智能推荐或自动分配最佳库位。
*   **实现逻辑:**
    *   **触发场景:** 料框注册后需入库、退火/检验完成后需入库、物料在车间内转运需暂存。
    *   **候选库位筛选:**
        1.  根据待入库物料的`料框规格`、`产品类型`（可选）、`状态`（如待退火/已退火），筛选出`3.1.4`中`适用规格`匹配、`所属库区`合适（如待退火物料只能去待退火区）、`占用状态`为“空闲”的所有库位。
        2.  同时需满足`承重限制`和`尺寸限制`（若配置）。
    *   **优化策略与评分 (示例，需可配置):**
        *   为每个候选库位计算一个综合得分 (Score)，选择得分最高的N个推荐或直接分配最高分。
        *   **评分因子 (示例):**
            *   `就近原则 (Proximity)`: 计算库位到该物料下一个典型目的地（如待退火物料->对应炉型备料台/炉口；已退火物料->质检区/出库口）的物理距离或路径成本（需库位坐标和路径信息），距离越近得分越高。权重 `w_prox`。
            *   `先进先出 (FIFO)`: 考虑库位空闲时长，空闲时间越长的库位得分越高（鼓励利用旧空位，防止死角）。权重 `w_fifo`。
            *   `同类聚集 (Affinity)`: 检查库位附近（如同一排/相邻几位）是否已存放相同`产品代码`或`批次号`的物料，若有则得分较高（便于管理和查找）。权重 `w_affin`。
            *   `区域填满度 (Utilization)`: 优先填满某个区域或巷道，或考虑是否有利于后续操作（如靠近主通道），给予评分。权重 `w_util`。
            *   `堆叠潜力 (Stacking)`: 如果是地面库位且允许堆叠，检查其上方库位是否空闲。若空闲且待入库物料满足堆叠条件（通常同规格），则给予加分。权重 `w_stack`。
        *   `Score = w_prox * Score_prox + w_fifo * Score_fifo + ...` (权重可配置)。
    *   **分配模式:**
        *   **推荐模式:** 系统显示得分最高的N个库位列表（含得分和推荐理由），由操作员最终选择确认。
        *   **自动模式:** 系统自动选择最高分库位，将其状态置为“预留”，并直接触发`3.3.4 物流任务生成`，将物料运往该库位。
*   **操作流程:**
    1.  系统（或操作员）触发库位分配请求，传入待分配物料信息。
    2.  系统执行筛选和评分算法。
    3.  根据配置模式，显示推荐列表或直接执行分配。
    4.  若为推荐模式，操作员确认选择；若为自动模式，系统自动完成。
    5.  库位状态更新为“预留”或“占用”，物流任务生成。
*   **数据需求:** 实时库位状态、库位主数据、物料信息、优化策略配置、库位坐标和距离数据（可选）。
*   **界面交互要点:** 推荐列表清晰展示库位ID、得分、理由。可在地图上高亮显示推荐库位。
*   **关键业务规则:** 分配的库位必须满足物理和业务规则约束。优化策略应可配置以适应不同场景。自动分配模式下需确保高可靠性。

### 3.3. 生产任务管理模块 (PROD)

**核心功能:** 管理生产计划的接收、分解、排程和调度，确保生产活动有序、高效进行。

#### 3.3.1. 生产计划管理 (Production Planning Management)

*   **功能描述:** 接收来自ERP的生产订单或手工创建内部生产计划，进行管理和跟踪。
*   **实现逻辑:**
    *   **订单接收:** 通过`4.1.1 ERP接口`，定时（如每小时）或由ERP触发，接收生产订单信息。关键字段：`ERP订单号`, `产品编码`, `需求数量` (吨或卷), `计划交期`, `客户信息` (可选), `特殊要求` (文本)。系统将接收到的订单转化为内部生产计划。
    *   **手工创建:** 支持计划员在MES界面直接创建生产计划，录入类似信息。
    *   **计划管理:** 提供界面展示生产计划列表（包含来源、状态、进度等）。支持对计划进行：
        *   `拆分`: 将一个大计划拆分成多个小批次计划。
        *   `合并`: 将多个小计划合并（如相同产品）。
        *   `调整`: 修改计划数量、交期、优先级。
        *   `状态更新`: 计划状态可包括：`已接收`, `已确认`, `部分下达`, `已下达`, `执行中`, `完成`, `取消`。
    *   **初步评估:** (可选) 基于`产品编码`关联的`工艺模板`中的大致炉次时间 和 `退火炉`的`容量`，粗略估算所需炉次数和时间，与交期对比，给出预警。
*   **操作流程:**
    1.  计划员查看从ERP同步或手工创建的计划列表。
    2.  对`已接收`状态的计划进行审核，可进行拆分/合并/调整操作。
    3.  确认无误后，将计划状态改为`已确认`或`已下达`，使其进入待分解或待排程状态。
    4.  系统根据后续任务执行情况，自动更新计划的进度和最终状态。
*   **数据需求:** 生产计划表，记录上述字段及状态、创建/更新信息。
*   **界面交互要点:** 计划列表信息全面，状态清晰。提供甘特图可视化展示计划时间安排（可选）。调整操作记录变更历史。
*   **关键业务规则:** 从ERP接收的订单号应保持关联，便于后续数据反馈。计划调整需有权限控制和记录。

#### 3.3.2. 退火任务管理 (Annealing Task Management)

*   **功能描述:** 将生产计划分解为具体的、可分配给退火炉执行的退火任务。
*   **实现逻辑:**
    *   **任务创建:**
        *   **方式一 (基于计划分解):** 计划员选择一个`已确认`的生产计划，系统根据计划数量、产品对应的标准料框装载量、退火炉容量（约40T），自动建议需要分解成的`退火任务`数量和每个任务大致的物料量。计划员确认或调整。
        *   **方式二 (手动创建):** 计划员直接创建退火任务，手动指定关联的`生产计划`（可选）、`产品编码`等。
    *   **物料关联:** 任务创建后，需要关联具体的待执行物料。
        *   系统可根据任务指定的`产品编码`和`批次号`（若有），从库存中筛选出状态为`待退火`的料框/料垛列表，供计划员选择关联。
        *   关联时，系统需校验所选物料的总重量是否大致符合炉容（如 35-42T 范围），并提示。
    *   **工艺分配:** 系统根据关联物料的`产品编码`，自动从`3.1.6`查找并加载“生效”状态的`标准退火工艺模板ID`，计划员可预览并确认。
    *   **任务记录字段:** `任务ID` (唯一), `关联生产计划ID`, `计划执行炉号` (排程后指定), `待退火料框ID列表`, `待退火料垛ID列表`, `产品编码`, `预计总重量`, `关联工艺模板ID`, `计划装炉时间`, `计划出炉时间`, `实际装/出炉时间`, `任务优先级` (可继承计划), `任务状态`。
    *   **任务状态机 (示例):** `草稿` → (保存) → `待排程` → (排程算法分配炉号和时间) → `已排程` → (关联物料到达备料区/炉口) → `待装炉` → (执行装炉操作) → `装炉中` → (装炉完成确认) → `待执行` → (PLC反馈退火开始) → `执行中` → (PLC反馈退火完成) → `待出炉` → (执行出炉操作完成) → `已完成`。 异常状态：`挂起`, `取消`, `异常终止`。
*   **操作流程:**
    1.  计划员/调度员创建退火任务（通过计划分解或手动）。
    2.  选择并关联具体的待退火料框/料垛。
    3.  确认系统自动匹配的工艺模板。
    4.  保存任务，状态变为`待排程`。
    5.  后续状态由排程模块、执行模块自动更新或操作员确认驱动。
*   **数据需求:** 退火任务表，任务-物料关联表。
*   **界面交互要点:** 任务列表清晰展示关键信息和状态。创建/编辑界面提供物料选择助手。工艺模板预览方便。
*   **关键业务规则:** 每个退火炉的一个完整炉次通常对应一个退火任务。关联的物料必须是`待退火`状态且满足任务要求。必须关联有效的工艺模板。

#### 3.3.3. 退火炉排程 (Annealing Furnace Scheduling)

*   **功能描述:** 基于待执行的退火任务、设备资源状况和优化规则，制定详细的炉次排程计划。
*   **实现逻辑:**
    *   **输入数据获取:**
        *   `待排程任务队列`: 获取所有状态为`待排程`的退火任务 (`3.3.2`)，包含其优先级、关联物料、工艺要求、期望完成时间（来自计划交期）。
        *   `设备资源状态`: 实时获取所有退火炉的当前状态 (`空闲`, `运行中` - 含预计完成时间, `故障`, `维护计划`) (`3.4.5`)。
        *   `物料准备状态`: 获取任务关联物料的当前位置和状态 (`3.2.1`, `3.2.3`)。
        *   `配置规则`: 排程算法所需的约束和优化目标配置。
    *   **约束条件检查:**
        *   `炉子容量`: 任务总重量需在炉子容量范围内（如 35-42T）。
        *   `炉子能力`: 任务所需工艺（如最高温度）不能超过炉子能力。
        *   `工艺兼容性 (混炉规则)`: 根据配置规则判断哪些不同产品/牌号/工艺类型的任务可以合并到同一炉次执行（需明确具体规则，如：O态产品可以混装，H态产品需同牌号同炉；或基于温度曲线相似度等）。**这是影响排程效率的关键规则，需仔细定义。**
        *   `物料可用性`: 排程的计划开始时间必须晚于关联物料预计可到达炉口/备料台的时间（需考虑物流时间）。
        *   `设备维护`: 不能将任务排在设备计划维护时间段内。
    *   **排程算法 (核心):**
        *   采用合适的调度算法（如基于规则的启发式算法、约束规划、或模拟退火等优化算法）。
        *   **算法目标 (可配置优先级):**
            1.  **满足交期:** 优先安排交期紧急或优先级高的任务。
            2.  **提高炉子利用率:** 尽量减少炉子空闲时间，考虑连续排产。
            3.  **提高装载率:** 在满足混炉规则前提下，尽量拼凑满炉任务。
            4.  **（可选）降低能耗:** 尽量将工艺相似（如温度曲线接近）的任务连续排，减少大幅升降温。
    *   **排程结果输出:**
        *   为每个`待排程`任务分配合适的`退火炉ID`和`计划装炉/出炉时间`。
        *   对于允许混炉的情况，将多个兼容的任务合并为一个炉次计划。
        *   **可视化展示:** 提供甘特图，以时间轴和炉号为维度，清晰展示每个炉子分配的任务块（包含任务ID、产品、计划时间）。
    *   **手动调整与冲突检测:** 允许调度员在甘特图上通过拖拽方式调整任务的分配（换炉、改时间）。系统需实时检查调整后的合法性（是否违反约束），并高亮显示冲突或潜在延期。
*   **操作流程:**
    1.  调度员进入排程界面，查看待排程任务列表和设备实时状态甘特图。
    2.  配置排程参数（如时间范围、优化目标侧重）。
    3.  运行自动排程算法。
    4.  系统生成排程计划并展示在甘特图上。
    5.  调度员评审排程结果，进行必要的手动调整。
    6.  确认并“发布”排程计划。发布的计划将驱动后续的物流和执行。
*   **数据需求:** 退火任务数据、设备实时状态、维护计划、物料位置/状态、排程规则配置、排程结果数据。
*   **界面交互要点:** 交互式甘特图是核心，支持缩放、平移、拖拽、显示详情、冲突预警。提供不同排程方案模拟对比功能（可选）。
*   **关键业务规则:** 排程必须严格遵守所有硬性约束（容量、能力、兼容性、物料、维护）。混炉规则需要明确定义和配置。发布的排程是后续作业的依据。

#### 3.3.4. 物流任务生成 (Logistics Task Generation)

*   **功能描述:** 根据确定的生产排程计划和库存变动需求，自动生成物料搬运任务，分配给行车或自动料车。
*   **实现逻辑:**
    *   **触发条件:**
        *   `退火炉排程计划`发布或更新时，自动为其涉及的装炉、出炉生成配套的物流任务。
        *   `库位分配`完成后，自动生成入库物流任务。
        *   其他需要物料移动的指令，如从`已退火区`移动到`质检区`，或从`质检区`移动到`合格/不合格品区`。
    *   **任务生成规则:**
        *   **装炉相关:**
            *   `待退火区库位 -> 备料台` (若使用备料台)
            *   `待退火区库位 -> 自动料车` (若直接由料车取料)
            *   `备料台 -> 自动料车`
            *   `自动料车 -> 退火炉炉膛` (需要精确对接)
        *   **出炉相关:**
            *   `退火炉炉膛 -> 自动料车`
            *   `自动料车 -> 已退火区库位`
        *   **其他转运:**
            *   `已退火区库位 -> 质检区库位/工位`
            *   `质检区 -> 合格品区库位` 或 `不合格品区库位`
            *   `合格品区库位 -> 出库口/包装区` (或由WMS触发)
    *   **任务内容:** `任务ID` (唯一), `任务类型` (按上述规则定义), `物料ID` (料框/料垛), `起始位置ID` (库位/设备), `目标位置ID` (库位/设备), `优先级` (可基于关联的生产任务优先级设定), `计划开始时间窗口` (基于排程计划), `要求设备类型` (行车/料车), `指派设备ID` (分配后填写), `任务状态` (待分配/已分配/执行中/完成/失败/取消)。
    *   **任务推送:** 生成的物流任务进入`物流调度模块`的任务池 (`LOGI_Dispatch`)，等待分配给具体设备。
*   **操作流程:**
    1.  系统根据触发条件自动生成物流任务。
    2.  调度员可在“物流任务管理”界面查看所有待处理、执行中、已完成的任务列表。
    3.  （可选）调度员可手动创建或调整物流任务优先级。
*   **数据需求:** 物流任务表。需关联生产排程、物料位置、库位/设备定义。
*   **界面交互要点:** 物流任务列表清晰展示任务详情、状态、优先级、分配设备。可在地图上可视化任务起止点（可选）。
*   **关键业务规则:** 物流任务的生成逻辑必须与车间实际作业流程和设备能力匹配。任务优先级需合理设定以保障生产顺畅。

### 3.4. 执行与监控模块 (EXEC)

**核心功能:** 实时监控生产过程执行，确保按计划和工艺进行，控制设备，并记录实绩数据。

#### 3.4.1. 退火过程监控 (Annealing Process Monitoring)

*   **功能描述:** 实时监控退火炉内工艺参数的执行情况，与设定值对比，记录数据并报警。
*   **实现逻辑:**
    *   **数据采集:** 通过`4.2.1 PLC接口` (OPC UA)，按预定频率（如：温度点1秒/次，压力/流量5秒/次）实时读取退火炉运行数据。数据点需详细定义在PLC接口规范中，至少包括：炉内多个测温点实际温度、炉膛压力、保护气氛瞬时流量、循环风机频率、当前运行阶段标识、加热器状态/功率、报警状态字等。
    *   **过程可视化 (炉次监控界面):**
        *   **实时曲线:** 同一图表内绘制：
            *   `设定温度曲线`: 从当前执行任务关联的`工艺模板` (`3.1.6`) 获取。
            *   `实际温度曲线`: 绘制一个或多个关键测温点的实时数据。
            *   X轴为时间（显示绝对时间和相对炉次开始时间），Y轴为温度。支持缩放、平移、游标显示具体数值。
        *   **关键参数显示:** 使用仪表盘、数字标签等形式实时显示：当前炉压、气氛流量、风机频率，及其设定值（若有）。
        *   **状态信息:** 显示当前炉号、执行中的`退火任务ID`、`产品信息`、`工艺阶段名称` (如升温1段, 保温2段)、本阶段`已执行时间 / 设定总时间`、炉次整体`进度百分比`。
    *   **数据记录:** 将采集到的所有时间序列数据（时间戳, 参数名, 实际值）与`炉次ID` (可通过任务ID关联生成) 绑定，并持久化存储（推荐存入时序数据库`TSDB`，或优化设计的关系数据库表）。
    *   **报警与事件:**
        *   **偏差报警:** 实时比较关键参数（如主控温度）与设定值的偏差。若 `|实际值 - 设定值| > 允许偏差阈值` (如温度±5°C，需可配置) 且持续时间超过 `设定时长` (如60秒，需可配置)，则触发报警。
        *   **超限报警:** 实时比较关键参数（如炉压、气氛流量）是否超出预设的安全或工艺上下限，超出即报警。
        *   **事件记录:** 记录重要事件，如工艺阶段切换、报警发生与恢复、操作员干预（若有接口）。
        *   **报警处理:** 报警触发时，系统记录报警详情（时间、炉号、任务号、参数、值、类型），在监控界面、看板（`3.5.4`）上显著提示（如声音、闪烁、颜色变化、报警列表），并可配置通知机制（如短信、邮件）。
*   **操作流程:** 操作员或中控室人员通过PC Web端访问“退火监控”界面，选择或概览正在运行的炉子，实时查看曲线、参数和状态。报警发生时按应急预案处理，并在系统中确认报警（若需要）。
*   **数据需求:** PLC实时数据（通过接口获取），工艺模板数据，报警规则配置，过程数据存储表/库，报警/事件记录表。
*   **界面交互要点:** 曲线图清晰易读，多曲线对比明确。关键参数醒目。报警信息突出且易于理解。支持历史曲线回顾查询。
*   **关键业务规则:** 数据采集频率和精度需满足工艺控制要求。过程数据必须完整、准确地与炉次/任务关联。报警规则需灵活可配置且可靠。

#### 3.4.2. 行车作业跟踪 (Crane Operation Tracking)

*   **功能描述:** 调度、下发、跟踪行车的物流任务执行，并监控行车状态。
*   **实现逻辑:**
    *   **任务接收与分配:** 从`物流调度模块` (`LOGI_Dispatch`) 接收到类型为“行车”的`物流任务` (`3.3.4`)。根据任务优先级、任务起止点位置、行车实时位置和状态（空闲/作业中/故障，来自`4.2.2`接口反馈），选择最优的空闲行车进行任务分配。
    *   **指令下发:** 将分配好的任务信息（任务ID、动作类型如PICKUP/DROPOFF、物料ID、目标坐标/库位ID）通过`4.2.2 设备接口`发送给对应的行车控制系统。
    *   **状态跟踪与反馈:** 实时接收行车控制系统通过接口反馈的信息：
        *   `任务状态`: 已接收、执行中、到达取货点、取货完成、到达卸货点、卸货完成、任务成功、任务失败（含原因代码）、任务中断。
        *   `行车状态`: 空闲、手动模式、自动模式-执行任务、故障、急停。
        *   `实时位置`: X, Y, Z 坐标。
        *   `载荷状态`: 空载、负载（可包含重量信息，若传感器支持）。
    *   **系统更新:** 根据反馈信息，实时更新MES中对应的`物流任务状态`和`物料位置` (`3.2.3`)。
    *   **可视化监控:** 在车间电子地图上实时显示：
        *   行车图标及其当前位置。
        *   行车状态（用颜色或图标区分）。
        *   当前执行的任务ID。
        *   （可选）行车计划路径和已完成轨迹。
    *   **异常处理:** 接收到行车故障或任务失败信号时，系统记录日志，更新任务状态为“失败”，触发报警通知调度员进行人工干预（如重新分配任务、处理故障）。
*   **操作流程:** 任务分配通常由系统自动完成。调度员主要通过监控界面查看行车状态和任务执行情况，处理异常。
*   **数据需求:** 物流任务数据，行车实时状态/位置/任务反馈数据（来自接口），行车主数据。
*   **界面交互要点:** 地图可视化直观展示行车动态。任务列表实时更新状态。报警信息清晰。
*   **关键业务规则:** 任务分配需考虑效率和安全性（防碰撞通常由行车控制系统负责，MES需避免逻辑冲突）。MES状态需与行车实际状态保持同步。

#### 3.4.3. 料车作业跟踪 (AGV/RGV) (Automatic Guided Vehicle / Rail Guided Vehicle)

*   **功能描述:** 调度、下发、跟踪自动料车（AGV/RGV）的物流任务执行，监控其状态，并管理充电。
*   **实现逻辑:** (与行车跟踪类似，增加料车特性)
    *   **任务接收与分配:** 从`物流调度模块`接收类型为“料车”的`物流任务`。分配逻辑除考虑位置、状态、优先级外，还需考虑：
        *   `电池电量`: 优先分配电量充足的车辆，避免任务中途缺电。
        *   `载重能力`: 确保车辆载重能力满足任务物料重量。
        *   `路径可用性`: （若AGV系统提供信息）考虑目标路径是否通畅。
    *   **指令下发:** 通过`4.2.2 设备接口`与AGV/RGV控制系统（通常是一个上位调度系统）交互，发送任务指令（任务ID、动作类型、起止点、物料信息）。
    *   **状态跟踪与反馈:** 实时接收AGV/RGV控制系统反馈的信息：任务状态、车辆状态（空闲、执行任务、充电中、故障、避障等待等）、实时位置、`电池电量百分比`。
    *   **系统更新:** 同行车，更新`物流任务状态`和`物料位置`。
    *   **可视化监控:** 在车间地图上显示料车图标、位置、状态、电量、当前任务。
    *   **充电管理:**
        *   监控所有料车的实时电量。
        *   当某料车电量低于预设阈值（如 20%，可配置）且当前无任务或任务即将完成时，系统自动生成一个优先级较低的“前往充电站充电”的物流任务，并分配给该料车。
        *   到达充电站并开始充电后，车辆状态变为“充电中”。充电完成后（由AGV系统反馈），状态恢复为“空闲”可用。
    *   **异常处理:** 同行车，处理故障、任务失败等情况。增加低电量无法执行任务的预警。
*   **操作流程:** 任务分配和充电管理主要由系统自动进行。调度员监控状态，处理异常。
*   **数据需求:** 物流任务数据，料车实时状态/位置/电量/任务反馈数据（来自接口），料车主数据（含电量阈值、充电站位置）。
*   **界面交互要点:** 地图可视化需包含电量信息。提供料车状态列表，包含电量和充电状态。
*   **关键业务规则:** 充电管理策略需合理配置，确保有足够可用车辆。与AGV调度系统的接口需稳定可靠。

#### 3.4.4. 炉次过程管理 (Furnace Batch Process Management)

*   **功能描述:** 指导和确认从装炉、退火到出炉的整个炉次生命周期的关键操作节点。
*   **实现逻辑:**
    *   **装炉管理 (Loading Management):**
        1.  **触发:** 当前时间到达某`退火任务` (`3.3.2`) 的`计划装炉时间`附近，且关联的`退火炉`状态为`空闲`。
        2.  **指导:** 系统在炉前操作终端（PC或PDA）或中控室界面显示“待装炉”提示，包含：炉号、任务ID、计划装炉物料清单（料框/料垛ID、产品、重量）。
        3.  **执行:** 操作员根据指示，通过行车/料车将物料运至炉口。
        4.  **扫描确认:** 操作员使用扫描器，在物料**实际装入炉膛前或装入过程中**逐一扫描料框/料垛ID。
        5.  **校验:** 系统实时校验扫描的ID是否在任务的待装清单内。
            *   `成功`: 在清单内，界面提示成功，标记该物料已扫描。
            *   `失败 (不在清单内)`: 提示“错误物料，非本任务计划内”。
            *   `失败 (重复扫描)`: 提示“该物料已扫描确认”。
        6.  **完成确认:** 当清单内所有物料均扫描确认后，操作员点击界面上的“**确认装炉完成**”按钮。
        7.  **系统记录:** 记录`实际装炉开始时间`（第一次有效扫描时间）、`实际装炉结束时间`（点击完成按钮时间）、`操作员ID`、`实际装炉清单`。更新关联`退火任务`状态为`待执行`（或根据PLC交互逻辑而定）。更新涉及的料框/料垛状态为`装炉中`或`待执行`，位置更新为`炉膛虚拟位`。更新`退火炉`状态为`装载完成`或`待启动`。
        8.  **(可选) 触发PLC:** 若PLC需要MES指令启动退火程序，则在确认装炉完成后，通过`4.2.1接口`发送启动指令。
    *   **退火过程:** 由`3.4.1 退火过程监控`模块负责。任务状态变为`执行中`。
    *   **出炉管理 (Unloading Management):**
        1.  **触发:** 系统接收到PLC通过`4.2.1接口`发送的“退火完成”信号，并且（可选）满足安全出炉条件（如炉温降至某阈值以下）。
        2.  **指导:** 系统在操作终端显示“待出炉”提示，包含炉号、任务ID、应出炉物料清单。
        3.  **执行:** 操作员按规程打开炉门，通过行车/料车将物料从炉膛运出。
        4.  **扫描确认:** 操作员在物料**实际离开炉膛后**（如放置到料车或转运区时）逐一扫描料框/料垛ID。
        5.  **校验:** 系统校验扫描的ID是否为本炉次内的物料。
        6.  **完成确认:** 当清单内所有物料均扫描确认出炉后，操作员点击“**确认出炉完成**”按钮。
        7.  **系统记录:** 记录`实际出炉开始/结束时间`、`操作员ID`。更新关联`退火任务`状态为`已完成`。更新涉及的料框/料垛状态为`已退火`，位置更新为`炉口虚拟位`或指定的出炉暂存位（如料车上）。更新`退火炉`状态为`空闲`。
*   **操作流程:** 操作员严格按照系统界面提示和指导，执行装炉、扫码、确认；出炉、扫码、确认等步骤。
*   **数据需求:** 退火任务数据，物料库存数据，PLC接口信号，操作员确认信息。
*   **界面交互要点:** 炉前操作界面需简洁、醒目。待操作物料清单清晰。扫描反馈（成功/失败/重复）及时明确。确认按钮在满足条件时才可用。
*   **关键业务规则:** 装炉物料必须与任务计划一致（允许配置是否强制，若不强制需记录差异）。出炉必须在工艺完成且满足安全条件下进行。所有关键操作节点（装/出炉开始结束）的时间和操作员必须准确记录。

#### 3.4.5. 设备状态监控 (Equipment Status Monitoring)

*   **功能描述:** 实时监控车间关键设备（退火炉、行车、自动料车）的运行状态、关键参数及异常信息。
*   **实现逻辑:**
    *   **数据采集:**
        *   `退火炉`: 通过`4.2.1 PLC接口`获取运行状态字（如 空闲/运行/故障/维护）、运行模式（自动/手动）、当前炉次号、报警代码等。
        *   `行车`: 通过`4.2.2 接口`从行车控制系统获取状态（空闲/作业中/故障）、模式、载荷、位置、报警信息等。
        *   `自动料车`: 通过`4.2.2 接口`从AGV/RGV控制系统获取状态（空闲/作业中/充电中/故障）、模式、位置、电量、报警信息等。
    *   **状态定义与映射:** 在MES中定义一套标准的设备状态码（如 RUN, IDLE, DOWN, MAINT, SCH_DOWN 等），并将从设备接口获取的状态信息映射到这些标准状态。
    *   **可视化展示:**
        *   **设备列表:** 提供各类型设备的列表，实时显示其`设备ID`、`名称`、`当前状态`（用颜色区分）、`当前任务ID`（若在作业中）、`关键参数`（如炉温、电量）等。
        *   **车间地图:** 在`3.2.2`的地图上，除了库位，也实时显示设备图标及其状态颜色。
        *   **设备详情页:** 点击设备可查看更详细的实时信息、近期状态变化历史、关联任务、报警记录等。
    *   **状态历史记录:** 记录设备每次状态变化的时间戳、旧状态、新状态、持续时长。
    *   **故障与停机管理:**
        *   接收到设备故障代码或状态时，系统自动记录故障事件（时间、设备、代码），并触发报警。
        *   支持维护人员在系统中手动记录故障原因、处理过程、修复时间。
        *   区分计划停机（如计划维护）和非计划停机（故障）。
    *   **OEE (Overall Equipment Effectiveness) 计算 (基础版):**
        *   **数据准备:** 需要设备状态历史（区分运行、空闲、计划停机、非计划停机）、标准生产节拍（或单炉次标准时间）、实际产量、合格品数量。
        *   **计算周期:** 可按班次、日、周、月进行计算。
        *   **计算公式:**
            *   `计划运行时间 = 总日历时间 - 计划停机时间`
            *   `实际运行时间 = 计划运行时间 - 非计划停机时间`
            *   `可用率 (Availability) = 实际运行时间 / 计划运行时间`
            *   `性能率 (Performance) = (总产量 * 标准节拍) / 实际运行时间`  (注：对于炉次生产，可用 `(标准炉次时间 * 完成炉次数) / 实际运行时间` 或 `实际平均炉次时间 / 标准炉次时间` 的倒数)
            *   `质量率 (Quality) = 合格品产量 / 总产量` (产量数据需关联到设备)
            *   `OEE = 可用率 * 性能率 * 质量率`
        *   系统需能按设备、按时间段计算并展示OEE及其三个组成部分。
    *   **维护提醒:** (可选) 根据设备运行累计时间（从PLC或控制系统获取）或日历时间，对照`3.1.5`中配置的维护周期，自动生成维护提醒或工单，通知维护部门。
*   **操作流程:** 监控人员通过PC Web端查看设备状态列表、地图或OEE报表。维护人员记录故障处理和维护信息。
*   **数据需求:** 设备实时状态数据（来自接口），设备主数据（含维护周期），设备状态历史表，故障记录表，OEE计算所需原始数据和结果表，维护任务表（可选）。
*   **界面交互要点:** 设备状态颜色区分醒目（如 绿-运行，灰-空闲，红-故障，黄-计划停机/维护）。OEE结果以图表（如趋势图、构成图）展示。
*   **关键业务规则:** 设备状态映射需准确。停机时间分类（计划/非计划）需明确记录。OEE计算公式和参数来源需标准化。

#### 3.4.6. 质量检验执行 (Quality Inspection Execution)

*   **功能描述:** 支持在生产流程中（通常是退火后）执行质量检验操作，录入结果，并自动判定。
*   **实现逻辑:**
    *   **任务生成与分配:**
        *   **触发:** 当料框/料垛状态变为 `待检验`，并且其当前位置处于 `质检区` 或指定的 `质检工位` 时。
        *   **显示:** 系统自动将这些待检物料显示在质检员的操作界面（PC或PDA）的“待检任务列表”中。
    *   **检验过程:**
        1.  质检员从列表中选择一个任务，或直接扫描待检料框/料垛的二维码。
        2.  **显示标准:** 系统根据物料关联的`产品编码`，从`3.1.7`（或通过`4.1.2`从QMS同步）加载其对应的**生效状态**的`质量标准`，并在界面上显示：需要检验的`检验项目`列表、每个项目的`标准值/范围`、`检测方法`提示。
        3.  **结果录入:** 质检员按照标准进行检验操作，然后在界面上录入每个项目的实际`检验结果`。
            *   支持多种输入类型：数值（如硬度值）、布尔（如表面是否合格 OK/NG）、文本（如缺陷描述）。
            *   **(可选) 仪器对接:** 若检验仪器（如电子秤、硬度计）能通过`4.2.2接口`连接，系统可提供按钮触发仪器读数并自动填充结果。
        4.  **自动判定:** 每录入一项结果，系统可根据`3.1.7`配置的`判定规则`，实时判断该单项是否合格。所有必检项录入完成后，系统根据整体判定规则（如：所有项合格才算合格，或允许某些项在特定范围内）给出该料框/料垛的**最终判定结果**（合格/不合格）。
        5.  **提交确认:** 质检员复核录入结果和系统判定，无误后点击“**提交检验结果**”。
    *   **结果处理:**
        *   **记录:** 系统保存详细的检验记录：`检验任务ID`, `物料ID`, `检验项目代码`, `标准值`, `检验结果`, `单项判定`, `最终判定结果`, `检验员ID`, `检验时间`。
        *   **状态更新:** 更新被检料框/料垛的状态为 `已检验(合格)` 或 `已检验(不合格)`。
        *   **不合格品处理:** 若最终判定为`不合格`，系统可根据预设流程执行后续操作：
            *   触发报警或通知。
            *   自动生成将其移动到`不合格品区`的`物流任务`。
            *   通过`4.1.2接口`将不合格信息推送给QMS系统，启动评审或报废流程。
            *   （可选）要求操作员选择不合格原因代码。
*   **操作流程:** 质检员登录系统，查看待检列表，选择任务或扫码，按界面显示的项点录入结果，确认提交。
*   **数据需求:** 质量标准主数据，待检任务列表，检验结果记录表，物料库存状态，不合格品处理流程配置。
*   **界面交互要点:** 待检列表清晰。检验录入界面需并排显示项目、标准和结果录入框。自动判定结果醒目。仪器对接按钮方便。
*   **关键业务规则:** 检验必须依据当前产品生效的质量标准。所有必检项必须录入才能提交。不合格品的处理流程需闭环。检验数据需完整准确记录，以支持`3.5.2 质量分析`和追溯。

### 3.5. 报表与看板模块 (REPORT)

**核心功能:** 提供数据可视化展示和统计分析功能，支持实时监控、管理决策和持续改进。

#### 3.5.1. 生产分析报表 (Production Analysis Reports)

*   **功能描述:** 提供各类与生产执行相关的统计分析报表。
*   **实现逻辑:** 基于记录的生产实绩数据 (`3.3`, `3.4`) 生成报表。
    *   **产量报表:**
        *   按班次/日/周/月统计各`产品代码`的`总产量` (按重量Tons)、`合格产量`、`不合格产量`、`合格率 (%)`。
        *   可按`退火炉`维度统计。
    *   **炉次效率报表:**
        *   统计指定时间段内各`退火炉`的`炉次数量`、`平均装载率 (%)` (`实际装炉总重 / (炉次数 * 额定容量)`)、`平均炉次时间` (从装炉完成到出炉完成)。
        *   `单位产品能耗` (kWh/Ton): 统计总能耗（需`4.2.1`接口提供能耗数据）除以总产量。可按炉次、按产品统计。
        *   `工艺符合度分析`: 统计炉次中关键工艺参数（如保温温度）偏离设定范围的时间占比或次数。
    *   **计划执行报表:**
        *   `生产计划达成率 (%)`: 对比`3.3.1`中的生产计划，`实际完成产量 / 计划产量`。
        *   `退火任务按时完成率 (%)`: `按计划出炉时间完成的任务数 / 总完成任务数`。
        *   `延误分析`: 统计延误的任务数量、平均延误时间，并可按记录的延误原因（需在任务异常处理时记录）分类统计。
    *   **趋势分析:** 对关键指标（如日产量、合格率、OEE）绘制时间趋势图（线图、柱状图）。
    *   **报表特性:** 支持按时间范围（日期选择控件）、产品、设备（炉号）、班次等维度进行筛选和分组。提供数据导出功能（Excel, CSV）。
*   **操作流程:** 用户通过PC Web端访问“报表中心”，选择报表类型，设置查询条件，系统生成并展示报表（表格+图表）。用户可查看、导出。
*   **数据需求:** 退火任务记录，炉次过程数据，物料检验结果，设备状态历史，能耗数据，生产计划数据。
*   **界面交互要点:** 报表菜单清晰分类。查询条件设置灵活。图表具有交互性（如下钻、筛选）。导出功能便捷。
*   **关键业务规则:** 报表数据来源必须准确、及时。统计口径和计算公式需明确定义并保持一致。

#### 3.5.2. 质量分析报表 (Quality Analysis Reports)

*   **功能描述:** 提供与产品质量相关的统计分析和追溯功能。
*   **实现逻辑:** 基于`3.4.6`记录的质量检验数据和`3.4.1`的过程数据。
    *   **质量检验汇总报表:**
        *   按时间、产品、批次、检验员等维度统计 `送检数量`、`合格数量`、`不合格数量`、`一次合格率 (%)`。
        *   分`检验项目`统计合格率，识别主要不合格项。
    *   **不合格品分析:**
        *   统计不合格品的`产品分布`、`批次分布`、`发生炉次/设备分布`。
        *   **柏拉图 (Pareto Chart):** 基于记录的不合格原因代码（若`3.4.6`有记录），分析主要不合格原因及其占比，找出关键少数（80/20原则）。
    *   **工艺-质量关联分析 (探索性):**
        *   选择一个`工艺参数`（如某段保温平均温度、保温总时间）和一个`质量指标`（如硬度平均值）。
        *   绘制**散点图**，观察两者是否存在相关性趋势。
        *   （可选，需数据量支持）计算相关系数。
        *   目的：帮助工艺工程师优化工艺参数以提升质量稳定性。
    *   **产品质量追溯 (Traceability):**
        *   **输入:** 提供`料框ID`、`料垛ID`或`产品批次号`作为查询条件。
        *   **输出:** 生成一份完整的正向/反向追溯报告，包含该物料相关的所有关键信息：
            *   基本信息：产品代码、批次号、料框/料垛ID。
            *   生产过程：关联的`退火任务ID`、执行`炉号`、`装/出炉时间`、`操作员`。
            *   工艺数据：执行的`工艺模板ID`和`版本号`，实际`温度曲线图`（关键点数据列表或缩略图）、关键`工艺参数`记录。
            *   质量数据：`检验时间`、`检验员`、各`检验项目`的`标准`、`实际结果`、`判定`。
            *   （可选）关联的上游（如轧制/分切批号）和下游（如包装号）信息（若接口支持）。
        *   报告形式应清晰、易于理解，可导出为PDF。
*   **操作流程:** 用户选择质量报表类型或追溯功能，输入查询条件，系统生成报告。
*   **数据需求:** 质量检验记录，不合格品记录，炉次过程数据，退火任务数据，物料批次信息。
*   **界面交互要点:** 质量报表提供SPC控制图（如 Xbar-R 图，若适用）等专业工具。追溯报告结构清晰，关键信息突出。
*   **关键业务规则:** 质量数据必须与生产过程数据准确关联。追溯链条需完整、可靠。

#### 3.5.3. 设备分析报表 (Equipment Analysis Reports)

*   **功能描述:** 提供设备运行效率、能耗、故障等相关的统计分析报表。
*   **实现逻辑:** 基于`3.4.5`记录的设备状态和运行数据。
    *   **OEE分析报表:**
        *   按设备（炉号、行车号、料车号）、按时间段（班/日/周/月）展示 `OEE` 及其三个构成因子：`可用率`、`性能率`、`质量率`。
        *   提供**OEE损失分析**：将 100% - OEE 的损失分解到各个环节（如计划停机损失、故障停机损失、速度损失、小停顿损失、废品损失），可用瀑布图展示。需详细记录停机原因代码。
    *   **能耗分析报表:**
        *   按设备、按时间段统计总能耗 (kWh)。
        *   计算 `单位产量能耗` (kWh/Ton) 或 `单位炉次能耗` (kWh/炉次)。
        *   （可选）分析不同产品或工艺的能耗差异。
    *   **故障分析报表:**
        *   统计设备 `故障次数`、`总故障时间`。
        *   计算 `MTBF (平均故障间隔时间)` = 总运行时间 / 故障次数。
        *   计算 `MTTR (平均修复时间)` = 总故障停机时间 / 故障次数。
        *   **故障原因柏拉图:** 基于记录的故障原因代码，分析主要故障原因分布。
    *   **维护记录分析 (若记录维护信息):**
        *   统计计划维护按时完成率。
        *   分析维护成本、备件消耗（若记录）。
*   **操作流程:** 用户选择设备报表类型，设置查询条件（设备、时间范围），系统生成报表。
*   **数据需求:** 设备状态历史（含停机原因），设备运行参数（能耗），产量数据，故障记录，维护记录（可选）。
*   **界面交互要点:** OEE分析使用瀑布图等可视化效果。故障分析提供MTBF/MTTR趋势图和柏拉图。
*   **关键业务规则:** OEE等指标的计算公式需标准化、透明化。停机原因分类需规范且记录到位。

#### 3.5.4. 实时监控看板 (Real-time Monitoring Dashboards)

*   **功能描述:** 在大屏幕或管理人员PC上，以可视化方式集中、实时地展示车间关键运行状态和KPI。
*   **实现逻辑:**
    *   设计多个不同主题的看板页面，数据需自动刷新（刷新频率可配置，如10-60秒）。
    *   **车间总览看板:**
        *   显示简化的车间布局图，实时标注设备（炉、车、行车）位置和状态颜色。
        *   关键KPI指标卡片：当日累计产量、合格率、实时OEE（可选）、进行中炉次数、待处理报警数。
        *   各库区（待退火、已退火）的实时容量占用率。
        *   滚动显示最新的报警信息或重要通知。
    *   **退火炉状态看板:**
        *   以卡片或列表形式展示所有退火炉。
        *   每个炉子显示：炉号、当前状态（颜色区分）、运行中的任务ID/产品、实时主控温度、当前工艺阶段、阶段剩余时间、炉次进度条、是否有报警。
        *   （可选）点击炉号可弹出实时温度曲线缩略图。
    *   **物流任务看板:**
        *   显示当前 `待分配`、`执行中` 的行车和料车任务列表（关键信息：任务ID、类型、物料、起止点、优先级、分配设备）。
        *   在地图上实时显示行车和料车的位置及状态。
        *   统计任务队列长度、平均等待时间等。
    *   **库存状态看板:**
        *   可视化展示各库区的库存占用图（如按库位填色）。
        *   按产品/状态分类的库存数量/重量分布图（饼图/柱状图）。
        *   高亮显示超期库存或触发库存预警的区域/物料。
    *   **KPI指标看板:**
        *   集中展示核心KPI（如班次产量达成率、实时合格率、设备综合故障率、单位能耗）的当前值、目标值（若设定）及达成情况（如红绿灯指示）。
        *   提供短期趋势图。
*   **操作流程:** 系统管理员配置看板布局和显示内容。用户打开看板URL进行实时监控。看板通常是只读的。
*   **数据需求:** 整合来自各模块（库存、生产、执行、设备、质量）的实时数据。
*   **界面交互要点:** 视觉设计清晰、美观，信息密度适中。使用醒目的图表（仪表盘、进度条、状态灯）、颜色、字体。支持不同分辨率屏幕适配（特别是大屏）。
*   **关键业务规则:** 数据刷新频率需满足监控实时性要求。报警信息需优先、醒目展示。看板内容应可根据不同角色或场景进行配置。

#### 3.5.5. 数据分析工具 (Data Analysis Tools)

*   **功能描述:** 提供更灵活的数据查询、自定义报表和数据导出能力。
*   **实现逻辑:**
    *   **自定义报表:**
        *   提供一个用户友好的报表设计器界面（通常面向管理员或高级用户）。
        *   允许用户通过拖拽方式选择数据源（预定义的业务视图或表）、选择所需字段、设置筛选条件、分组条件、排序方式。
        *   支持选择不同的图表类型（表格、线图、柱状图、饼图等）来展示结果。
        *   用户可以保存自己设计的报表模板，并分享给其他用户（需权限控制）。
    *   **高级数据查询:**
        *   提供一个强大的查询界面，允许用户针对核心业务实体（如料框、炉次、任务、检验记录）进行多条件组合查询。
        *   支持复杂条件（等于、不等于、大于、小于、包含、区间、为空等）。
        *   查询结果以表格形式展示，支持在线排序、筛选和分页。
    *   **数据导出:**
        *   所有标准报表、自定义报表和高级查询的结果列表，均提供导出功能。
        *   支持导出为常用格式，如 Excel (xlsx/xls), CSV。
        *   导出时可选择导出当前页或所有页数据。
    *   **数据钻取:** 在部分汇总类图表（如按产品统计产量的柱状图）上，支持点击某个数据点（如某个产品的柱子），下钻到该产品的明细数据列表或更细维度的分析图表。
*   **操作流程:** 用户根据需求使用自定义报表设计器创建报表，或使用高级查询界面进行即席查询，或在已有报表/查询结果上进行导出或钻取操作。
*   **数据需求:** 访问底层数据库或预构建的数据仓库/数据集市。
*   **界面交互要点:** 自定义报表设计器需直观易用，无需编程知识。查询界面条件设置清晰。钻取操作流畅自然。导出选项明确。
*   **关键业务规则:** 所有数据访问（查询、报表、导出）必须严格遵守用户的`数据权限` (`3.6.4`)。自定义报表和查询的性能需要优化，避免影响系统正常运行。

### 3.6. 用户权限管理模块 (USER)

**核心功能:** 管理系统用户、角色及其对系统功能和数据的访问权限，确保系统操作安全合规。

#### 3.6.1. 用户管理 (User Management)

*   **功能描述:** 管理系统用户的账号信息、基本资料和状态。
*   **实现逻辑:**
    *   提供用户账号的创建、编辑、查询、禁用/启用、删除（逻辑删除）功能。
    *   记录用户信息：`用户ID` (唯一, 如工号), `登录名` (唯一), `姓名`, `密码` (加密存储), `所属部门` (关联`3.6.2`), `岗位` (关联`3.6.2`), `联系方式` (手机/邮箱), `账号状态` (活动/锁定/禁用), `关联角色` (多对多, 关联`3.6.3`)。
    *   **密码管理:** 支持管理员重置用户密码，用户首次登录强制修改密码，密码过期提醒。需符合`3.6.5`设定的密码策略。
    *   **查询与筛选:** 支持按用户名、姓名、部门、状态等条件查询用户。
*   **操作流程:**
    1.  管理员登录，进入“用户权限 -> 用户管理”。
    2.  查看用户列表。
    3.  点击“新增”创建用户，填写信息，分配初始角色。
    4.  选择用户进行编辑、状态变更（禁用/启用）、重置密码、删除（标记为禁用）等操作。
*   **数据需求:** 用户信息表，用户-角色关联表。
*   **界面交互要点:** 用户列表清晰，支持搜索和筛选。编辑表单分组合理。密码输入以星号显示。
*   **关键业务规则:** 登录名全局唯一。密码必须加密存储，不可明文查看。用户删除为逻辑删除，保留历史操作记录关联。

#### 3.6.2. 组织结构管理 (Organization Structure Management)

*   **功能描述:** 维护企业的部门层级结构和岗位信息。
*   **实现逻辑:**
    *   **部门管理:**
        *   提供界面创建、编辑、删除部门信息。
        *   记录字段：`部门ID` (唯一), `部门名称`, `上级部门ID` (用于构建层级关系), `部门描述` (可选)。
        *   支持以树状结构展示和维护部门层级。
    *   **岗位管理:**
        *   提供界面创建、编辑、删除岗位信息。
        *   记录字段：`岗位ID` (唯一), `岗位名称` (如 车间主任, 调度员, 炉前工, 质检员), `岗位描述`。
    *   **用户关联:** 在`3.6.1`用户管理中，为用户指定其所属的`部门`和`岗位`。
*   **操作流程:**
    1.  管理员进入“用户权限 -> 组织结构”。
    2.  维护部门树状结构（增删改节点）。
    3.  维护岗位列表。
    4.  在用户管理界面为用户分配部门和岗位。
*   **数据需求:** 部门表，岗位表。
*   **界面交互要点:** 部门管理使用树形控件直观展示层级。
*   **关键业务规则:** 部门和岗位信息是进行数据权限配置（如下属数据查看）和工作流分配的基础。

#### 3.6.3. 角色定义 (Role Definition)

*   **功能描述:** 定义不同的用户角色，角色是权限分配的载体。
*   **实现逻辑:**
    *   提供界面创建、编辑、复制、删除角色。
    *   记录字段：`角色ID` (唯一), `角色名称` (如 管理员, 调度岗, 操作岗, 质检岗), `角色描述`, `状态` (启用/禁用)。
    *   **权限关联:** 每个角色关联一组权限集合（功能权限、数据权限），在`3.6.4`中配置。
    *   **角色分配:** 在`3.6.1`用户管理中，将一个或多个角色分配给用户。一个用户可以拥有多个角色，其最终权限是所有角色权限的并集。
    *   **(可选) 角色继承:** 支持定义角色之间的继承关系（如 班组长角色 继承 操作岗角色 的所有权限，并增加额外权限）。
*   **操作流程:**
    1.  管理员进入“用户权限 -> 角色管理”。
    2.  创建或编辑角色基本信息。
    3.  在“权限配置”(`3.6.4`)界面为该角色分配具体权限。
    4.  在“用户管理”(`3.6.1`)界面为用户分配角色。
*   **数据需求:** 角色表，角色-权限关联表，用户-角色关联表。
*   **界面交互要点:** 角色列表清晰。
*   **关键业务规则:** 角色是权限管理的核心机制 (RBAC - Role-Based Access Control)。用户的权限由其被赋予的角色决定。

#### 3.6.4. 权限配置 (Permission Configuration)

*   **功能描述:** 精细化地为角色分配系统中各项功能、操作和数据的访问权限。
*   **实现逻辑:**
    *   **权限分类:**
        *   **功能权限 (Menu/Page/Button Permissions):** 控制用户能看到哪些菜单项、能访问哪些页面、能点击哪些按钮（如 新增、编辑、删除、审批、强制完成等）。通常以树状结构展示系统所有功能点，管理员为角色勾选其允许访问的功能。
        *   **数据权限 (Data Permissions):** 控制用户能看到和操作哪些范围的数据。实现方式通常包括：
            *   `组织维度`: 只能看本部门/本班组的数据。
            *   `创建者维度`: 只能操作自己创建的数据。
            *   `设备/区域维度`: 只能看分配给自己负责的设备/区域的数据。
            *   `数据类型维度`: 如普通操作员不能查看成本相关数据。
            *   需要设计灵活的数据权限规则引擎，允许管理员为角色配置规则。
        *   **操作权限 (Action Permissions):** 对某些敏感操作（如 修改生效工艺、删除历史数据、手动干预自动流程）进行单独授权，并可关联审批流程。
    *   **权限分配:**
        *   主要将权限分配给**角色**。选择一个角色，然后为其配置允许的功能权限树节点、数据权限规则和特殊操作权限。
        *   （不推荐）允许将特定权限直接分配给用户，作为角色的补充或例外。
*   **操作流程:**
    1.  管理员进入“用户权限 -> 权限配置”。
    2.  选择目标角色。
    3.  在功能权限树上勾选允许访问的菜单/页面/按钮。
    4.  配置数据权限规则（如选择可见的部门、设备范围等）。
    5.  勾选允许执行的特殊操作权限。
    6.  保存配置。
*   **数据需求:** 系统权限项定义表（功能点、数据对象、操作点），角色-权限关联表。
*   **界面交互要点:** 功能权限树结构清晰，易于勾选。数据权限规则配置界面需灵活且易于理解。
*   **关键业务规则:** 权限配置需遵循最小权限原则。权限变更应有记录。用户的最终权限是其所有角色权限的并集，加上直接分配给用户的权限。

#### 3.6.5. 安全策略 (Security Policy)

*   **功能描述:** 配置系统级的安全相关参数和规则。
*   **实现逻辑:** 提供系统配置界面，允许管理员设置以下策略：
    *   **密码策略:**
        *   `最小密码长度` (如 8位)。
        *   `密码复杂度要求` (如 必须包含大小写字母、数字、特殊符号中的至少三种)。
        *   `密码有效期` (如 90天后强制过期)。
        *   `密码历史限制` (如 不允许使用最近5次用过的密码)。
        *   `登录失败锁定` (如 连续输错5次密码后锁定账号15分钟)。
    *   **会话管理:**
        *   `会话超时时间` (如 用户30分钟无操作自动登出)。
    *   **登录控制 (可选):**
        *   `IP地址白名单/黑名单`: 限制允许登录的IP范围。
        *   `登录时间限制`: 限制用户只能在特定时间段登录。
        *   `单点登录 (SSO)`: 若需与企业统一认证平台集成。
    *   **操作日志:**
        *   配置需要记录的操作日志范围和级别（如 记录所有关键数据的增删改操作、用户登录登出、权限变更等）。
        *   配置日志的`保存期限` (如 至少1年)。
*   **操作流程:**
    1.  系统管理员进入“系统设置 -> 安全策略”。
    2.  配置各项安全参数。
    3.  保存并启用策略。
*   **数据需求:** 安全策略配置存储表。
*   **界面交互要点:** 配置项清晰明了，配有解释说明。
*   **关键业务规则:** 安全策略应符合企业整体的信息安全要求。

## 4. 接口设计

### 4.1. 外部系统接口

#### 4.1.1. ERP系统集成

*   **目的:** 实现MES与企业核心ERP系统的数据交换，保障计划源头统一和生产实绩及时反馈。
*   **技术选型建议:** 优先采用基于HTTP/HTTPS的API接口（RESTful风格，JSON格式）。备选方案：数据库中间表/视图、文件交换（需约定格式和路径）。需明确接口的认证机制（如API Key, OAuth）。
*   **接口列表:**
    *   **接收生产订单 (ERP → MES):**
        *   触发方式: 定时轮询ERP接口 或 ERP主动推送。
        *   频率: 如每30-60分钟，或实时。
        *   数据内容 (示例): `{"orderNumber": "SO12345", "productCode": "AF-1100-H18-...", "quantity": 10.5, "unit": "Ton", "dueDate": "2023-11-15", "priority": 1, "customer": "Client A", "specialNotes": "..."}`
        *   MES处理: 将接收到的订单数据存入`3.3.1`生产计划表。
    *   **反馈生产进度/完工 (MES → ERP):**
        *   触发方式: MES中退火任务完成时 或 按日/班次汇总。
        *   数据内容 (示例): `{"mesTaskID": "ATASK001", "linkedOrderNumber": "SO12345", "productCode": "...", "completedQuantity": 1.2, "unit": "Ton", "completionTime": "2023-10-29T10:00:00", "status": "Completed", "actualStartTime": "...", "actualEndTime": "..."}`
        *   ERP处理: 更新对应订单的执行状态和已完成数量。
    *   **反馈物料消耗 (MES → ERP) (可选):**
        *   触发方式: 炉次完成时 或 按日汇总。
        *   数据内容: 炉次/任务关联的原材料批次及理论/实际消耗量（若MES管理此信息）。
        *   ERP处理: 用于库存扣减或成本核算。
    *   **反馈工时/设备成本要素 (MES → ERP) (可选):**
        *   触发方式: 按日/周/月汇总。
        *   数据内容: 各炉次/任务的实际工时、设备运行时间、能耗数据。
        *   ERP处理: 用于成本核算。
*   **错误处理:** 需定义接口错误码和重试机制。记录接口交互日志。

#### 4.1.2. 质量管理系统(QMS)集成

*   **目的:** 同步质量标准，反馈检验结果，实现质量流程闭环。
*   **技术选型建议:** 同ERP接口，优先API。
*   **接口列表:**
    *   **同步检验标准 (QMS → MES):**
        *   触发方式: QMS标准更新时推送 或 MES定时拉取。
        *   频率: 每日一次 或 实时。
        *   数据内容: `产品编码` 对应的 `检验项目`、`标准值/范围`、`检测方法`、`判定规则`、`标准版本号`。
        *   MES处理: 更新`3.1.7`质量标准主数据。
    *   **反馈检验结果 (MES → QMS):**
        *   触发方式: MES中`3.4.6`检验任务完成并提交时。
        *   频率: 实时 或 按批次汇总。
        *   数据内容 (示例): `{"inspectionRecordID": "QI001", "materialID": "BIN007", "productCode": "...", "batchNumber": "...", "inspectionTime": "...", "inspector": "...", "overallResult": "Passed/Failed", "items": [{"itemCode": "Hardness", "standard": "HV 30-35", "result": "32.5", "itemResult": "Passed"}, ...]}`
        *   QMS处理: 记录检验结果，用于质量统计分析和证书生成。
    *   **不合格品处理协同 (双向):**
        *   `MES → QMS`: 上报不合格品信息（物料ID, 产品, 批次, 不合格项, 结果）。
        *   `QMS → MES`: QMS评审后下发处理指令（如 返工/报废/降级）。
        *   `MES → QMS`: MES执行指令后反馈处理结果。
        *   交互方式：API调用或消息队列。
    *   **提供质量追溯数据查询接口 (MES → QMS):**
        *   MES提供API，允许QMS根据`批次号`或`物料ID`查询`3.5.2`中定义的追溯信息。

#### 4.1.3. 仓储系统(WMS)集成 (若需要与原料库/成品库交互)

*   **目的:** 实现车间库存与仓库库存的信息同步和调拨协同。
*   **技术选型建议:** 同ERP接口，优先API。
*   **接口列表:**
    *   **库存信息同步 (双向):**
        *   同步内容: 特定物料（如待退火料卷、已包装成品卷）在MES管理的区域和WMS管理的库区的库存数量和状态。
        *   方式: 定时比对差异 或 基于库存变更事件触发增量同步。
        *   频率: 根据业务需求，可能需要较高频率（如每10-30分钟）。
    *   **物料调拨请求与反馈 (双向):**
        *   `MES → WMS`: MES需要原料（如特定批次的铝卷）时，生成调拨请求。
        *   `WMS → MES`: WMS完成出库并送达车间入口后，反馈信息。
        *   `MES → WMS`: MES完成退火和检验，待入成品库时，生成调拨请求。
        *   `WMS → MES`: WMS确认入库后反馈。
        *   交互方式: API任务接口。
    *   **库位信息共享 (可选):** 若需要，可提供接口查询对方管理的库位状态。

### 4.2. 设备接口

#### 4.2.1. PLC 集成 (西门子 S7-1200)

*   **核心重要性:** 这是MES系统与退火炉进行实时数据交互和控制的关键接口，其可靠性和准确性直接影响系统核心功能。
*   **通讯协议:** **强制要求使用 OPC UA (OPC Unified Architecture)**。MES系统侧需使用支持OPC UA的客户端SDK。PLC侧S7-1200需启用OPC UA服务器功能（需要相应固件版本和授权）。
*   **网络架构:** MES服务器（或专用接口服务器）与S7-1200 PLC通过工业以太网连接。网络需稳定可靠，建议划分独立的工业网络段。
*   **安全:** OPC UA通讯必须启用安全策略（如用户认证、数据加密签名），确保通讯安全。
*   **数据点表 (关键依赖):**
    *   **必须由自动化工程师提供一份详细的《S7-1200 PLC - MES OPC UA接口数据点表》文档。**
    *   该文档需明确定义MES需要读/写的**每一个数据点**在PLC中的**精确地址**（如DB块号 + 偏移量 或 符号地址）、**数据类型**（Bool, Int, Real, String, Array of Real等）、**读写权限**（Read Only / ReadWrite）、**建议的读写频率/时机**、以及**每个数据点的业务含义**。
    *   **数据点分类示例 (需极度细化):**
        *   **设备状态 (PLC → MES, ReadOnly):**
            *   `运行状态`: (整数) 0=停止, 1=运行中, 2=暂停, 3=故障, 4=维护...
            *   `运行模式`: (整数) 0=手动, 1=自动
            *   `炉门状态`: (Bool) True=开, False=关
            *   `急停状态`: (Bool) True=按下
            *   `系统报警总状态`: (Bool) True=有报警
            *   `当前活动报警代码`: (整数数组或字符串)
            *   `当前执行的任务ID` (由MES下发并回读确认): (整数或字符串)
            *   `当前工艺阶段序号`: (整数)
        *   **工艺参数 (PLC → MES, ReadOnly):**
            *   `实际温度数组`: (Array of Real) 炉内多个测温点的实时温度值。需明确数组大小和测点对应关系。
            *   `炉膛压力`: (Real)
            *   `气氛流量`: (Real)
            *   `循环风机频率`: (Real)
            *   `本阶段已执行时间`: (Real 或 DWord, 单位秒)
            *   `加热器功率/电流`: (Real)
        *   **能耗监控 (PLC → MES, ReadOnly):**
            *   `瞬时总功率`: (Real)
            *   `累计电能消耗`: (Real 或 DWord)
            *   `累计气体消耗`: (Real 或 DWord)
        *   **任务执行反馈 (PLC → MES, ReadOnly):**
            *   `装炉完成信号`: (Bool) 由PLC确认物理装载完成。
            *   `退火开始信号`: (Bool) 工艺程序正式开始运行。
            *   `退火完成信号`: (Bool) 工艺程序正常结束。
            *   `允许出炉信号`: (Bool) 满足出炉条件（如降温完成）。
            *   `出炉完成信号`: (Bool) 由PLC确认物理出炉完成。
            *   `任务执行异常代码`: (整数)
        *   **配方与指令下发 (MES → PLC, ReadWrite):**
            *   `下发任务ID`: (整数或字符串)
            *   `下发工艺模板版本`: (字符串)
            *   `下发温度设定曲线` (分段参数数组): (Array of Struct) 每段包含目标温度、时间、速率等。
            *   `下发气氛/压力/风机设定值`: (Real)
            *   `控制指令字`: (Word/DWord) 如 bit0=启动, bit1=停止, bit2=暂停, bit3=复位... (需详细定义位含义和握手机制)。
            *   `指令序列号`: (整数) 用于指令确认，防止重复执行。
            *   `时钟同步`: (DTL) MES定期向PLC同步时间。
*   **交互逻辑 (示例):**
    *   **任务启动:** MES确认炉子空闲 -> MES下发任务ID和工艺配方到PLC指定DB块 -> MES设置启动指令位 -> PLC检测到指令位和有效配方 -> PLC执行启动逻辑 -> PLC置位“任务已接收”或“退火开始”信号，并回写当前执行的任务ID -> MES读取到反馈信号，确认启动成功。
    *   **过程监控:** MES按频率读取设备状态和工艺参数 -> MES进行显示、记录、比对、报警。
    *   **报警处理:** PLC置位报警状态字并写入报警代码 -> MES读取到报警 -> MES记录并通知 -> 操作员处理后在MES确认 -> MES发送复位指令给PLC（若需） -> PLC清除报警状态。
    *   **任务完成:** PLC完成工艺 -> PLC置位“退火完成”信号 -> （等待降温） -> PLC置位“允许出炉”信号 -> MES提示操作员出炉 -> 操作员完成出炉 -> （可选）操作员在MES确认 -> MES（或PLC）置位“出炉完成”信号 -> MES更新任务状态，炉子变为空闲。
*   **通讯异常处理:**
    *   MES需实现心跳检测机制，监控与PLC的OPC UA连接状态。
    *   连接中断时，MES界面提示通讯中断，并尝试自动重连。
    *   PLC侧应设计通讯中断时的安全处理逻辑（保持/停机/安全状态）。
    *   通讯恢复后，需有机制进行状态和数据的同步（如MES重新读取PLC状态，或PLC缓存关键事件待恢复后上传）。

#### 4.2.2. 其他设备接口

*   **条码/二维码扫描器 (Scanner):**
    *   **连接方式:**
        *   `PDA内置扫描头`: 通过设备SDK集成。
        *   `USB扫描枪 (模拟键盘模式)`: 作为HID设备，扫描结果直接输入到当前焦点输入框。MES客户端需捕获输入。
        *   `串口扫描枪/固定式扫描器`: 通过RS232/RS485串口通讯。MES客户端需监听串口，解析数据。
        *   `网络扫描器`: 通过TCP/IP或HTTP协议通讯。
    *   **数据内容:** 通常是解码后的字符串（即料框ID）。
    *   **交互逻辑:** MES客户端（PC或PDA）在需要扫码的界面激活监听或输入框，获取扫描结果，填充到对应字段，并触发后续业务逻辑（如查询、校验、确认）。
*   **行车/自动料车控制系统 (Crane/AGV Control System):**
    *   **连接方式:** 通常通过工业以太网。
    *   **通讯协议:** **需要与设备供应商确认具体的协议。** 可能是：
        *   `TCP/IP Socket`: 自定义报文格式。
        *   `Web Service / HTTP API`: 控制系统提供API供MES调用。
        *   `OPC UA`: 如果控制系统支持作为OPC UA服务器。
        *   `数据库接口`: 通过读写中间数据库表交互（不推荐，实时性差）。
    *   **数据内容:**
        *   `MES → 控制系统`: 下发物流任务指令（任务ID, 动作类型, 物料ID, 起始/目标位置坐标或ID）。
        *   `控制系统 → MES`: 反馈任务执行状态（接收/执行中/完成/失败）、设备实时状态（位置、速度、载荷、电量、故障代码）。
    *   **交互逻辑:** MES (`LOGI_Dispatch`) 分配任务后调用接口下发指令 -> 控制系统执行任务并周期性或事件触发地反馈状态 -> MES接收反馈并更新任务和设备状态。
*   **电子秤/称重仪表 (Scale):**
    *   **连接方式:** 常见为串口(RS232/RS485)或以太网TCP/IP。
    *   **通讯协议:** 通常有标准的仪表通讯协议（需查阅仪表手册）或简单的ASCII码指令响应。
    *   **数据内容:** 实时重量值（含单位）。
    *   **交互逻辑:** MES在需要称重的环节（如`3.2.1`料框注册），向仪表发送读取指令（或配置仪表主动发送），获取稳定后的重量数据，填充到界面字段。
*   **检验仪器 (Inspection Instruments) (可选):**
    *   **连接方式:** 多样，可能为串口、以太网、USB、专用软件接口或文件共享。
    *   **通讯协议:** 高度依赖具体仪器，需与仪器供应商确认。
    *   **数据内容:** 特定检验项目的测量结果。
    *   **交互逻辑:** MES在`3.4.6`检验执行时，可触发仪器测量（若支持），并接收返回的测量结果，自动填充到对应的检验项目字段。

## 5. 非功能需求

### 5.1. 性能需求

*   **实时数据采集与监控:**
    *   `PLC关键工艺参数 (温度) 采集-处理-展示延迟`: ≤ 2秒。
    *   `PLC其他工艺参数采集延迟`: ≤ 6秒。
    *   `设备 (PLC/行车/AGV) 状态变化反映到监控界面延迟`: ≤ 5秒。
    *   `工艺参数超限/设备故障 检测到触发界面报警延迟`: ≤ 3秒。
*   **业务操作响应时间 (并发用户数50):**
    *   `用户登录验证`: ≤ 2秒。
    *   `PDA扫码识别并查询料框信息`: ≤ 3秒。
    *   `库存查询 (常用条件, 数据量1万条料框)`: ≤ 3秒。
    *   `提交料框注册/状态变更/检验结果`: ≤ 3秒。
    *   `退火炉排程计算 (待排任务50个, 炉数10台)`: ≤ 60秒（可接受后台异步执行）。
    *   `物流任务自动生成 (基于发布排程)`: ≤ 10秒。
    *   `物流任务下发到设备接口`: ≤ 2秒。
*   **报表与查询:**
    *   `标准生产/质量日报表生成`: ≤ 10秒 (数据量一个月)。
    *   `复杂统计分析报表 (如OEE分析)`: ≤ 30秒 (数据量一个月)。
    *   `质量追溯报告生成`: ≤ 15秒。
    *   `历史数据查询 (3个月内)`: ≤ 5秒。
*   **系统容量与并发:**
    *   支持同时监控 `20` 台退火炉的实时数据。
    *   支持 `10` 台行车和 `10` 台自动料车的状态监控与任务调度。
    *   支持至少 `50` 个并发用户（包括PC端和PDA端）执行常规操作，系统CPU和内存使用率峰值 ≤ 75%。
    *   数据库设计需支持至少 `3` 年的生产和过程数据存储与查询性能。

### 5.2. 易用性需求

*   **面向车间操作员 (PDA/固定终端):**
    *   `界面简洁`: 布局清晰，字体够大（≥16px），按钮尺寸易于触摸（≥ 44x44 CSS像素）。避免信息过载。
    *   `流程引导`: 操作步骤明确，提供必要的提示信息。关键操作（如装出炉确认）需二次确认防止误操作。
    *   `扫码高效`: 支持连续扫描。扫码成功/失败/重复的反馈（声音+视觉）必须及时、清晰。
    *   `状态可见`: 物料、设备的关键状态使用颜色（绿/黄/红）或图标直观表示。
    *   `减少输入`: 尽可能通过扫描、选择或系统自动填充，减少手动键盘输入。
    *   `适应环境`: 界面设计考虑车间光线、噪音、操作员可能戴手套等因素。
    *   `学习成本`: 新操作员经过 ≤ 4小时 的培训和练习，应能熟练掌握其岗位所需的核心操作。
*   **面向管理/调度/技术人员 (PC Web):**
    *   `导航清晰`: 系统菜单结构逻辑清晰，易于查找功能。
    *   `信息可视化`: 广泛使用图表（曲线图、柱状图、饼图、甘特图、仪表盘）展示数据和趋势。
    *   `操作便捷`: 提供批量处理（如批量确认任务）、快速搜索、数据筛选/排序等功能。
    *   `配置灵活`: 对于规则（排程、报警、库位分配）、参数（工艺、质量）等，提供易于理解和操作的配置界面。
    *   `帮助文档`: 提供在线帮助或操作手册。
*   **通用易用性:**
    *   `一致性`: 整个系统保持统一的界面风格、术语和操作逻辑。
    *   `反馈及时`: 用户操作后，系统应给出明确的处理结果或进度反馈。
    *   `语言支持`: 提供简体中文和英文两种界面语言切换选项。

### 5.3. 可靠性需求

*   **系统可用性:**
    *   `年可用率`: ≥ 99.9% （每年计划外停机时间 ≤ 8.76小时）。
    *   `核心功能 (数据采集、状态监控、基本执行) 可用率`: ≥ 99.95%。
    *   `平均故障间隔时间 (MTBF)`: ≥ 3000 小时。
    *   `平均故障修复时间 (MTTR)`: ≤ 2 小时 (对于关键故障)。
*   **数据准确性与完整性:**
    *   `关键数据采集精度`: 温度 ±1°C (取决于传感器和PLC精度)，时间 ±1秒。
    *   `物料追踪准确率`: 通过扫码等校验，确保系统记录的物料状态和位置与实际情况高度一致（盘点校验偏差率 ≤ 1%）。
    *   `数据校验`: 所有用户输入和接口传入的关键数据必须进行有效性校验（格式、范围、逻辑）。
    *   `事务完整性`: 涉及多表更新的关键业务（如出入库、任务完成）必须保证事务原子性，要么全部成功，要么全部回滚。
*   **容错与恢复:**
    *   `PLC通讯中断`: 中断期间，MES监控界面明确提示，尝试自动重连。PLC应能独立完成当前工艺段或进入安全状态。恢复后，自动同步状态和关键事件数据（如报警、阶段切换）。
    *   `网络中断 (PDA)`: PDA在网络中断时，应能本地缓存一定量的关键操作数据（如扫码记录），网络恢复后自动上传。需明确缓存容量（如支持离线工作2小时或缓存100条记录）。
    *   `服务器故障`: 应用服务器应支持冗余部署（如主备或集群）。数据库服务器需有高可用方案（如主从复制、故障切换）。
    *   `数据备份`:
        *   `数据库`: 每日全量备份，每小时增量备份（或配置事务日志连续备份）。备份文件异地存储。
        *   `配置文件`: 定期备份。
    *   `数据恢复`:
        *   `恢复点目标 (RPO)`: ≤ 1小时。
        *   `恢复时间目标 (RTO)`: ≤ 4小时 (对于灾难性恢复)。

### 5.4. 安全性需求

*   **身份认证与访问控制:**
    *   `身份认证`: 强制用户名密码登录。密码策略满足`3.6.5`要求。支持与企业LDAP/AD集成实现统一认证（可选）。
    *   `角色授权 (RBAC)`: 严格基于`3.6.3`定义的角色进行授权。权限配置满足`3.6.4`要求，实现功能权限和数据权限的精细化控制。遵循最小权限原则。
    *   `会话管理`: 实现`3.6.5`定义的会话超时策略。用户登出需清除会话。防止会话固定攻击。
    *   `接口安全`: 所有对外的API接口（与ERP/QMS/WMS等）必须使用HTTPS加密传输，并实现有效的认证授权机制（如API Key + IP白名单, OAuth2）。
    *   `设备接口安全 (OPC UA)`: 与PLC的OPC UA通讯必须启用安全策略（如`Basic256Sha256` Sign & Encrypt），使用用户证书或用户名密码进行认证。
*   **数据安全:**
    *   `传输加密`: Web访问强制HTTPS。内部服务间通讯建议也加密。
    *   `存储加密`: 用户密码、密钥等敏感信息必须使用强哈希算法（如bcrypt, scrypt）加盐存储。数据库连接密码等配置信息需加密存储。考虑对数据库中特别敏感的业务数据（如工艺配方核心参数）进行加密存储（可选，需评估性能影响）。
    *   `防注入攻击`: 所有接收用户输入或外部传入参数的地方（Web表单、API参数、URL参数）必须进行严格的输入验证和过滤，有效防止SQL注入、跨站脚本(XSS)、命令注入等攻击。使用参数化查询或ORM。
    *   `访问控制`: 数据库访问使用权限受限的专用账户。禁止使用高权限账户运行应用。
*   **审计与合规:**
    *   `操作日志`: 对所有用户的登录、登出、重要操作（如主数据修改、任务创建/删除、权限变更、工艺/质量标准修改、手动状态干预、报警确认）、数据导出等行为进行详细记录。日志内容包括：时间戳、用户ID、来源IP、操作对象、操作类型、操作内容（关键参数变更前后值）、结果（成功/失败）。
    *   `日志保护`: 操作日志需存储在安全的位置，具备防篡改能力（如写入独立日志系统、校验和保护）。
    *   `日志查询与审计`: 提供便捷的日志查询界面，支持按用户、时间、操作类型等条件检索。日志保存期限满足`3.6.5`配置（至少1年）。
    *   `安全事件监控`: （可选）集成或实现对可疑活动（如短时间多次登录失败、异常IP登录、权限提升尝试）的监控和告警。
*   **系统完整性与部署安全:**
    *   `输入验证`: 对所有外部输入进行严格验证（类型、长度、格式、范围）。
    *   `错误处理`: 向用户显示的错误信息应友好且不暴露系统内部敏感细节（如堆栈跟踪、SQL语句）。详细错误记录在后台日志中。
    *   `依赖库安全`: 定期扫描项目使用的第三方库，及时更新存在已知漏洞的版本。
    *   `部署安全`: 生产环境服务器应进行安全加固（最小化安装、关闭不必要端口、及时打补丁）。应用部署使用非root用户。网络层面配置防火墙规则。

## 6. 数据字典 (示例)

**目的:** 定义系统中核心数据实体的关键字段，确保开发过程中的数据结构一致性。以下为部分核心表示例，需在详细设计阶段补充完整。

**表名: `bin_master` (料框主表)**

| 字段名 | 数据类型 | 长度/精度 | 是否允许为空 | 主/外键 | 描述 | 示例值 |
|---|---|---|---|---|---|---|
| `bin_id` | VARCHAR | 30 | N | PK | 料框唯一ID (来自二维码) | BIN-000001 |
| `spec_code` | VARCHAR | 20 | N | FK (->bin_spec.spec_code) | 料框规格代码 | LK001 |
| `current_status` | VARCHAR | 20 | N |  | 当前状态 |待退火 |
| `current_location_id` | VARCHAR | 30 | Y | FK (->location.loc_id / equipment.equip_id) | 当前位置ID | DA-01-01-A |
| `current_location_type` | VARCHAR | 10 | Y |  | 当前位置类型 (库位/设备) | LOCATION |
| `product_code` | VARCHAR | 50 | Y | FK (->product.prod_code) | 关联产品代码 | AF-1100-H18-... |
| `batch_number` | VARCHAR | 30 | Y |  | 批次号 | 20231028001 |
| `weight_kg` | DECIMAL | 10, 2 | Y |  | 重量 (公斤) | 1250.50 |
| `stack_id` | VARCHAR | 30 | Y | FK (->stack_master.stack_id) | 所属料垛ID (若在垛中) | STK-0001 |
| `register_time` | TIMESTAMP |  | N |  | 注册入系统时间 | 2023-10-28 08:00:00 |
| `last_update_time` | TIMESTAMP |  | N |  | 最后更新时间 | 2023-10-28 09:15:30 |
| `operator_id` | VARCHAR | 20 | Y |  | 最后操作员工号 | OP001 |

**表名: `annealing_task` (退火任务表)**

| 字段名 | 数据类型 | 长度/精度 | 是否允许为空 | 主/外键 | 描述 | 示例值 |
|---|---|---|---|---|---|---|
| `task_id` | VARCHAR | 30 | N | PK | 退火任务唯一ID | ATASK-00001 |
| `erp_order_number` | VARCHAR | 50 | Y |  | 关联ERP订单号 | SO12345 |
| `planned_furnace_id` | VARCHAR | 20 | Y | FK (->equipment.equip_id) | 计划执行炉号 | FURNACE-01 |
| `assigned_furnace_id` | VARCHAR | 20 | Y | FK (->equipment.equip_id) | 实际执行炉号 | FURNACE-01 |
| `product_code` | VARCHAR | 50 | N | FK (->product.prod_code) | 产品代码 | AF-1100-H18-... |
| `total_planned_weight_kg` | DECIMAL | 10, 2 | Y |  | 计划总重量 | 38500.00 |
| `process_template_id` | VARCHAR | 30 | N | FK (->process_template.template_id) | 关联工艺模板ID | PROC-TPL-001 |
| `process_template_version` | VARCHAR | 10 | N |  | 工艺模板版本号 | v1.2 |
| `priority` | INT |  | Y |  | 任务优先级 | 1 |
| `task_status` | VARCHAR | 20 | N |  | 任务状态 | 执行中 |
| `planned_load_time` | TIMESTAMP |  | Y |  | 计划装炉时间 | 2023-10-29 14:00:00 |
| `planned_unload_time` | TIMESTAMP |  | Y |  | 计划出炉时间 | 2023-10-30 02:00:00 |
| `actual_load_start_time` | TIMESTAMP |  | Y |  | 实际装炉开始时间 | 2023-10-29 14:05:10 |
| `actual_load_end_time` | TIMESTAMP |  | Y |  | 实际装炉结束时间 | 2023-10-29 14:25:30 |
| `actual_unload_start_time` | TIMESTAMP |  | Y |  | 实际出炉开始时间 | 2023-10-30 02:10:05 |
| `actual_unload_end_time` | TIMESTAMP |  | Y |  | 实际出炉结束时间 | 2023-10-30 02:35:15 |
| `creation_time` | TIMESTAMP |  | N |  | 任务创建时间 | 2023-10-28 10:00:00 |

**(注: 还需定义 料垛表、库位表、设备表、工艺模板详情表、质量标准表、检验记录表、状态/位置历史表、用户/角色/权限表、日志表等。详细字段需在数据库设计阶段确定。) **

## 7. 部署概述

*   **部署模式:** 初期采用**本地化部署 (On-Premise)**，所有服务和数据库部署在客户内部的服务器上。
*   **服务器建议:**
    *   `应用服务器`: 建议采用虚拟化或物理服务器，配置需满足性能需求（如 CPU ≥ 8核, RAM ≥ 16GB）。支持负载均衡和冗余部署（至少主备）。操作系统建议 Linux (如 CentOS, Ubuntu) 或 Windows Server。
    *   `数据库服务器`: 建议使用独立的服务器，配置需根据数据量和并发访问量确定（如 CPU ≥ 8核, RAM ≥ 32GB, 高速磁盘阵列如SSD RAID）。需配置高可用方案。
    *   `接口服务器 (可选)`: 若OPC UA客户端、设备适配器等资源消耗较大，可部署在独立的接口服务器上。
*   **数据库选型:**
    *   `关系型数据库`: 用于存储主数据、事务数据、状态等。可选 PostgreSQL (推荐), SQL Server, MySQL。需考虑授权费用。
    *   `时序数据库 (推荐)`: 用于存储大量的工艺过程时间序列数据。可选 InfluxDB, TimescaleDB (基于PostgreSQL)。能显著提高过程数据读写和查询性能。
*   **网络要求:** 应用服务器、数据库服务器、接口服务器、PLC、行车/AGV控制系统、操作终端（PC/PDA）需处于稳定可靠的同一局域网或可互通的网络环境中。建议划分独立的工业网络或VLAN。确保MES服务器到各设备接口的网络带宽和延迟满足要求。
*   **客户端要求:**
    *   `PC Web端`: 支持主流现代浏览器（Chrome, Firefox, Edge 最新版本）。
    *   `PDA端`: 开发原生App (Android 或 iOS) 或 PWA/Web App。需考虑PDA设备的硬件配置（内存、处理器、扫描性能、电池续航）。
*   **环境分离:** 必须建立独立的**开发环境、测试环境、预生产环境(UAT)和生产环境**，确保代码和配置变更经过充分验证才能部署到生产。
*   **部署方式:** 采用容器化部署（如 Docker + Kubernetes/Docker Swarm）能提高部署效率、一致性和可管理性（推荐）。传统部署方式亦可。需提供详细的部署手册。

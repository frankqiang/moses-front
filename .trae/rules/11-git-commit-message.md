---
description: 生成 git 提交消息规范
globs: 
alwaysApply: false
---
# Git 提交消息规范

## 格式要求

【必须】使用以下格式：
```
<类型>([可选范围]): <简短描述>

[可选详细描述]
[可选关联问题]
```
- **范围 (scope)** 是可选的，用于说明此次提交影响的模块或功能区域
- 范围可以使用中文模块名，如 `routing`、`mock`、`component` 等
- 支持多级范围，如 `process-management/routing`、`routing-form` 等

## 类型标识

【必须】使用以下类型之一：
- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档修改
- `style`: 格式修改（不影响功能）
- `refactor`: 代码重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建/工具变动

## 范围 (Scope)
【推荐】提供提交影响的范围，例如：
- `feat(auth)`: 认证相关功能
- `fix(routing)`: 工艺路线模块修复
- `feat(mock)`: Mock数据相关功能
- `refactor(router)`: 路由代码重构
- `fix(routing-form)`: 工艺路线表单修复
- `feat(process-management/routing)`: 工序管理中的工艺路线功能
- `docs(routing)`: 工艺路线相关文档
- `fix(component)`: 组件修复

## 描述规范

【必须】遵循：
- 使用中文，简洁明了
- 动词开头，现在时（"添加"非"添加了"、"修复"非"修复了"）
- 不以句号结尾
- 可以包含关键信息，如具体修复的问题或新增的功能点

## 示例

### 基础示例
```
feat(routing): 为工序选择模态框添加分页功能
fix(routing-form): 修复代码字段重复验证问题
refactor(router): 优化路线代码唯一性校验逻辑
docs(routing): 更新工艺路线工序步骤管理文档
```

### 复杂功能示例
```
feat(process-management/routing): 优化工艺路线工序步骤管理

- 重构步骤详情配置，支持动态流程逻辑
- 添加时间标准配置功能
- 优化用户交互体验

close #123
```

### Mock数据示例
```
feat(mock): 统一响应工具函数

- 新增标准化响应工具函数 (success, error, paginated, batch)
- 解决项目中响应格式不一致问题
- 完善文档体系，包含迁移指南和使用示例
```

## 分支管理

【推荐】使用：
- `main`: 主分支
- `feat/<功能名>`: 新功能分支
- `fix/<问题描述>`: 修复分支

## 🔍 检查清单：提交前确认

在执行 `git commit` 之前，【必须】逐项确认以下内容：

- [ ] **[P0]** 提交消息格式：本次提交的 commit message 是否严格遵循了 `<类型>([可选范围]): <简短描述>` 格式？（包括类型选择、描述简洁性、时态、标点等）
- [ ] **[P0]** 代码质量：提交的代码是否无语法错误、无编译警告或错误？
- [ ] **[P0]** 类型定义 (TypeScript)：所有相关的 TypeScript 类型定义是否完整、准确且已通过类型检查？
- [ ] **[P0]** 代码规范符合性：代码是否遵循了项目约定的代码风格（如 ESLint, Prettier 配置）和命名规范？
- [ ] **[P0]** 功能完整性：本次提交是否完整实现了其宣称的功能或修复？是否存在未完成或遗漏的逻辑？
- [ ] **[P0]** 错误处理：相关的代码路径是否添加了必要的错误处理（参考 `error-handling.mdc`）？
- [ ] **[P0]** 测试覆盖：是否为新增或修改的功能编写了必要的单元测试/集成测试，并且所有测试是否通过？
- [ ] **[P0]** 本地工具检查：是否已在本地运行并通过了 ESLint, Prettier 等代码质量和格式化工具的检查？
- [ ] **[P0]** 提交内容相关性：本次提交是否只包含与当前 `<类型>([可选范围]): <简短描述>` 直接相关的代码变更，没有混杂不相关的修改？

- [ ] **[P0]** 详细描述与关联问题（可选）：如果提交涉及复杂更改，是否在消息体中提供了详细描述？如果关联了 issue，是否已正确标记？
- [ ] **[P0]** 测试覆盖：是否为新增或修改的功能编写了必要的单元测试/集成测试，并且所有测试是否通过？
- [ ] **[P0]** 本地工具检查：是否已在本地运行并通过了 ESLint, Prettier 等代码质量和格式化工具的检查？
- [ ] **[P0]** 提交内容相关性：本次提交是否只包含与当前 `<类型>([可选范围]): <简短描述>` 直接相关的代码变更，没有混杂不相关的修改？

- [ ] **[P0]** 详细描述与关联问题（可选）：如果提交涉及复杂更改，是否在消息体中提供了详细描述？如果关联了 issue，是否已正确标记？

## 常用范围参考

根据项目实际使用情况，以下是常用的范围标识：

### 业务模块
- `routing`: 工艺路线管理
- `process-management`: 工序管理
- `routing-form`: 工艺路线表单
- `operations`: 工序操作
- `auth`: 认证授权

### 技术模块
- `mock`: Mock数据和API
- `component`: 通用组件
- `router`: 路由配置
- `form`: 表单相关
- `table`: 表格相关
- `api`: API接口

### 工程化
- `build`: 构建配置
- `config`: 配置文件
- `deps`: 依赖管理
- `ci`: 持续集成
/**
 * 文件名称：index.js
 * 文件描述：维护计划管理常量配置统一导出
 * 创建日期：2025-10-15
 * 修改记录：
 *   - 2025-10-15: 初始创建
 *   - 2025-10-15: 移除硬编码枚举，改用字典系统
 *
 * 说明：
 * - 枚举值（如维护类型、周期类型等）已迁移到字典系统，通过 tpmDictionaryMixin 获取
 * - 此文件仅保留UI展示相关的配置（如Tag颜色映射）
 */

// ============================================
// UI展示配置（不是枚举值，需要保留）
// ============================================

/**
 * 状态标签配置
 * 用于表格中状态字段的Tag颜色显示
 */
export const STATUS_TAG_CONFIG = {
  typeMap: {
    '启用': 'success',
    '禁用': 'info'
  },
  textMap: {
    '启用': '启用',
    '禁用': '禁用'
  }
}

/**
 * 维护类型标签配置
 * 用于表格中维护类型字段的Tag颜色显示
 */
export const MAINTENANCE_TYPE_TAG_CONFIG = {
  typeMap: {
    '日常保养': 'primary',
    '定期检查': 'success',
    '大修': 'warning',
    '专项维护': 'danger'
  }
}

/**
 * 周期类型标签配置
 * 用于表格中周期类型字段的Tag颜色显示
 */
export const CYCLE_TYPE_TAG_CONFIG = {
  typeMap: {
    '按时间': 'primary',
    '按运行时长': 'success',
    '按生产批次': 'warning'
  }
}

// ============================================
// 导出表格配置
// ============================================
export * from './table-config'

// ============================================
// 导出表单配置
// ============================================
export * from './form-config'
